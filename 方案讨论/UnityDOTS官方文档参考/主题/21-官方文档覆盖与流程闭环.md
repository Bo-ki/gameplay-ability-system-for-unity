# 21 官方文档覆盖与流程闭环

## 职责

本主题维护官方文档结论如何进入行动报告、任务树、目标态 Spec、当前事实和进度状态，确保每次从官方文档学到的东西不会丢失、不会重复发现。

## 核心概念详解

### 覆盖闭环流程

```
Unity 官方文档读取
    → 提取规则/模式/约束
    → 判断影响范围
    → 反哺对应 owner
    → 行动报告/任务树/Spec/事实/治理规范更新
```

### 各类发现的归属判断

| 发现类型 | 反哺 Owner | 示例 |
|---|---|---|
| 改变目标态设计 | `01-目标态架构共识` | 官方 API 能力改变 Runtime Core phase 设计 |
| 暴露当前实现问题 | `00-当前架构事实/核心问题诊断` | 当前 ECB 使用模式违反官方建议 |
| 改变执行顺序/优先级 | `02-主线任务树` | 新发现需要先修 backbone 再继续功能 |
| 改变流程规范 | 对应主题文件夹内的治理规范 | 需要新的交还检查项 |
| 本轮临时发现 | `04-当前进度状态/迭代摘要` | 本轮验证中的特殊情况 |

## ODF 规则详解

| 编号 | 规则 | 说明 |
|---|---|---|
| `ODF-01` | DOTS 相关任务必须先读 `UnityDOTS官方文档参考/README.md` | 再按任务类型读单主题文档 |
| `ODF-02` | 行动报告必须列出 PackageCache 实际版本和相关主题 | 不得只写"最新版" |
| `ODF-03` | 官方依据改变目标态 → 反哺 `01-目标态架构共识` | 不能只记在行动报告里 |
| `ODF-04` | 官方依据暴露当前实现问题 → 反哺 `00-当前架构事实/核心问题诊断` | 附带具体规则编号 |
| `ODF-05` | 官方依据改变任务顺序 → 反哺 `02-主线任务树` | 更新任务优先级和依赖 |
| `ODF-06` | 官方依据改变流程 → 反哺对应主题文件夹内的治理规范 | 更新交还检查项 |
| `ODF-07` | 本轮发现 → 写入迭代摘要 | 判断是否升级为长期事实 |
| `ODF-08` | 保留旧聚合文档 → 迁移内容后删除 | 所有内容归位到单主题文档 |
| `ODF-09` | 性能结论拆分 core / physics / render / runner | 不归因到 GAS Core 的成本绝不归因 |
| `ODF-10` | PackageCache hash、Burst AOT、Baking world、allocator 等环境项进入交还证据 | 完整可复现环境 |

### ODF-09..18 覆盖项速查

| ODF | 覆盖项 | 关联主题 |
|---|---|---|
| `ODF-09` | 性能结论拆分为 core/physics/render/runner | 06 |
| `ODF-10` | PackageCache 证据、Burst AOT、Baking world、allocator 进入交还证据 | 00, 05, 09, 10 |
| `ODF-11` | PackageCache hash 路径 | 00 |
| `ODF-12` | Burst AOT / Player 口径 | 09 |
| `ODF-13` | Managed boundary 规则 | 01, 08 |
| `ODF-14` | Baking world + EntityPrefabReference + allocator aliasing | 05, 10 |
| `ODF-15` | Unity Physics 相关 | 07 |
| `ODF-16` | Physics 配置 | 07 |
| `ODF-17` | Entities Graphics 相关 | 08 |
| `ODF-18` | Render evidence | 08 |

## 覆盖矩阵（按任务类型）

| 任务类型 | 必读主题 | 最小交还证据 |
|---|---|---|
| **Runtime Core** | `01` `02` `03` `04` `09` `10` `13` `14` `15` `16` `20` | API 选型表、query/buffer/job/structural metrics、状态机选型、规范检查清单、`chunk.DidChange` 变更检测、`EnabledMask` 批量 enableable、`EntityQueryMask` 匹配、`ChunkEntityEnumerator` 迭代、`chunk.Has<T>` 可选组件检查、Chunk Component 空间分区、`[ChunkIndexInQuery] sortKey` 确定性 ECB 回放、`Buffer.Reinterpret` 重解释、`LinkedEntityGroup` 批量生命周期、独立 ECB per job、`IJobEntityChunkBeginEnd` Chunk 预评估、读写数据分离、Baker 只添加不读取、Baker 必须无状态、Baking System 手动追踪依赖和增量还原、禁止手动调用其他 System Update()、禁止直接修改 Child/PreviousParent（由 ParentSystem 管理）、Singleton API 依赖完成、Scene Section 跨引用限制、Content Management 弱引用加载（Burst 兼容）、`PostLoadCommandBuffer` + `ProcessAfterLoadGroup` 场景实例化、SystemAPI vs EntityManager 同步语义选型、Custom Section Metadata 智能流式加载、System-Associated Entity Data 系统数据组件化、`SystemAPI.Query` 不可存储复用、`DynamicBuffer<T>` 默认读写限制 |
| **Debugger** | `02` `03` `04` `06` `09` `10` `13` `15` | 分组归因、Profiler/Journaling 对照 |
| **Luban/SourceGenerator** | `00` `05` `09` `13` `16` `20` | generated lookup、Blob/BakePlan、runtime managed boundary、TransformUsageFlags、`BlobBuilder` 构建模式 |
| **AutoChessDemo** | `02` `03` `04` `07` `08` `10` `11` `13` `14` `15` `16` `20` | battle hash、core/physics/render profile、ScaleProfile、状态机选型、LocalToWorld 精确坐标、TransformUsageFlags、`ICustomBootstrap` 多世界、`EnabledMask` 批量操作、Custom Transform (WriteGroup+ManualOverride) |
| **Presentation/Cue** | `05` `08` `10` `13` `15` `20` | outbox、resource binding、log marker/real asset 等价性 |
| **Physics 目标获取** | `07` `10` `11` `13` `15` `20` | broadphase policy、query count、event window |

## 官方证据

所有 ODF 规则都可追溯到各单主题文档中的官方证据表。本主题本身不重复维护官方证据。

## 使用模式与反模式

**正确模式：**
- 发现 → 判断影响 → 选择 owner → 反哺 → 验证
- 行动报告中引用具体主题文档，不写"看过官方文档"

**反模式：**
- 读了官方文档但不在行动报告中记录
- 发现的问题只记在对话里，不落文档
- P0/P1 发现无 owner 追踪

## EX-GAS 项目解读

### 当前反哺路径检查

每次 DOTS 相关任务完成后，同步检查：
1. 是否有新的目标态 Spec 需要补充？
2. 是否有当前实现问题需要记录到诊断？
3. 是否有任务顺序需要调整？
4. 是否有新的 CASE 模式需要加入 `12` 文档？

### 行动报告的最小模板

```
官方文档覆盖检查：
  - PackageCache: Entities e90944159b94 (1.4.6)
  - 读取主题: 01, 02, 03, 04, 20
  - 适用规则: QRY-01, JOB-01, SC-01, BUF-01, SEL-03
  - 采用: IJobEntity (CASE-02) for AttributeDeltaApply
  - 拒绝: SystemAPI.Query (CASE-01) — 主线程 sync 不可接受
  - 暂不相关: Physics (ODF-15) — 当前无范围技能需求
  - 需要反哺: 更新 00-当前架构事实 — ECB 使用模式偏差
```

## 常见陷阱

1. **ODF-09..18 被视为"模板填空"**：每个 ODF 项必须真正判断相关/不相关
2. **反哺延迟**：发现放在迭代摘要后不下沉到长期事实
3. **旧聚合文档残留**：内容已迁移但旧文件未删除

## 验收指标

1. 行动报告和交还内容能定位到具体主题，不只是"看过官方文档"
2. 每个 P0/P1 发现都有 owner 和反哺状态
3. 已解决问题从当前事实中归档，不留在活跃诊断里污染任务输入
4. 行动报告中 ODF-09..18 每项都有相关/不相关的明确说明
