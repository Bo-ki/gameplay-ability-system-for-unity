# 当前窗口

> 上次更新：2026-06-08 | 本文件只作接力短窗口；长期事实以 `00-当前架构事实/` 为准，任务路由以 `02-主线任务树/` 为准。长表拆分前全文已归档到 [_归档/2026-06-07-当前窗口长表拆分前.md](_归档/2026-06-07-当前窗口长表拆分前.md)。

## 当前状态

| 项 | 当前状态 |
|---|---|
| 当前阶段 | T0 文档治理规则进入持续执行态；`00/01/02/04` owner 边界、生成-消费-归档链路、整理信号卡和执行 Agent 交还包已固化。 |
| 第一性事实源 | `Assets/GAS/Runtime`、`Assets/GAS/Generated/CodeGen/Runtime` 当前代码，以及 `00-当前架构事实/` 中的复审事实。 |
| 当前主链 | `GASFramePrepareSystemGroup -> GASCommandResolveSystemGroup -> GASCoreSimulationSystemGroup -> GASStructuralCommitSystemGroup -> GASBoundaryProjectionSystemGroup`。 |
| 本轮代码交还 | GAS Concept Trace 与 Sequence Owner API 收口已完成窄验证：runtime build / AutoChess demo build 通过，Unity batchmode AutoChess x50 日志 `Temp/AutoChessBattleValidation.ConceptTrace.log` 输出 `passed=True`、`blockingDebugErrors=0`、`journalingCaptured=True`、`gasConceptCoverageMask=0x1FF`、`runtimeTraceStageMask=0x2CD`。该结果只证明 concept trace / allocator API 已接入真实业务链，不代表 carrier 物理拆分、Profiler enabled 或 x100/x1000 scale gate 完成。 |
| 本轮性能口径修正 | x50 headless 旧 `passed=True` 口径已降级为“业务链路通过”。`AutoChessHeadlessLogicBudgetResult` 已把 strict pure logic budget 接入 validation hard gate，并新增 Runtime-owned data-oriented scorecard：units、commands/facts per measured tick、us per unit / command / fact、ActiveMutation / PendingAttribute / OwnerLocalFact owner group 与 max range、query / lookup / random / sync budget。`GasRuntimeDebugger` 已新增 `runtimeDataOrientedScorecard` facade，`GasRuntimeDerivedExportSink` 已承接派生文本导出，第三刀新增 `GasRuntimeMetricFamilySnapshot` 并挂入 `GasRuntimeDiagnosticSnapshot.MetricFamilies`；scorecard / derived export / AutoChess report 现在消费 `metricFamilySource=GasRuntimeMetricFamilySnapshot`、`metricFamilyMask`、`dominantRisk` 和 `runtimeMetricFamilySnapshot`。本轮 runtime build 与 AutoChess demo build 通过；AIBridge compile 返回 `status=timeout` / `statusConfirmed=false`，不能写成 Unity 编译通过；当前 x50 证据在新预算下不达标，后续必须从 DOTS 数据布局、owner-local range、lookup pressure、buffer carrier、structural playback 和 Burst / managed boundary 原理解释热点，而不是只看总 ms。 |
| 文档整理进展 | `00` 事实索引、`01/03B`、`01/03E`、`01/13`、`01/16`、`04` 短窗口和 T0 唯一职责整理规则已进入持续维护态；本轮已把整体架构最新 codedb 校准和 Runtime Access / RunnerSync capability 事实写入 `00` 与 `04`，并将 `01/16-06` 从单大正文降权为端到端入口，完整代码骨架正文由 `16-06A` 唯一维护。`13-EntityComponent物理布局Spec.md` 已从多阅读任务长文压缩为短索引；`10/11/21` 已完成 AutoChess runner / schema / sample 三 owner 裁决；`03D` / `16-02` / `10B-04/05` 已完成 Command / Target / Fan-In / Attribute / ActiveEffect / Fact 通用规则 owner 合并裁决，10B 只保留业务案例投影；`16-06` 与 `16-03..16-05` 已完成完整骨架 / 局部规则 owner map 裁决，`03B-03` 与 `08/14/15` 已完成 generated 配置链 owner 裁决；本轮追加 `19/20/22/23/24` 业务配置链 owner 裁决和 `10B-07` 对 `08/14/15/11` 的示例生成物裁决，并将 `00/09` 作为文档矛盾与过时口径事实账本。该轮同时保留“体量只触发审查、不自动拆分；相似主题优先合并唯一正文；过时架构描述必须按 owner 裁决”的规则，第三十一轮、第三十三轮、第三十四轮、第三十五轮、第三十六轮、第三十七轮、第四十四轮和本轮架构校准信号卡见 `02/T0-文档治理与目标态共识/文档维护规则迭代计划/04-唯一职责拆分记录.md`。 |
| 当前推荐领取 | R1/R6 继续收缩 Shell capability、AutoChess driver runtime store 的 StructuralCommit / bootstrap owner、ObservationGateway / lifecycle direct-EM adapter，并把 catalog session 的 runtime-created Blob / entity dispose 推向最终 DefinitionCatalogLifetime；R2/R3/R5 处理 singleton carrier / fan-in、active effect slot SourceAttribute snapshot、capacity / spill 与 generated lifecycle 防回流；R5 当前重点是 `RuntimeDefinitionGlue` / `RuntimeAbilityActivation` / `RuntimeEffectInstant` / `RuntimeActiveEffect` 四个 `RuntimePureGlue` artifact 与手写 `GASRuntimeDefinitionResolver`、`GEEffectInstantSystems`、`GASActiveEffectRuntime` 的职责对账，当前 `GeneratedRuntimeBoundaryHits=0` 的负例验证，manifest/report/category/file/schedule 四方对账、system budget 和 release-ready gate；R4/R6/R8 补 StructuralCommit / Debugger official evidence、Presentation / Replay / StructuredLog owner、lane-specific magnitude source attribution、capacity / spill、TopN 归因和 performance / diagnostic pass 分离。 |
| 下一文档治理入口 | 继续按 R0 检查旧任务漂移、重复 owner、相似主题合并、矛盾描述和过期架构口径；新增文档矛盾先对账 `00/架构重划分审查事实/09-文档矛盾与过时口径事实.md`。`01/13` 已完成同 owner 拆分，后续只观察 `03G` / `05` / `16` 是否恢复第二份物理布局或 capacity 正文；`01/10/11/21` 已完成 owner 裁决，后续只观察 runner evidence、schema 和 sample 是否恢复第二份正文；`03D` / `16-02` / `10B-04/05` 已完成合并裁决，后续只观察是否恢复第二份通用 Command / Target / Fan-In / Attribute / ActiveEffect / Fact 正文；`16-06` 已完成根入口 / `16-06A` 正文拆分，`03B-03`、`10B-07` 和 `19/20/22/23/24` 已完成 owner map 裁决。下一批优先审查旧任务漂移、`08/14/15` 的 artifact / gate 边界是否继续重复、`19/20/22/23/24` 是否恢复第二份流程正文，而不是按行数机械拆分。`04/当前窗口.md` 已压缩为短窗口，后续长细节必须回到 `最近验证摘要.md`、`迭代摘要.md`、`00` 或 `02`。 |

## 已反哺 Owner

| Owner | 本窗口已反哺内容 |
|---|---|
| `00-当前架构事实/` | Shell capability、query materialization、generated runtime compatibility marker、AutoChess adapter、NativeStream evidence、Boundary snapshot、Magnitude Source evidence、generated registry fail-fast、active effect slot SourceAttribute snapshot gather Run3 验证、端到端消息流职责泄露、TagRequirement query / Definition glue Run5 证据边界、文档矛盾与过时口径防回流账本、driver public raw handle 退出 Run7 证据、catalog lifetime Interface 收窄、driver owner snapshot 证据、本轮 Module depth / owner locality 当前截面、Shell / Catalog / Runner capability 事实、Runtime Access wrapper capability 分类事实、Debugger evidence owner 续审事实以及 CodeGen / SourceGenerator artifact responsibility 续审事实均已落入当前事实入口；当前 codedb 截面为 434 files / 434 outlines / 764 chunks / graph 448 nodes / 1829 edges / 34 communities，Runtime path-prefix 主社群为 146 files / 2227 indexed symbols，dependency edges 为 internal 603 / boundary 132 / incoming 101 / outgoing 31；`RuntimeAbilityActivation.gen.cs`、`RuntimeEffectInstant.gen.cs` 与 `RuntimeActiveEffect.gen.cs` 已退为 `RuntimePureGlue` marker，ability activation 实际 consumer 已转向手写 `GASRuntimeDefinitionResolver`，instant spec / reduce 实际 consumer 已转向手写 `GEEffectInstantSystems.cs`，active effect helper / job 主体已转向手写 `GASActiveEffectRuntime.cs` 与 `GEActiveEffectLifecycleSystems.cs`，`ActiveEffectLifecycleOwnerSystems.cs` 当前磁盘缺失，`GasGlueCodeGenPhases.cs` 当前为 5556 行；新增本轮事实卡见 `00/架构重划分审查事实/10-整体架构重审事实.md`。 |
| `01-目标态架构共识/` | `16-03` 已补 active effect slot snapshot key 目标骨架；`16-04` 已补纯目标态 `Capability Access Matrix`；`16-06` 已降权为端到端入口，`16-06A` 是完整目标代码骨架正文唯一 owner；`16-01A/B/D` 已补目标态整体重划分、Module depth、deletion test、runtime lifecycle owner、dependency graph、cost domain、generation purity 判定门；`01/14` 已补 `Generation Artifact Responsibility Matrix` 和 artifact manifest contract 代码骨架；`01/15` 已补 Gate Disposition Policy、Runtime Core 调用 generated pure glue 的目标代码骨架、TagRequirement query evaluator、OOP Shell 非中间层、Debugger evidence、DOTS 优秀结论门槛、owner-local dispatch、spec/delta/fact lane、目标态文档纯度和生成-消费-归档闭环；`18` 已补 Headless Pure Logic Strict Budget 与 DOTS principle vector，明确性能指标必须面向数据形态而不是单看总 ms；`03E`、`13`、`16-01`、`16-06` 已完成同 owner 渐进式拆分，`10/11/21` 已补 AutoChess runner / schema / sample owner 边界。 |
| `02-主线任务树/` | R1-R8 已全部消费 `01/16-01` 架构重划分判定准则；R0-R8 已消费 `00/07` 端到端重划分事实、`01/16-06` 端到端消息流入口与 `16-06A` 完整代码骨架正文；R1/R6 已消费 `16-04 Capability Access Matrix`；R5 已新增 artifact responsibility matrix、manifest/report category 对账、pure glue consumer 对账、`GASRuntimeDefinitionResolver` / generated resolver 对账和 release-ready disposition 交还门；本轮已把 live codedb 截面、`AutoChessGasRuntimeAccess` wrapper 收口但非完成态、`OwnerLocalGameplayFactBuffer` Attribute / ASC command fact 多写入面但非全链路 carrier 退出、generated fact writer `MigrationProofOnly`、GameplayFact lane source contract 和 Scale Gate 阻塞矩阵同步到根任务、R0-R8 索引以及 R0/R1/R2/R3/R4/R5/R6/R7/R8；R0 继续维护“整理信号卡”和旧数字防回流扫描。 |
| `04-当前进度状态/` | `最近验证摘要.md` 保留最新验证摘要、未跑项和下一验证入口；`迭代摘要.md` 已修正当前最新索引里的过时 query 口径，追加 Shell / Catalog / Runner capability 续审索引，并归档 AM5 长实现流水后重建为短接力索引；本文件只保留短窗口。 |

## 未跑项

1. 未运行 Unity Test Runner EditMode / PlayMode。
2. 除已归档的 x50 headless validation 外，未运行 x100 / x1000 scale profile 或 Profiler enabled 场景。
3. 本轮 Concept Trace / Sequence Owner 切片已运行 runtime build、AutoChess demo build 和 Unity headless AutoChess x50；未运行 AutoChess x100/x1000、Profiler enabled、Unity Test Runner、Luban process gate、SourceGenerator / Unity CodeGen 或独立 generated runtime build。
4. 最近一次 R1/R6 代码切片曾运行 runtime boundary diagnostics、runtime build 和 AutoChess demo build；本轮 CodeGen / SourceGenerator artifact responsibility 文档审查未重新运行 Core GasCodeGenCli / SourceGenerator、独立 generated runtime build、Luban process gate、Unity CodeGen、Unity Test Runner、Unity headless AutoChess、AutoChess x100/x1000 或 Profiler enabled 场景。
5. `com.exhard.exgas.runtime.tests.csproj` 仍受当前不存在的 `Assets/_Test/GAS/...` 源文件引用影响，不能写成 runtime tests 通过。
6. 本轮 Debugger Data-Oriented Scorecard / DerivedExportSink / MetricFamilySnapshot 切片已运行 runtime build 与 AutoChess demo build；AIBridge Unity compile 为 `statusConfirmed=false`，尚未运行 Unity headless AutoChess x50、x100/x1000 scale profile、Profiler enabled 场景或 Unity Test Runner。

## 注意事项

1. `01-目标态架构共识/` 只能写理想目标态、禁止方向、官方依据和验收门槛，不写当前代码事实、行号、执行流水或下一步任务。
2. `02-主线任务树/` 只承载可领取任务、退出条件和验证面；长进度摘要回到 `04`，当前事实回到 `00`。
3. generated runtime 是真实执行链的一部分；R5 不能把 `GeneratedRuntimeSystemRegistrationHits=0`、缺失 type fail-fast 或 pure resolver 正向证据写成 SourceGenerator 完成证明。
4. Contract 字段不是完成证明；完成度必须来自执行链、Profiler、Entities Journaling、Debugger counters、测试和规模门。
5. 新增 DOTS 结论必须先对照 `方案讨论/UnityDOTS官方文档参考/主题/20-GASRuntimeCore-API选型基线.md`、`主题/21-官方文档覆盖与流程闭环.md` 和 `主题/90-规则编号索引.md`。
6. T0 历史 proof 只从 `02/T0-文档治理与目标态共识/历史proof索引.md` 追溯；`已完成文档校准日志.md` 等兼容入口不再作为当前支线正文。
7. 后续文档治理不能只看体量拆分；若 `03B` / `03E` / `08` / `14` / `15` / `16-01D` / `16-06` / `16-06A` 等相近主题出现第二份规则正文，优先合并或降权旧入口。
8. `10` / `11` / `21` 这类相似 AutoChess 目标态文档要保持 owner 纯粹：runner / evidence、配置 schema、策划验收样例分别维护；重复字段才合并到唯一正文 owner。
9. `02/T0/文档维护规则迭代计划/04-唯一职责拆分记录.md` 的物理文件名只为兼容旧链接；读取时按“唯一职责整理记录”处理。根入口只作索引，已完成动作读 `04A`，合并 / 矛盾 / 归档规则读 `04B`，候选和后续切片读 `04C`。
10. T0 归档快照由 `02/T0/_归档/README.md` 统一消费；归档内容重新使用前必须先生成信号卡并重新判定 owner。
11. 本轮新增整理动作状态机：观察 / 拆分 / 合并 / 降权 / 归档 / 矛盾清理都必须写进入条件和退出条件；观察项不能保留已被唯一 owner 吸收的旧正文。
