# 迭代摘要

> Owner：`04-当前进度状态` | 状态：当前接力索引 | 最近整理：2026-06-08

本文件只保留当前窗口需要的一手交接索引、反哺状态和下一轮建议阅读。长历史流水已归档到 [_归档/2026-06-08-迭代摘要长正文归档前.md](_归档/2026-06-08-迭代摘要长正文归档前.md)。

## 已迁移轮次

| 日期范围 | 迁移文件 |
|---|---|
| 2026-05-24 之前全部 40 轮 | 历史迭代记录归档；当前工作树未发现对应归档文件 |
| 2026-05-26 AM5 cleanup / tick / global-index 早前轮次 | 历史迭代记录归档；当前工作树未发现对应归档文件 |
| 2026-05-26 AM5 最近轮次摘要和 2026-06-08 前长接力正文 | [_归档/2026-06-08-迭代摘要长正文归档前.md](_归档/2026-06-08-迭代摘要长正文归档前.md) |

## 当前最新索引

2026-06-08 GAS Concept Trace 与 Sequence Owner API 收口（当前代码验证最新）：本轮新增 `GASRuntimeConceptCoverageSnapshot`、`GASRuntimeTracePreview`、`GASRuntimeDefinitionResolver.CreateConceptCoverageSnapshot`、`TryBuildRuntimeTracePreview`，并新增 `GASRuntimeSequenceOwnerContract` / `GASRuntimeSequenceAllocator` 作为 GE stream context / command / spec / delta / fact sequence 的唯一 API owner。Runtime / AutoChess producer 不再直接 `Allocate(ref stream.Next*)`，直接访问只剩字段定义、初始化和 allocator 内部；AutoChess validation summary 已接入真实业务 ability `AbilityPlayerExecute=9103` 的 trace preview。验证已跑 runtime build 和 AutoChess demo build，均 0 error，仅保留既有 `MSB3277` warning；Unity batchmode AutoChess x50 日志 `Temp/AutoChessBattleValidation.ConceptTrace.log` 与摘要 `TestResults/AutoChess/Headless/AutoChessHeadlessValidationSummary.txt` 输出 `passed=True`、`completed=True`、`winner=Player`、`blockingDebugErrors=0`、`journalingCaptured=True`、`summaryHash=0x6CC8210F`、`factsHash=0x75720A4D`、`gasConceptCoverageMask=0x1FF`、`gasConceptMissingMask=0x1E00`、`runtimeTraceStageMask=0x2CD`、`runtimeTraceMissingStageMask=0x430`。本轮不能写成 singleton carrier 物理拆分、Shell capability 最终收权、DefinitionCatalogLifetime dispose owner、Profiler enabled、x100/x1000 scale gate 或 DOTS 性能优秀完成。

2026-06-08 R1/R6 AutoChess runtime access concrete capability 收口（当前代码验证最新）：本轮把 `AutoChessGasRuntimeAccess` 从“集中 raw ECS seam 的 wrapper”继续收窄为 AutoChess adapter 内部 concrete capability owner；`AutoChessGasRuntimeHost` 不再取得 session `World`，catalog session 不再取得 definition `EntityManager`，battle lifecycle 不再取得 lifecycle `EntityManager`，observation / official diff 不再取得 diagnostics `World`。对外只保留 runtime system registration、tick group、catalog install/uninstall、battle driver lifecycle/snapshot、diagnostics sink、command port 和 runner drain 等具名 capability；`Verify-GAS-RuntimeCoreBoundary.ps1` 已阻断 `TryResolveSessionWorld`、`TryResolveDefinitionEntityManager`、`TryResolveBattleLifecycleEntityManager`、`TryResolveDiagnosticsWorld` 等 shared raw resolver 回流，并修正 active-effect handwritten owner 相关门禁路径。验证已跑 runtime boundary diagnostics、runtime build、generated runtime build、AutoChess demo runtime/editor build 和 Unity batchmode `-runAutoChessValidation` x50；x50 日志 `Temp/AutoChessBattleValidation-RuntimeAccessCapability-Final.log` 输出 `passed=True`、`thresholdsPassed=True`、`runtimeChainPassed=True`、`repeatRunPassed=True`、`driverAdapterRawEntity=false`、`entries=5200`，错误扫描未命中 Burst / BC10 / compiler errors / exception。build 仍只有既有 `MSB3277` warning；本轮仍未运行 Unity Test Runner、AutoChess x100/x1000、Profiler enabled、Luban 或 SourceGenerator，不能写成 Thin Adapter 完成、driver lifecycle 最终 StructuralCommit owner 完成或 DOTS 性能优秀。

2026-06-08 Structured Evidence / Presentation Boundary 整体审查（上一截面记录，当前 codedb 数字以第 23 条 live 校准为准）：codedb 当时为 431 files / 431 outlines / 780 chunks / graph 445 nodes / 1798 edges / 34 communities，Runtime 主社群为 142 files / 2044 indexed symbols，dependency edges internal 551 / boundary 166 / incoming 138 / outgoing 28。本轮定点读取 Presentation outbox、Replay sink、structured log export、Replay retention、Cue managed lifecycle、Debugger observation materialization 和 AutoChess observation gateway，结论是 `BoundaryObservationFact -> PresentationOutbox / ReplaySink / StructuredLog` 是正向 Boundary evidence 链，但仍通过 singleton event bus、`EntityManager` append、managed export、report-key resolve 和 Cue managed callback 承担 Boundary / diagnostic 成本，不能回写 Core，也不能写成 Core hot path 性能证明。已更新 `00/架构重划分审查事实/04-整体审查与Owner重划分事实.md`、`01/06-Observation-Presentation-ReplaySpec.md`、R4、R6、当前窗口和最近验证摘要；`01` 只新增纯目标态 owner 验收矩阵。验证结果：`01/06` 当前事实污染扫描无命中，触达文件行尾空白扫描无命中，相对链接扫描 `BROKEN_LINKS=0`，`git diff --check` 只有 LF/CRLF warning。本轮未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen、runtime boundary diagnostics 或 build。

2026-06-08 T0 唯一职责整理记录拆分收口（本轮文档治理最新）：`02/T0/文档维护规则迭代计划/04-唯一职责拆分记录.md` 已从 670 行级单文件压缩为根兼容索引，正文拆到 `04A` 整理信号卡流水、`04B` 合并与矛盾清理检查表、`04C` 当前候选与后续切片；拆分前全文归档到 `02/T0/_归档/2026-06-08-唯一职责拆分记录拆分前.md`，并新增 `02/T0/_归档/README.md` 统一说明归档来源、当前 owner 和重新消费规则。本轮同步了 T0 README、R0 叶子、当前窗口、最近验证摘要和本摘要；`04A` 因 owner 单一暂不继续拆，后续若继续增长，优先按轮次范围归档旧流水。验证结果：活动 Markdown NUL 扫描无命中，触达入口相对链接扫描 `BROKEN_LINKS=0`，活动 Markdown 行尾空白扫描无命中，`01` 目标态纯度扫描未命中 Runtime Access / codedb 当前数字污染，`git diff --check` 仅有 LF/CRLF warning。本轮未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen、runtime boundary diagnostics 或 build。

2026-06-08 `16-06` 渐进式披露拆分（本轮文档治理最新）：本轮先按相似主题 owner 裁决复核 `16-06`、`16-01D`、`16-02..16-05`、`01/README.md`、`16/README.md` 和 `00/09`，确认问题不是新增第二个 owner，而是单大文档同时承载入口、完整 C# 骨架、代码解读、验收和局部 owner map。已将拆分前全文归档到 `01/_归档/2026-06-08-16-06-端到端消息流代码骨架Spec拆分前.md`；根 `16-06` 降权为端到端入口和 owner 裁决；完整代码骨架正文移动到 `16-06-端到端消息流代码骨架/16-06A-完整端到端消息流代码骨架Spec.md`；`01` / `16` 索引、`00/09`、`02` 根任务 / R0-R8 消费门、T0 第四十四轮信号卡和 `04/当前窗口.md` 已同步。本轮只做目标态文档组织整理和消费门维护，未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen、runtime boundary diagnostics 或 build。

2026-06-08 live codedb 再校准（当前事实最新）：本轮重新跑 `codedb_status`、Runtime `module_map`、关键 owner outline、Shell imported_by、schedule contract imported_by、ASC command resolve depends_on、OwnerLocal fact / SpecStream / generated runtime 搜索，并对照 DOTS `10-Collections-Allocator-NativeStream.md`、`13-DOTS编写规范与性能陷阱.md`、`20-GASRuntimeCore-API选型基线.md` 与 `06-Diagnostics-Profiler-Journaling.md`。当前事实卡为 434 files / 434 outlines / 764 chunks / graph 448 nodes / 1829 edges / 34 communities；Runtime 主社群 146 files / 2227 indexed symbols，dependency edges internal 603 / boundary 132 / incoming 101 / outgoing 31；`GEEffectCommandSpecStream.cs` outline 为 967 行，`GEEffectInstantSystems.cs` outline 为 643 行，`GEActiveEffectLifecycleSystems.cs` outline 为 372 行，`GASActiveEffectRuntime.cs` outline 为 3389 行，`ASCCommandBufferResolveSystem.cs` outline 为 952 行，`GasRuntimeDebugger.cs` outline 为 4156 行，`ActiveEffectLifecycleOwnerSystems.cs` 当前磁盘缺失，`GasGlueCodeGenPhases.cs` 当前为 5556 行级大聚合。`GASRuntimeShell.cs` 直接 imported_by 为 AutoChess runtime access wrapper、Editor watcher、Runtime binding；`GASSystemScheduleContract.cs` 直接 imported_by 为 GASManager、AutoChess bootstrap、FrameBudget / StreamOwner / EvidenceGate / Rebind contract。已同步活动 `00/02/04` 当前事实卡；旧 431-to-434 file-count、759 / 761 / 780-to-803 chunk-count、1798-to-1837 edge-count、2044-to-2218 symbol-count、9467-line、5559 / 5567-line 以及更早旧数字只进入 `00/09` 和 R0 防回流清单。`01` 只维护目标态 owner 判定准则、owner map、代码骨架和不变量，不写入当前 codedb 数字。本轮未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen、runtime boundary diagnostics 或 build。

以下较早同日摘要保留历史语境；如数字与上一条最新校准冲突，以“live codedb 再校准（本轮最新）”为当前事实。

2026-06-08 整体架构 Owner 重划分 live 复审：本轮以 codedb live 截面 431 files / 431 outlines / 780 chunks / graph 445 nodes / 1798 edges / 34 communities 为输入，复核 Runtime module-map、`GASRuntimeShell` direct imported_by、`ASCCommandGateway` / `AbilitySystemBinding`、`ASCCommandBufferResolveSystem`、`GEEffectCommandSpecStream`、generated runtime、`RuntimeDefinitionGlue.gen.cs`、`GasRuntimeDebugger.cs` 和 AutoChess runtime access wrapper。当前事实写入 `00/README.md`、`00/架构重划分审查事实.md`、`00/架构重划分审查事实/00-总览与代码证据事实.md` 与 `04-整体审查与Owner重划分事实.md`：保留面是 5 段 physical backbone、owner-local applicator、generated immutable catalog / pure glue 和 structured evidence；退出或隔离面是多 capability Shell facade、singleton stream carrier、generated lifecycle / lookup / ECB / NativeContainer owner、Debugger / Cue materialization 混入 Core 性能口径、AutoChess wrapper 共享 ECS handle。目标态只维护 `01/16-01B` 的整体责任链和 `16-06A` 的代码骨架纯度护栏，没有写入当前文件名、行号或 codedb 数字。本轮未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen、runtime boundary diagnostics 或 build。

2026-06-08 CodeGen / SourceGenerator artifact responsibility 续审（上一截面记录，具体数字已由上方 live 校准覆盖）：本轮定点读取 `GasCodeGenPipeline.cs`、`GasGlueCodeGenPhases.cs`、`GasCodeGenManifest.cs`、`LubanNormalizedRowBootstrap.cs`、`DefinitionCatalog.gen.cs`、`RuntimeDefinitionGlue.gen.cs`、`GASGeneratedDefinitionRuntimeIntegrationPlan.cs`、`GEEffectInstantSystems.cs`、`GasCodeGenValidationReport.md` 和 `GasCodeGen.manifest.json`。当前事实写入 `00/CodeGen链路复审事实.md`、`00/SourceGenerator链路复审事实.md` 与 `00/架构重划分审查事实/04-整体审查与Owner重划分事实.md`：CodeGen 已具备统一 pipeline、manifest category、generated catalog、pure definition glue 和 boundary gate，但 `GasGlueCodeGenPhases.cs` 仍是大聚合 Module，`RuntimeLifecycleMigration` 仍是迁移豁免。最新事实已修正为 report manifest 表展示 `ArtifactCategory`、`RuntimeAbilityActivation.gen.cs` 与 `RuntimeEffectInstant.gen.cs` 退为 marker、ability activation 由手写 runtime resolver 接管、instant spec / reduce 由手写 `GEEffectInstantSystems.cs` 接管、active effect evaluator 仍有 generated lifecycle consumer。目标态只维护 `01/14-DefinitionCodeGen目标链路Spec.md` 的 `Generation Artifact Responsibility Matrix` / manifest contract 和 `01/15-SourceGenerator职责边界Spec.md` 的 Gate Disposition Policy / Runtime Core 调用 pure glue 代码骨架；R5 任务已消费为 artifact responsibility matrix、manifest/report category 对账和 pure glue consumer 对账交还门。本轮未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen、runtime boundary diagnostics 或 build。

2026-06-08 live codedb 截面追平：本轮重新跑 `codedb_status`、Runtime `module_map` 和关键文件 outline，当前事实卡更新为 431 files / 431 outlines / 780 chunks / graph 445 nodes / 1798 edges / 34 communities，Runtime 主社群 142 files / 2044 indexed symbols，dependency edges internal 551 / boundary 166 / incoming 138 / outgoing 28；`GEEffectCommandSpecStream.cs` outline 为 967 行，`GasRuntimeDebugger.cs` outline 为 4156 行。已把活动 `00/02/04` 的上一轮 chunks、indexed symbols、internal-edge 和 outline 行数旧口径追平，R0 防回流扫描也已追加旧口径描述。`01` 保持纯目标态，没有写入当前 codedb 数字。本轮未运行 Unity、AutoChess、Luban、SourceGenerator、Profiler / Journaling 或 runtime boundary diagnostics。

2026-06-08 Debugger evidence owner 续审：本轮以 codedb 当前截面 431 files / 431 outlines / 780 chunks / graph 445 nodes / 1798 edges / 34 communities 为输入，定点读取 `GasRuntimeDebugger.cs`、`GasRuntimeOfficialToolDiff.cs`、`AutoChessBattleValidationReport.cs` 和 `AutoChessBattleFlow.cs`，并对照 DOTS `06-Diagnostics-Profiler-Journaling.md`、`20-GASRuntimeCore-API选型基线.md`。当前事实写入 `00/架构重划分审查事实/04-整体审查与Owner重划分事实.md`：Debugger 已能记录 tick timing、system timing aggregate、runtime core counters、observation materialization、magnitude source、official diff 和 AutoChess validation evidence，但它仍是大聚合 evidence owner；TopN、official capture、derived export、performance / diagnostic pass、lane-specific hotspot attribution 和 overhead owner 必须进一步矩阵化。目标态只维护 `01/07-RuntimeCoreDebuggerSpec.md` 的纯 evidence contract；R4 任务已消费为交还门。本轮未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen 或 runtime boundary diagnostics。

2026-06-08 Shell / Catalog / Runner capability 续审：本轮以 codedb live 截面定点读取 `GASRuntimeShell.cs`、`GASManager.cs`、`AutoChessGasRuntimeAccess.cs`、`AutoChessGasRuntimeHost.cs`、`AutoChessGasCatalogSession.cs` 和 `AutoChessGasRuntimeTicker.cs`。当前事实已写入 `00/架构重划分审查事实/04-整体审查与Owner重划分事实.md`：`GASRuntimeShell` 虽以 Thin OOP shell 为目标，但实现仍分发 World、`EntityManager`、command port、live read model、presentation binding、runtime singleton 和 job drain；`AutoChessGasRuntimeAccess` 只是 direct import 收口点，仍聚合 session、definition / lifecycle、diagnostics、command 和 runner sync；CatalogSession 与 Ticker 是正向拆分切片，但仍依赖 wrapper / Shell resolver。目标态只更新 `01/16-04-ShellCapabilityContractSpec.md` 的纯 `Capability Access Matrix`，没有写入当前文件名、行号或 codedb 数字；R1/R6 叶子已消费该矩阵，后续交还必须输出 capability access matrix before / after。本轮未运行 Unity、AutoChess、Luban、SourceGenerator、Profiler / Journaling、Unity CodeGen 或 runtime boundary diagnostics。

2026-06-08 `02-主线任务树` 全量任务口径刷新：本轮以 codedb live 截面 431 files / 431 outlines / 780 chunks / graph 445 nodes / 1798 edges / 34 communities、Runtime 主社群 142 files / 2044 indexed symbols、dependency edges internal 551 / boundary 166 / incoming 138 / outgoing 28 为任务输入，更新 `02/README.md`、`GAS架构重划分主线任务.md`、`R0-R8/README.md` 和 R0/R1/R2/R3/R4/R5/R6/R7/R8。R1/R6 已明确 `AutoChessGasRuntimeAccess` 是 Shell direct import 收口点而非 Thin Adapter 完成态；R3/R4/R5/R7/R8 已明确 `OwnerLocalGameplayFactBuffer` 是 Attribute / ASC command fact 多写入面正向切片而非 command/spec/delta/fact 全链路 carrier 退出；R5 继续把 generated fact writer 归入 `MigrationProofOnly` 审查；R8 规模门新增 GameplayFact lane scale 表。`01` 纯度扫描未发现当前 codedb 数字或当前代码文件名污染。本轮只做静态 codedb / rg 架构审查和文档维护，未运行 Unity、AutoChess、Luban、SourceGenerator、Unity CodeGen、Profiler / Journaling 或 runtime boundary diagnostics。

2026-06-08 frame stream carrier 过时口径清理：本轮按“单一职责整理不等于只拆分”的要求，没有新增目标态正文，也没有拆新文件，而是修正 `00/架构重划分审查事实/00-总览与代码证据事实.md`、`07-端到端重划分事实.md` 与 `09-文档矛盾与过时口径事实.md` 的旧事实口径。当前 `GASRuntimeFrameStreamOwnerPlanner.CreateCurrent()` 显示 8 个 stream id 中，EffectCommand / EffectCommandSetByCaller / TypedSimulationFact 的 current carrier 仍为 SingletonDynamicBuffer，InstantEffectSpec / ActiveEffectMutation / ActiveEffectNextFrameMutation / OwnerLocalInstantNextFrame / AttributeDelta 的 current carrier 为 OwnerLocalDynamicBuffer，8 个 stream 的 scaleStatus 均仍为 MigrationCarrier；因此不能继续写成“6 类全是 singleton carrier”，也不能把 owner-local carrier 写成 scale-ready 完成态。已同步 T0 第三十六轮信号卡、`04/当前窗口.md` 和 `04/最近验证摘要.md`。本轮只做静态文档审查和过时口径清理，未运行 Unity、AutoChess、Luban、SourceGenerator、Unity CodeGen 或 runtime boundary diagnostics。

2026-06-08 T0 唯一职责整理规则收口复核：本轮只做文档治理规则的轻量补强，明确 `02/T0/文档维护规则迭代计划/04-唯一职责拆分记录.md` 的物理文件名保留“拆分记录”只是兼容旧链接，正文职责是唯一职责整理；后续必须同时判定拆分、合并、降权、归档、矛盾清理和过时架构描述，不得只按行数机械拆分。复核扫描显示 `01` 强信号命中均为禁止写入护栏，旧口径扫描只命中当前事实或“不能当完成证明”的允许语境，行尾空白无命中，`git diff --check` 只有 LF/CRLF warning，无 whitespace error。本轮未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 整体架构事实与目标态 owner-local fact 续审：该条为上一截面记录，codedb 数字已由上方 live 校准覆盖。`OwnerLocalGameplayFactBuffer` 已被代码证据确认进入 ASC archetype、pending AttributeDelta chunk-local apply、ASC command resolve、hand-written instant reduce、execution output modifier apply、FramePrepare 清理和 deterministic flush；它已经不是单一 pending AttributeDelta 切片，但仍属于 Attribute / ASC command fact 范围内的多写入面正向切片，flush 后仍导出现有 stream fact export，不能写成 command/spec/delta/fact 全链路已退出 singleton carrier。已更新 `00` 当前事实数字和 `04` 接力口径；`01` 继续保持纯目标态，只保留 CoreReactionFact / BoundaryObservationFact、owner-local dispatch 和 release-ready 验收不变量。本轮只做静态 codedb / rg 架构审查和文档维护，未运行 Unity batchmode、Unity Test Runner、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen 或 runtime boundary diagnostics。

2026-06-08 `16-06` 局部规则与 `03B-03` generated 配置链 owner 裁决：本轮继续执行“相似主题优先合并唯一正文”的治理规则，没有拆新文件。`16-06` 保持完整端到端消息流代码骨架 owner，但已补局部 Owner Map：`16-03` 拥有 fan-in / Debugger / SourceGenerator / magnitude snapshot lane 局部规则，`16-04` 拥有 Shell / Adapter capability 和 opaque handle 规则，`16-05` 拥有 Snapshot / Identity / API Health / 验收规则；若完整骨架与局部规则冲突，修正 `16-06`。generated 配置链也已裁决：`08` 管 Excel / Luban / SourceGenerator / Baker / Bootstrap / Runtime catalog 端到端流程，`14` 管 Definition CodeGen artifact 责任，`15` 管 SourceGenerator 权限 / gate，`03B-03` 只管 Runtime Core 对 generated pure glue 的消费接口。已同步 T0 第三十五轮信号卡、候选表、`01` 总入口、`03B` 子目录索引、`当前窗口.md` 和 `最近验证摘要.md`。本轮只做目标态文档治理和 owner map 校准，未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 `03D` / `16-02` / `10B-04/05` 通用规则 owner 合并裁决：本轮没有拆新文件，而是按“相似主题优先合并唯一正文”的规则裁决维护权。`03D-CommandResolve与TargetResolveSpec.md` 是泛化 Command Resolve / Target Resolve 唯一正文 owner，request-owned command normalization 降为低频物化路径；`16-02-BoundaryCommand与CoreCommandResolveSpec.md` 只维护 Shell intent -> owner-local Boundary command envelope 与 Core 消费骨架；`10B-04` / `10B-05` 只保留 AutoChess 业务字段和案例投影，通用 Command / Target / Fan-In / Attribute / ActiveEffect / Fact 规则分别回到 `03D`、`03E`、`05`、`06`。本轮还修正 `03G` 与 `13-01` 中把 Boundary request 读成默认路径的残留语气。已同步 `10B/README.md`、T0 第三十四轮信号卡、候选表、`当前窗口.md` 和 `最近验证摘要.md`。本轮只做目标态文档治理和矛盾口径清理，未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 `01/10/11/21` AutoChess 目标态相似主题 owner 裁决：本轮没有把三份 AutoChess 目标态 Spec 合并成一个更大的长文档，而是按唯一正文 owner 裁决重复面。`10-AutoChess无头验收Spec.md` 是 runner / evidence / scale gate owner，`11-AutoChessDemo-Luban配置方案Spec.md` 是 Luban schema / generated artifacts / ScaleProfile / ValidationExpectation owner，`21-AutoChessDemo策划配置验收样例Spec.md` 是策划验收样例 / row projection / trace preview / impact analysis owner。已把 `21` 的“当前目标态 Spec 审查结论 / 缺口”语气改为纯目标态相邻 Spec Owner 边界，并在 `10` / `11` 补反向边界说明。T0 第三十三轮信号卡和候选表已同步；本轮只做目标态文档组织整理，未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 `01/13` Entity / Component 物理布局同 owner 拆分：本轮按“单一职责不只是拆分，也包括合理合并、降权、归档和矛盾清理”的规则，先裁决 `13` 是否与 `03G`、`05`、`16` 重复维护正文。结论是 `13` 仍是目标态物理布局与 capacity 策略唯一 owner，`03G` 只维护 component matrix / job topology，`05` 只维护 ActiveEffectStore 语义，`16` 只维护边界重划分和端到端代码骨架。因此本轮不合并成更大文档，而是把原 `13-EntityComponent物理布局Spec.md` 压缩为短索引，正文拆入 `13-EntityComponent物理布局/` 下 3 个子页：Entity 清单与运行时布局、Archetype 与 Component 分类、Buffer 容量与 Phase 映射。拆分前全文已归档到 `01/_归档/2026-06-08-13-EntityComponent物理布局Spec拆分前.md`；已同步 `01` 索引、归档索引、T0 第三十一轮信号卡和 `04` 当前窗口 / 最近验证摘要，并把 `03G` / `05` 中旧 `13` 根文件行号引用改为子页语义链接。本轮只做目标态文档组织整理，未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 R1/R6 AutoChess catalog lifetime / driver owner Interface 收窄：本轮把 `AutoChessGasRuntimeHost` 从 catalog lifetime `EntityManager` 解析中移出，host 只保留 runtime bootstrap、system registration、tick group lookup 和 job drain；catalog install / uninstall 通过 `AutoChessGasCatalogSession.TryInstall()` / `Uninstall()` 进入 catalog session owner，`AutoChessBattleDefinitionCatalogBuilder` 降为 internal implementation。driver owner 证据通过 `AutoChessBattleDriverOwnerSnapshot` 从 runtime store 输出到 validation result，summary 字段覆盖 `driverOwnerHandleMatched`、`driverStructuralCreates` 和 `driverAdapterRawEntity=false`。验证已跑 `Tools\Diagnostics\Verify-GAS-RuntimeCoreBoundary.ps1`、runtime build 和 AutoChess demo build，均通过；build 仍有既有 `MSB3277` warning。本轮未运行 Unity headless、Unity Test Runner、AutoChess x100/x1000、Profiler / Journaling、Luban 或 SourceGenerator；不能写成 driver lifecycle 已进入 StructuralCommit owner、catalog Blob dispose 已进入最终 DefinitionCatalogLifetime、Thin Adapter 完成或性能优秀。

2026-06-08 整体架构四次校准与目标态代码骨架纯度复核：该条为上一截面记录，旧 codedb 数字、Runtime module-map 数字和 `GEEffectCommandSpecStream` 行数已被本文件顶部的 owner-local fact 续审覆盖；保留该条用于追溯当轮纯度复核。`01` 保持纯目标态：`16-06` 是端到端代码骨架唯一正文 owner，fan-in 后续延伸到 target owner-local dispatch、spec build、attribute delta apply 和 typed fact lane；`01/90` 第 172-174 条继续作为 owner-local dispatch 与 spec/delta/fact lane 目标态不变量。此次仍只做静态 codedb / rg 架构审查和文档维护，未运行 Unity batchmode、Unity Test Runner、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen 或 runtime boundary diagnostics，不能写成 Runtime Core 达标、DOTS 性能优秀或 AutoChess 测试链完善。

2026-06-08 T0 唯一职责整理规则补强：本轮按用户补充要求，把“单一职责和渐进式披露不只是拆分，也包括合理合并、降权、归档、矛盾清理和过时架构描述治理”落实到维护规则。体量扫描显示 `16-06`、`10B-05`、`13`、`03D`、`10B-04`、`11`、`10`、`03B-03`、`03C`、`08` 是当轮最大活动 Spec；heading 审查后不拆 `16-06`，因为它仍是唯一完整端到端代码骨架 owner。`13`、`03D`、`10/11/21`、`10B-04/05` 当轮被登记为 owner 分工 / 重复正文裁决候选；后续第三十一轮、第三十三轮和第三十四轮已分别完成 `13` 拆分、`10/11/21` owner 裁决、`03D/16-02/10B-04/05` 合并裁决。已更新 `规范手册.md`、T0 规则入口、T0 交还包、T0-R6、R0 叶子任务、唯一职责整理记录和 `04` 接力文档。旧口径扫描未发现 `GeneratedRuntimeLifecycleMigrationArtifacts=4`、未区分 public/internal 的 `driver raw entity`、`active effect lifecycle owner 生成物`、`RuntimeActiveEffect.gen.cs.*3177` 或 `TryGetEntityForAdapter` 活动回流。本轮只做文档治理规则和候选计划更新，未拆目标态正文，未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 TagRequirement query / Definition glue 事实合并：复核 generated catalog、`GASGeneratedRequirementEvaluator`、`RuntimeEffectInstant.gen.cs` / `RuntimeActiveEffect.gen.cs` consumer、`GasCodeGenValidationReport.md` 和 Run5 x50 日志后，已将散落在 `Runtime主链事实`、`CodeGen链路复审事实`、`SourceGenerator链路复审事实`、`AutoChessDemo事实` 的 TagRequirement 长正文收束到 `00/架构重划分审查事实/08-TagRequirementQueryDefinitionGlue事实.md`。`01/15` 与 `01/90` 只补目标态边界：SourceGenerator 可生成 immutable tag query mask 和 pure evaluator，但 tag 输入必须由 hand-written Runtime Core lane 提供 owner-local / frame-local snapshot，generated lifecycle live lookup 只能是 `MigrationProofOnly`。`02` 的 R3/R5/R7 已补领取门和交还字段；`04` 只保留本摘要。Run5 只能证明 x50 链路未被 TagRequirement catalog / evaluator 贯通破坏，不能证明 Profiler enabled、x100/x1000、非零 magnitude source、所有 tag query 组合或 generated lifecycle owner 退出。

2026-06-08 整体架构续轮审查与目标态纯度校准：codedb 默认索引 ready（428 files / 428 outlines / 766 chunks / scan ready），复核 Runtime module_map、`GASRuntimeShell` direct consumers、`ASCCommandBufferResolveSystem` 宽 job、`GEEffectCommandSpecStream`、`RuntimeActiveEffect.gen.cs`、`GasRuntimeDebugger` 与 Runtime API health 后，已将当前事实数字校准并补入 `00/架构重划分审查事实/00-总览与代码证据事实.md`、`04-整体审查与Owner重划分事实.md` 和 `07-端到端重划分事实.md`。`01` 本轮只补目标态 Module depth、deletion test、dependency graph、cost domain、generation purity 判定门和 `01/90` 全局不变量，没有写入当前代码行号、当前命中数或任务流水。本轮仍只做静态架构审查和文档维护，未运行 Unity batchmode、Unity Test Runner、AutoChess、Profiler / Journaling、Luban 或 SourceGenerator；不能写成 Runtime Core 达标、DOTS 性能优秀或 AutoChess 测试链完善。

2026-06-08 `02` R0-R8 端到端事实与目标骨架消费更新：codedb 默认索引 ready（428 files / scan ready），复核 `GASRuntimeShell`、`ASCCommandBufferResolveSystem`、`GASRuntimeStreamOwnerContract`、`CompleteAllTrackedJobs` 与 `MigrationProofOnly` 当前截面后，已把 `00/架构重划分审查事实/07-端到端重划分事实.md` 和 `01/16-纯ECS内核与边界重划分/16-06-端到端消息流代码骨架Spec.md` 接入 `02/README.md`、`02/GAS架构重划分主线任务.md`、`02/R0-R8/README.md` 与 R0-R8 叶子任务。R1-R8 的交还包现在必须同时对照 `16-01` 判定准则、`16-06` 端到端消息流代码骨架、目标态职责重划分验收表、目标态代码骨架判定准则和 Derived export；缺该对照只能算事实审查，不能算架构重划分完成。本轮只做 `02/04/T0` 文档消费和静态复核，未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 R5 SourceGenerator phase 分类拆分：该条为历史记录，artifact 分类和 gate 数字已被 `00/CodeGen链路复审事实.md`、`00/SourceGenerator链路复审事实.md`、本文件顶部 live 校准和 `04/最近验证摘要` 第 5 / 6 条覆盖。当时把 `RuntimeDefinitionGluePhase` 收窄为 pure glue 输出，并开始把 active-effect lifecycle owner 从 SourceGenerator 输出、manifest 和 generated boundary report 中移除；随后当前口径已进一步校准为 ability / instant / active effect 生成物均为 marker 或 pure glue 分类，active effect helper / lifecycle 主体进入 hand-written Runtime Core，`ActiveEffectLifecycleOwnerSystems.cs` 当前磁盘缺失。验证当时已跑 Core GasCodeGenCli / SourceGenerator 和 `Tools\Diagnostics\Verify-GAS-RuntimeCoreBoundary.ps1`；后续仍需重跑 runtime / generated runtime / AutoChess demo build、Unity Test Runner、AutoChess x100/x1000、Profiler / Journaling、Luban process gate 或 Unity CodeGen。历史 proof 不能写成 active effect release-ready 达标。

2026-06-08 `16-01D` / `16-06` 完整代码骨架 owner 合并裁决：复核 `16-06`、`16-01D`、`16-01C`、`16/README.md` 和 `01/README.md` 后，确认 `16-06` 已是 Shell intent -> Boundary command -> Core lane -> `NativeStream` fan-in -> Definition pure glue -> Diagnostics evidence 的完整端到端代码正文 owner；`16-01D` 原先保留的第二份完整 C# 骨架会与 `16-06` 在 command buffer、fan-in job、evidence 字段和 Definition lifetime 表达上分叉。已将 `16-01D` 降权为最小完整判定门，只保留判定项、阅读顺序、冲突裁决和禁止恢复第二正文的护栏；`16-01C`、`16-01` 根索引、`16-01` 子目录 README、`16` 子目录 README、`01` README、T0 整理记录和本接力文档已同步。本轮是文档合并 / 降权，不是新增拆分；未运行 Unity、AutoChess、Luban 或 SourceGenerator。

2026-06-08 端到端整体架构重划分静态审查：codedb ready（428 files / scan ready），复核 Runtime module_map、`GASRuntimeShell` direct consumers、Runtime API health、宽 command resolve、stream migration carrier、singleton stream owner、generated runtime lifecycle 和 generated pure glue 截面后，已把当前代码事实写入 `00/架构重划分审查事实/07-端到端重划分事实.md`，只记录保留面、退出面和不能推出的结论。目标态只新增 `01/16-纯ECS内核与边界重划分/16-06-端到端消息流代码骨架Spec.md`，用于串起 Shell intent -> Boundary command -> Core lane -> `NativeStream` fan-in -> Definition pure glue -> Diagnostics evidence；`01` 索引和 `01/90` 已同步，但不记录当前命中数或进度。此次只做静态架构审查和文档维护，未运行 Unity batchmode、Unity Test Runner、AutoChess x100/x1000、Profiler / Journaling、Luban 或 SourceGenerator。

2026-06-08 `01/16-01` 目标分层 / Owner Map / 消息协议 / 代码骨架同 owner 拆分：体量扫描显示 `01-目标态架构共识/16-纯ECS内核与边界重划分/16-01-目标分层官方依据与不变量Spec.md` 是当前最大目标态活动文档之一，正文同时承载目标分层、官方依据、Owner Map、职责验收表、消息流协议和完整代码骨架。已将拆分前全文归档到 `01/_归档/2026-06-08-16-01-目标分层官方依据与不变量Spec拆分前.md`；根文件压缩为 28 行短索引；正文拆入 `16-01-目标分层官方依据与不变量/` 下 4 个子页：`16-01A` 目标分层与官方依据、`16-01B` 判定准则与 Owner Map、`16-01C` 目标态消息流协议、`16-01D` 目标态最小完整代码骨架。`16/README.md`、T0 唯一职责整理记录、`04/当前窗口.md` 和 `04/最近验证摘要.md` 已同步。本轮只做 `01` owner 内目标态 Spec 组织整理，未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-08 ActiveEffect slot pre-tick SourceAttribute snapshot gather Run3 验证：在上一轮静态审查基础上，继续修正 generated active effect runtime 的 DOTS safety 问题。`GASRuntimeEntityArchetypes.GrantedAbility(em)` 已前移到任何 gather job schedule 之前，避免 `CreateArchetype` 在 snapshot gather 依赖写回前触发安全检查；`GASActiveEffectRemoveSystem` 的 explicit remove 分支传入 frame-local 空 snapshot map，满足 `GEActiveEffectPreTickJob` NativeContainer 字段调度有效性。验证已跑 `Verify-GAS-RuntimeCoreBoundary.ps1`、runtime build、AutoChess demo build 和 Unity batchmode AutoChess x50；成功日志归档到 `00/_归档/2026-06-08-AutoChessBattleValidation-PreTickSourceAttributeSnapshot-Run3.log`，`passed=True`、`thresholdsPassed=True`、`runtimeChainPassed=True`、`repeatRunPassed=True`、`blockingDebugErrors=0`。该证据说明本切片清除了 Run1/Run2 暴露的 Unity job safety 异常并跑通 x50 业务链路；它仍不是 Profiler enabled、x100/x1000、非零 magnitude source 覆盖或 generated lifecycle owner 收权完成证明。

2026-06-08 ActiveEffect slot magnitude snapshot 静态审查与文档消费：codedb ready（428 files / scan ready），复核 generated active effect pre-tick snapshot gather、owner-aware slot key、catalog-driven capacity、模板命中和防回流门后，确认 active effect slot SourceAttribute live lookup 已有正向迁移证据。已把目标态约束补入 `01/16-03` 与 `01/90`：slot tick snapshot key 必须包含 target owner、slot sequence、modifier index 和 timing；SourceGenerator 只能生成 key / static lookup / pure glue / validation graph，不能拥有 gather / apply lifecycle。`02` 的 R3/R4/R5/R8 已消费为领取门、交还包和规模门；`04/当前窗口.md` 与 `04/最近验证摘要.md` 已同步接力。验证已跑窄口径旧描述扫描、行尾空白扫描、`git diff --check` 和 `Tools\Diagnostics\Verify-GAS-RuntimeCoreBoundary.ps1 -ProjectPath .`，均未发现阻断；本轮未运行 Unity / AutoChess / Luban / SourceGenerator。该切片仍不能写成 generated lifecycle owner 收权、capacity / spill、非零业务覆盖或 Profiler 性能闭环完成。

2026-06-08 `01/03E` Effect Fan-In / State / Attribute / Fact 同 owner 拆分：按“单一职责不只拆分，也包括合并、降权和矛盾清理”的治理规则，复核 03E 原文 heading、03B 拆分样式、相邻 Spec 和 10B 反向入口后，将 `01-目标态架构共识/03-RuntimeCore管线/03E-EffectFanIn-State-Attribute-FactSpec.md` 从 677 行目标态长正文压缩为短索引。拆分前全文归档到 `01/_归档/2026-06-08-03E-EffectFanIn-State-Attribute-FactSpec拆分前.md`；正文拆入 `01/03-RuntimeCore管线/03E-EffectFanIn-State-Attribute-Fact/` 下 4 个子页，分别承载 Effect Fan-In、State Evaluate / ActiveEffect Store、Attribute Reduce / Apply、Gameplay Fact。相邻关系已写入根索引：ActiveEffectStore 总体规则仍归 `05`，Entity / Component 物理布局仍归 `13`，DOTS API 策略和 lane catalog 仍归 `03H` / `03I`，03E 子页只维护本 lane 的目标态代码骨架和执行契约。本轮只做 `01` owner 内目标态 Spec 组织整理，未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-08 Magnitude Source / 三 pass 文档消费续审：复核当前代码路径、既有验证摘要和已归档 `PassSplitMagnitudeSource-Run1` x50 日志后，已把 evidence 链路事实写入 `00/架构重划分审查事实/05-BoundarySnapshot与MagnitudeSourceOwner事实.md`，修正 `00/04-整体审查与Owner重划分事实.md` 的 ObservationMaterialization 旧耗时数字，并把 `ISSUE-003` 的口径收紧为“validation evidence 代码路径和 x50 证据边界已消费”，不是 Profiler 或规模完成证明。`01/07` 只补目标态 validation evidence 字段族和 pass 采样规则；`01/90` 只补 pass 分离、Magnitude Source evidence 和官方工具对账不变量。`04/当前窗口.md` 已修正未跑项：除已归档 x50 外，未运行 AutoChess x100/x1000、Profiler enabled、Luban 或 SourceGenerator。

2026-06-08 R4/R3/R5 Magnitude Source evidence 切片：已把 managed resolver 与 execution calculation 的 magnitude source 风险从“只能靠人工读代码”推进到机器可读 evidence。`GEEffectCommandStreamComponent` 增加 frame-local counter；`EffectMagnitudeResolver` 记录 current value lookup、capture hit/miss、capture miss live lookup、fallback value/fact；`GEExecutionCalculationSystem` 用 `ExecutionMagnitudeSourceChunkCounters` 在并行 chunk 内聚合，再由 deterministic merge job 写回 stream；`GasRuntimeDebugger` 增加 `MagnitudeSource` event、`GasRuntimeMagnitudeSourceCounters` snapshot 和 `runtimeMagnitudeSource` export；AutoChess validation evidence 消费 `RuntimeDiagnostics.MagnitudeSourceCounters` 并输出 `magnitudeSourceCaptureMissLiveLookups`、`magnitudeSourceFallbackFacts` 等字段。验证已跑 `Verify-GAS-RuntimeCoreBoundary.ps1`、runtime build、AutoChess demo build 和 Unity batchmode AutoChess x50，均通过；build 仍有既有 `MSB3277` warning。AutoChess 原始日志已归档到 `00/_归档/2026-06-08-AutoChessBattleValidation-PassSplitMagnitudeSource-Run1.log`，结果为 `passed=True`、`thresholdsPassed=True`、`runtimeChainPassed=True`、`repeatRunPassed=True`、`blockingDebugErrors=0`；performance evidence / hotspot 的 `performancePassObservationPollutionRisks=0`，diagnostic Debugger summary 仍有 `observationMaterializedQueries=12` 和 diagnostic 口径 `performancePassObservationPollutionRisks=12`；`magnitudeSource*` 字段当前全为 0。本轮未运行 Unity Test Runner、Luban 或 SourceGenerator；该切片只证明 evidence 闭环、pass 隔离和防回流门。active effect slot tick snapshot gather 的后续正向静态证据以 `00/06` 和上方最新索引为准，但仍不能写成真实 magnitude source 业务覆盖、generated lifecycle owner 收权或 Profiler 性能闭环完成。

2026-06-08 `01/03B` 业务调用链与配置消费 Spec 同 owner 拆分：按“先判定 owner，再决定拆分 / 合并 / 降权 / 归档”的治理规则，复核 `03B` heading 结构和 T0 旧入口后，将 `01-目标态架构共识/03-RuntimeCore管线/03B-业务调用链与配置消费Spec.md` 从 619 行目标态长正文压缩为短索引。拆分前全文归档到 `01/_归档/2026-06-08-03B-业务调用链与配置消费Spec拆分前.md`；正文拆入 `01/03-RuntimeCore管线/03B-业务调用链与配置消费/` 下的 `03B-01` 业务调用链、`03B-02` Luban 配置生成链 Runtime 消费、`03B-03` Generated Runtime Glue 消费接口。T0 旧历史 proof / 拆分盘点兼容入口未恢复第二份正文；`03B-03` 虽仍较长，但目前职责单一，先记录为持续观察而不是继续机械拆分。本轮未把当前代码事实或验证数字写入 `01`，未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-08 `00` 架构重划分事实同 owner 拆分：响应“单一职责不只是拆分，也包括合并、降权和矛盾清理”的治理要求，已把 `00-当前架构事实/架构重划分审查事实.md` 从约 465 行多主题事实聚合压缩为索引，拆分前全文归档到 `00/_归档/2026-06-08-架构重划分审查事实拆分前.md`。事实正文拆入 `00/架构重划分审查事实/` 下 6 个子页：总览与代码证据、DOTS API 健康、R1-R6 证据截面、当前重划分与目标态差距、整体审查与 Owner 重划分、Boundary Snapshot 与 Magnitude Source Owner；后续已追加第 7 个子页 `07-端到端重划分事实`。`00/README.md`、`00/_归档/README.md`、T0 整理记录和 `04` 当前窗口已同步；本轮只做 `00` owner 内事实拆分，不修改 `01` 目标态正文，不运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-08 R5 generated registry fail-fast 当前事实消费：复核 `Assets/GAS/Runtime/System/SystemGroup/GASSystemScheduleContract.cs` 和 `Tools/Diagnostics/Verify-GAS-RuntimeCoreBoundary.ps1` 后，当前代码已从缺失 generated type 静默跳过收口为 `InvalidOperationException("Generated GAS runtime system type is missing: ...")` fail-fast，验证脚本也有防回流门。`00/Runtime主链事实.md`、`00/SourceGenerator链路复审事实.md`、`00/CodeGen链路复审事实.md`、`00/架构重划分审查事实.md` 与 R5 任务已消费该事实；`01` 不写当前实现细节，只保留目标态 registration owner / SourceGenerator pure glue 契约。该条只缓解 silent skip，不证明 SourceGenerator pure glue 完成，R5 仍必须交还缺失 artifact / type mismatch / assembly unavailable 负例、system 数量、phase budget 和 generated lifecycle 退出。本轮只做静态复核和文档维护，未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-08 当前索引矛盾口径清理、SourceAttributeSnapshotLane 验证消费与 Debugger registered owner 收口：复核 `00/README.md`、`00/架构重划分审查事实.md`、`04/最近验证摘要.md` 和已归档 SourceAttributeSnapshotLane / DebuggerRegisteredOwner 日志后，修正 `04/迭代摘要.md` 当前最新索引中的旧 query 口径。当前口径是 Runtime / generated runtime `EntityManager.CreateEntityQuery` 可执行命中 0，`ToEntityArray` 运行调用点为 Debugger observation 两处 + Cue managed boundary 一处；GlobalTimer、ActiveEffectStore 和 GasRuntimeDebugger singleton lookup 均已改为 registered/cache owner。SourceAttributeSnapshotLane Run1 只证明 active mutation apply lane 在 x50 validation 中 `activeMutationEstimatedRandomLookups=0` / `activeMutationOwnerResourceLookups=0` / `activeMutationMigrationCarriers=0`；DebuggerRegisteredOwner Run1 证明 Debugger singleton owner 退场未破坏 x50 业务链路，但日志仍显示 `profiler disabled; Entities profiler modules collect no data`，不能写成性能闭环。本轮未重新运行 Luban / SourceGenerator / Unity CodeGen。

2026-06-07 T0 官方文档校准日志降权合并：继续执行“单一职责不只是拆分”的文档治理要求，已将 `02-主线任务树/T0-文档治理与目标态共识/已完成文档校准日志.md` 从 AM-1B 到 AM-2G 明细正文压缩为兼容入口，降权前全文归档到 `T0/_归档/2026-06-07-已完成文档校准日志降权前.md`。`历史proof索引.md` 现在是 T0 历史 proof 的唯一读取入口；DOTS 官方规则正文只从 `UnityDOTS官方文档参考/主题/20-GASRuntimeCore-API选型基线.md`、`21-官方文档覆盖与流程闭环.md` 和 `90-规则编号索引.md` 消费。本轮只做文档治理，不运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-07 T0 / R0 文档生成-消费-归档链路补强：针对长期 Goal 自动续轮容易把上一轮摘要误作当前事实的问题，已更新 `规范手册.md`、`02/T0/文档维护规则迭代计划/01-owner职责与写入链路.md`、`02-交还包与验收门.md`、`03-T0维护任务切片.md`、`04-唯一职责拆分记录.md` 和 `R0-文档生成消费归档链路收口.md`。信号卡新增当前权威复核、消费状态、归档触发；交还包新增 Goal 续轮审计；T0 新增 R7 Goal 续轮写入门；R0 要求自动续轮先复核工作树和 codedb / rg 截面，再检查上一轮信号在 `00/01/02/04` 的消费缺口。本轮只做文档治理，不运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-07 `02-主线任务树` 消费整体架构审查结果：该轮原先按 GlobalTimer / ActiveEffectStore fallback query 退场口径写过全局 0 命中摘要；后续复核曾记录过 Debugger query fallback 中间截面；2026-06-08 Debugger registered owner 切片已再次收口为 Runtime `CreateEntityQuery` 0 命中、`ToEntityArray` Debugger observation 两处 + Cue managed boundary 一处。`EffectRuntimeUtility` 仅被自身与 `ActiveEffectStore.ResolveFlags` 引用且多个方法仍为空/固定返回；`02/README.md`、`GAS架构重划分主线任务.md`、`R0-R8/README.md` 和 `R7-ActiveEffectStoreStoreOnlyLifecycle.md` 已按修正口径消费。后续 R 任务领取必须把新增 Core hot query 视为回归，同时把 Debugger / Boundary exception 写入成本和退出门；R7 必须交还 `EffectRuntimeUtility` 退出表，不能在 static helper 内补逻辑后写成 store-only lifecycle 完成。本轮只做静态审查和文档维护，未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-07 `00/01` 整体架构审查与目标态 owner 纯度收口：本轮使用 codedb status / module_map / deps / text_search 复核 Runtime 大模块、`GASRuntimeShell`、`ActiveEffectStore`、`EffectRuntimeUtility`、generated runtime、Debugger、Cue boundary 和 AutoChess adapter。该轮 query 数字经过多次复核；2026-06-08 稳定口径已再次收口为 Runtime 可执行命中 0，`ToEntityArray` 运行命中为 Debugger 2 + Cue managed boundary 1。`EffectRuntimeUtility` 归类为迁移期 helper，不是目标态 Effect lane owner。`01/16-01` 新增纯目标态 Module Owner Map，`01/02` 将 Boundary 到 Core 默认承载收紧为 command record / owner-local boundary buffer；`01/14`、`01/15`、`01/19`、`01/20`、`01/22` 只保留事实 owner 引用，不承载当前缺陷分析。本轮未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-07 `02-主线任务树` 目标态判定准则消费门收口：继续使用 codedb 复核 Shell、AutoChess report key、generated registry 和 query materialization 截面后，已把 `01/16-01` 架构重划分判定准则写入 R1-R8 每个叶子任务的目标态参考和交还包。执行 Agent 领取 R 任务时必须逐项对照 Shell / Adapter public seam、Runtime Boundary resolver、Runtime Core lane owner、frame-local carrier、cross-owner access、Debugger evidence 和 SourceGenerator pure glue；不合格项必须落回对应 R 任务或 proof-only 退出门。T1 已修正 AutoChess report projector raw ASC 旧口径，T4 已修正 GlobalTimer `ToEntityArray` fallback 旧口径。本轮只做静态审查和文档维护，未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-07 T0 历史 proof 入口合并：继续响应“单一职责不只是拆分，也包括相似主题合并和过时描述清理”的维护要求，已将 `02-主线任务树/T0-文档治理与目标态共识/文档渐进式披露.md`、`官方文档查缺补漏与流程闭环.md`、`UnityDOTS官方参考反推目标态与主线审查.md` 从历史 proof 正文压缩为兼容入口；新增 `历史proof索引.md` 作为唯一历史 proof 索引，合并前正文归档到 `T0/_归档/2026-06-07-T0历史proof合并前.md`。T0 README 已拆成“当前入口”和“历史 / 兼容入口”，`文档维护规则迭代计划.md` 与 `04-唯一职责拆分记录.md` 已登记第十轮合并信号卡。本轮只做文档治理合并、过时口径扫描和静态文档验证，未运行 Unity / AutoChess / Luban / SourceGenerator。

2026-06-07 T0 文档治理合并口径修正：响应“单一职责不只是拆分”的维护要求，已将 `02-主线任务树/T0-文档治理与目标态共识/文档唯一职责拆分盘点.md` 降为兼容入口，不再维护已执行拆分、候选清单和推荐切片；唯一正文 owner 改为 `文档维护规则迭代计划/04-唯一职责拆分记录.md`。`T0-R6` 规则同步从“文档唯一职责拆分”改为“文档唯一职责整理”，新增相近主题重复文档优先合并、过期入口降权为兼容跳转的规则。本轮只做文档治理合并，不运行 Unity / AutoChess / codegen。

2026-06-07 `01/10B` 文档唯一职责拆分：按 T0 拆分门消费 `10B子Spec纯度与体量信号卡.md`，将 `01-目标态架构共识/10B-AutoChess完整业务案例/10B-03-羁绊与Runtime基础设施Spec.md` 从约 640 行多职责正文拆成短入口、`10B-03A-羁绊系统Spec.md` 和 `10B-03B-Runtime基础设施Spec.md`。旧全文归档到 `01-目标态架构共识/_归档/2026-06-07-10B-03-羁绊与Runtime基础设施Spec拆分前.md`；`10B` README、`01` README、`01/_归档` README、T0 拆分盘点、T0 拆分记录和信号卡已同步。该轮只做同 owner 内目标态 Spec 拆分，不迁入当前代码事实，不运行 Unity / AutoChess / codegen。

2026-06-07 `02-主线任务树` 全量过时任务与描述再规划：结合 codedb 语义复核、`rg` exact count、`00` 当前事实、`01` 目标态 Spec 和前序审查结论，已继续更新根 README、`GAS架构重划分主线任务.md`、`R0-R8/README.md`、R0-R8 叶子任务、T1-T6 README 与旧叶子标题，并在本轮补充 R1/R6/R5 的 proof-only compatibility / `MigrationProofOnly` 退出门口径。新增 codedb 优先的“代码截面复核门”，要求 R1-R8 领取前重扫 Runtime API、SourceGenerator report、AutoChess 多 owner、Debugger evidence 和写入 owner；SourceGenerator gate 口径已从旧 `report-only` 修正为 `blocking-unclassified-migration-proof` 下 classified `MigrationProofOnly` 退出；T3 明确为 R5 后置，不再作为 Runtime 主线前置；T4/T6 测试链改为 structured evidence / official diff / timing split / direct EM owner 分类，字符串 summary、Mermaid 图、旧 x50 只作为派生或历史 proof；旧叶子的“重新激活条件”“当前领取要求”“R4 领取要求”已降权为“新建 R 任务条件”或“折算注意项”。本轮只做静态审查和文档维护，未运行 Unity / AutoChess / codegen。

2026-06-07 `02-主线任务树` R5 SourceGenerator 与旧任务清理门收口：对照当前 `GasCodeGenValidationReport.md`、`GasCodeGenPipeline.cs`、`RuntimeDefinitionGlue.gen.cs`、`RuntimeAbilityActivation.gen.cs`、`RuntimeEffectInstant.gen.cs` 和 `RuntimeActiveEffect.gen.cs` 后，已把 R5 任务入口改为“pure resolver 保留、generated lifecycle / query / ECB / NativeContainer / random lookup owner 迁出或阻断”的口径。`GAS架构重划分主线任务.md` 已补 R5 执行范围、`BuildCatalog()` bootstrap/materialization owner、handwritten registry fail-fast 交还字段和旧任务清理门；`02/README.md` 已补 generated hit 误用门；T2/T5 README 已同步 generated artifact 分类、pure resolver、blocking gate、bootstrap owner 与性能 pass 分离。该轮只更新 `02/04` 任务与接力文档，不修改 `01` 目标态 Spec，不运行 Unity / AutoChess / codegen。

2026-06-07 R2/R3 stream carrier 口径校准：复核 `RuntimeActiveEffect.gen.cs`、`GEEffectCommandSpecStream.cs`、`GASRuntimeStreamOwnerContract.cs` 和 DOTS 规则 `QRY-04`、`BUF-02`、`NAT-03`、`PRF-06`、`PRF-19` 后，确认 active mutation 已有 frame-local command list、owner/sequence/context 排序、owner range applicator 和 mutation counters，这是正向迁移证据；但输入仍是 `GEEffectCommandStreamComponent` singleton carrier，执行仍是 serial `IJob`，helper 内仍有 BufferLookup / ComponentLookup wide random access，不能写成 scale-ready owner-local store。已同步 `02/T1-GAS_ECS_Runtime/README.md` 和 `02/GAS架构重划分主线任务.md` 的任务口径，`04/当前窗口.md` 与 `04/最近验证摘要.md` 已记录本轮验证。

2026-06-07 `01-目标态架构共识` 纯度微修：复扫 `01` 的当前实现 / 当前代码 / 本轮 / 下一步 / profile / Assets 路径 / GeneratedRuntime* / x50-x1000 等强信号后，确认大部分命中属于 owner 规则、目标目录布局、目标 gate 字段、规模验收门槛或目标态 evidence model。唯一需要微修的是 `16-纯ECS内核与边界重划分Spec.md` 的 Shell / Adapter capability 代码骨架说明，已从“当前实现是否具备”改成“不作为实现完成证明”，保持 `01` 只表达目标态 public seam。`02/T0/01目标态纯度审计.md` 和 `04/最近验证摘要.md` 已同步消费该微修。

2026-06-07 `02-主线任务树` 过时任务描述再校准：对照当前 `Assets/AutoChessDemo/Integration/GasCore` 代码后，已把 R1/R6 领取口径从旧 handle raw entity 出口和 bridge direct-EM 口径，修正为 `AutoChessBattleRuntime` Flow 入口、`AutoChessGasCoreBridge` Session facade、Host / CatalogSession / ObservationGateway / Lifecycle / Ticker / ReportFactProjector / Contracts 多 owner，以及 lifecycle registry / runtime resolver / runtime link / report projection key 退出任务。同步更新 `02/README.md`、`GAS架构重划分主线任务.md`、T1 README、T6 README 和 T0 维护规则样例；`04` 接力摘要也同步同一口径。该条只消费当前任务口径漂移，不新增目标态 Spec delta。

2026-06-07 `02-主线任务树` 领取口径二次收口：对照当前 AutoChess Integration/GasCore 文件后，已把任务树里的 AutoChess 活动入口统一为 `AutoChessBattleRuntime` Flow 入口 + `AutoChessGasCoreBridge` Session facade + Host / CatalogSession / ObservationGateway / Ticker / Lifecycle / ReportFactProjector / Contracts 多 owner。`GAS架构重划分主线任务.md`、T6 README、T6 DemoRefactor、T4 README 和 T2/T5 分支 README 已同步：R4/R6 必须按 owner 分类 direct EM / query / runner sync / report projection，`CompleteAllTrackedJobs()` 只能归入 runner sync 或 debugger measurement cost；`GeneratedRuntimeBoundaryHits` 和 `ToEntityArray` exact counts 不再写死在任务树，领取前重扫并写入交还包或 `00/04`。本轮只做静态审查、文档维护和关键词/尾随空白扫描，未运行 Unity / AutoChess / codegen。

2026-06-07 AutoChess report projection owner 校准：对照 `AutoChessBattleReportBuilder.cs`、`AutoChessGasBattleReportFactProjector.cs`、`AutoChessGasCoreContracts.cs`、`AutoChessBattleSession.cs` 和 `AutoChessBattleRuntime.cs` 后，已修正上一轮把 raw ASC `Entity` 写到 `Battle/Report` builder 的过期事实。当前事实是：`AutoChessBattleReportBuilder` 只消费 `AutoChessBattleReportFact[]` 和 unit result；unit handle 已收缩为 battle unit key，旧 handle raw entity 导出口当前无命中；raw ASC `Entity` 传播仍存在，但已收缩在 Integration/GasCore 的 lifecycle registry / report fact projector / runtime resolver。长期事实已反哺 `00/AutoChessDemo事实.md`、`00/架构重划分审查事实.md`、`00/模块索引.md`；目标态只补 `01/16` 的 Report Projection Identity Contract；R6 任务已要求替换 projector key，而不是把 Battle/Report builder 误写成 raw Entity 消费者。本轮只做静态审查和文档维护，未运行 Unity / AutoChess / codegen。

2026-06-07 `01/16` Shell / Adapter capability 目标态代码骨架：已在 `16-纯ECS内核与边界重划分Spec.md` 的 Shell Capability Contract 后补充目标态 C# interface / value object 骨架，明确 `IGASCommandCapability`、`IGASSnapshotCapability`、`IGASDiagnosticsCapability`、`IGASBootstrapCapability`、`IGASDefinitionCapability` 的 public seam。public API 只允许业务 intent、opaque target、request id、snapshot version、evidence snapshot；raw `Entity` 只允许停留在 internal resolver。`02/GAS架构重划分主线任务.md` 已消费该 Spec：R1 交还必须对齐 public value object seam，R6 交还必须按同一骨架拆 Host / CatalogSession / ObservationGateway / Lifecycle / Ticker 的 bootstrap、catalog/definition、command write、snapshot read、diagnostics/export、fixed tick runner、dependency drain。本轮未新增当前事实到 `01`。

2026-06-07 `01-目标态架构共识` 纯度再扫：对 `01` 目录追加扫描 `Assets/` / `.cs:` 行号 / `CurrentMode` / `report-only` / GeneratedRuntime* 具体数字 / x50-x1000 / 当前实现与本轮流水等强信号。结论是未发现需要迁出的当前代码事实：`Assets/` 命中主要是目标目录布局、生成链输出边界和命名空间规范；x50/x1000 是目标验收规模与阈值；GeneratedRuntime* 是目标 gate 字段；README 命中是 owner 规则。未把当前 report-only 数字、当前代码行号或执行流水写入 `01` 正文。本轮不修改 `01`，只把复核结果写入 `04`。

2026-06-07 `02-主线任务树` 消费 AutoChess 多 owner 事实：重新对照当前代码后，确认活动入口不是单一聚合口，而是 `AutoChessGasCoreBridge` facade 下的 `AutoChessGasRuntimeHost`、`AutoChessGasCatalogSession`、`AutoChessGasObservationGateway`、`AutoChessGasBattleEntityLifecycle`、`AutoChessGasRuntimeTicker` 与 contracts/report projection。已把 `GAS架构重划分主线任务.md` 的当前事实约束、任务重排依据、R1 交还包、R4/R6 执行范围、R4/R6 owner 表和验收标准改为 Host / Gateway / Lifecycle / Ticker owner 口径；T6 README 与目录迁移历史 proof 也同步降权为当前 R6/R4/R8 的参考入口。后续领取必须按 bootstrap、catalog/definition、command write、snapshot read、diagnostics/export、fixed tick runner、dependency drain 分类交还。本轮只做文档维护和静态扫描，未运行 Unity / AutoChess / codegen。

2026-06-07 AutoChessDemo 项目 README 口径同步：`Assets/AutoChessDemo/README.md` 的 `Integration/GasCore` 描述已改为 Host / CatalogSession / ObservationGateway / Lifecycle / Ticker 多 owner 口径，明确该层只是 proof-only compatibility 集中口，不是 Thin Adapter 完成证明。该更新消费 `00/AutoChessDemo事实.md` 的当前事实，不新增目标态 Spec。

2026-06-07 整体项目审查：Shell capability 与 AutoChess 多 owner 事实校准。对照 `GASRuntimeShell.cs`、`GASWatcher.cs`、`AutoChessGasRuntimeHost.cs`、`AutoChessGasCatalogSession.cs`、`AutoChessGasObservationGateway.cs`、`AutoChessGasBattleEntityLifecycle.cs`、`AutoChessGasRuntimeTicker.cs`、`AutoChessGasCoreContracts.cs` 后，已把当前事实补入 `00/架构重划分审查事实.md`、`00/AutoChessDemo事实.md`、`00/当前架构图.md`、`00/P1-高风险缺陷.md` 与 `00/ISSUE-006`：`GASRuntimeShell` public 面已收窄，但 assembly 内仍是多能力 ECS 句柄口；当前 AutoChess direct EM 分布在 host bootstrap/catalog/job drain、observation reset/snapshot/debugger timing、driver lifecycle、unit attach、battle unit registry / internal `ASCHandle` command compatibility、runtime group lookup 和 driver runtime store 内部 `_driverEntity` owner；report projection 与 unit result snapshot 已改为 `SourceReportKey` / `TargetReportKey` coverage，不再使用旧 raw-entity matcher / live ASC result read；`DestroyBattleUnit()` 当前已走 `ASCCommandPort.RequestDestroy()`，旧 direct ability/effect cleanup 口径已过期。目标态只补 `01/16` capability aggregation 禁令与 `01/90` 不变量 84，未把当前文件名或行号写入目标态正文。本轮只做静态审查和文档维护，未运行 Unity / AutoChess / codegen。

2026-06-07 `01-目标态架构共识` 10B 纯度修正：复扫 `01` 时发现 `10B-AutoChess完整业务案例设计Spec.md` 的 AutoChess 羁绊检测、Effect Fan-In、Active Effect lifecycle 和 Death Check 示例仍有 proof-only singleton / ECB / 合并 StateEvaluate 口径污染风险。已将相关正文统一到目标态骨架：羁绊检测为 `IJobChunk + NativeStream + deterministic merge`；7.3 基础设施为 lane metadata / owner-local range / command reader / spec sink / mutation sink / range sink / target grouped reader / frame append writer / structural intent helper；8.1 AutoAttack 通过 `GASEffectCommandSink.ParallelWriter` 写 EffectCommand；8.3 EffectFanIn 走 `NativeStream.Reader -> IJobFor build -> spec/mutation stream -> deterministic merge(TargetAsc, SortKey) -> owner-local range`；8.5 拆为 `GASActiveEffectMutationApplySystem` 与 `GASActiveEffectTickSystem`；8.6 Death Check 写 fact / presentation / structural intent frame writer。13.2/13.3 表格已明确 frame command / fact / mutation 禁止通过 ECB 充当事件总线，ECB 只消费 structural intent；`GASNativeStreamWriter<T>` 只表达 registry-owned append 语义，不直接暴露 `NativeStream.Writer.BeginForEachIndex`。`02/T0-文档治理与目标态共识/01目标态纯度审计.md` 和 `04` 已同步当前纯度口径；本轮未运行 Unity / AutoChess / codegen。

2026-06-07 `00/01` registration owner 与目标态 Spec 纯度校准：已把 generated runtime registration 从旧 `RuntimeSystemRegistration.gen.cs` / `TryRegisterGeneratedRuntimeSystems` 口径校准为当前 `GASSystemScheduleContract` handwritten generated type-name registry。`00-当前架构事实` 记录当前执行事实与风险：generated runtime 是主链一部分，但 registration owner 仍是手写 registry；`01-目标态架构共识` 只保留目标态契约：Runtime Core schedule registry 是唯一 registration owner，SourceGenerator 不拥有 system registration，artifact/type/assembly 缺失必须 fail-fast。扫描后未发现 `01` 把当前代码命中数、行号或执行流水写成目标态正文。本轮只做静态审查和文档维护，未运行 Unity / AutoChess / codegen。

2026-06-07 `02-主线任务树` 消费 registration owner 与 AutoChess raw identity：R5 已补充 `GeneratedRuntimeSystemRegistrationHits=0` 的解释边界，该值只能证明 SourceGenerator 不自注册，不能证明 handwritten registry 达标；R5 交还包必须补 registry hit、missing artifact、type mismatch、assembly unavailable 的 fail-fast evidence。R1/R6 已补当前 raw ECS identity 传播面：`AutoChessGasBattleUnitHandle` 已收缩为 battle unit key，旧 handle raw entity 导出口退场；2026-06-08 后 driver public handle 也已收缩为 opaque `driverId/version`，剩余风险改为 `BattleUnitRegistry` 内部 `ASCHandle` command compatibility、`AutoChessBattleDriverRuntimeStore` 内部 `_driverEntity` / direct EM owner 和 Boundary report-key 解析；report projector 与 unit result snapshot 已消费 structured log `SourceReportKey` / `TargetReportKey`。T0-R5 已固化两条样例链路：generated registration owner 漂移、AutoChess raw ECS identity 传播面。当前事实归 `00`，任务消费归 `02`，未把这些当前实现细节写入 `01`。

2026-06-07 `02-主线任务树` 旧叶子任务全量收口补充：对照当前代码进度和 R0-R8 任务树，已在 `02/README.md` 新增“旧任务处置”表和“旧叶子领取判定”，并在 `GAS架构重划分主线任务.md` 新增“当前任务重排依据”。旧 T1 AM0/AM1/AM2/AM2B/AM3/AM5、T1 AM1B/C/D/E/G、T0 渐进式披露 / 官方文档闭环、T2 Chain-1/Chain-2/AutoChessConfig、T3 DefinitionAuthoring、T4 Export-1、T5 GeneratedRuntimeGlue、T6 AM9/LubanConfig/当前 Demo 业务迁移均已压缩或复扫为历史 proof / 后置参考，并明确消费入口为 R0-R8；`AM5-ActiveEffectStore.md` 的旧 `测试链路` 已改为 R7/R4/R8 验证素材。旧 `Assets/DemoForESC` 不再作为执行范围；当前有效 Demo 入口是 `Assets/AutoChessDemo`。本轮只做文档维护和静态扫描，未运行 Unity / AutoChess / codegen。

2026-06-07 `02-主线任务树` 过时任务收口：对照当前 AutoChess adapter 代码，已把 `GAS架构重划分主线任务.md` 的 R4/R6 从旧 `AutoChessGasCoreBridge` 单体 bridge 口径更新为 facade + Host / CatalogSession / ObservationGateway / Ticker / Lifecycle / Contracts owner 分类；`CompleteAllTrackedJobs()` 继续归入 `AutoChessGasRuntimeTicker.UpdateTimed()` runner sync / debugger measurement cost。T0 官方文档闭环 / 渐进式披露、T3 DefinitionAuthoring、T5 GeneratedRuntimeGlue、T6 当前 Demo 业务迁移等旧支线已压缩为历史 proof 或后置参考，不再维护独立活动入口。验证只做内容扫描和尾随空白扫描；因 `方案讨论/针对2.0的ECS架构的迭代方案讨论/*` 被 `.gitignore` 忽略，`git diff` 不展示这些任务树文档差异。

2026-06-07 整体项目 API 健康截面复核：对 `Assets/GAS/Runtime`、`Assets/GAS/Generated/CodeGen/Runtime` 和 `Assets/AutoChessDemo` 执行 `CreateEntityQuery` / `ToEntityArray` / `SystemAPI.Query` / `Complete` / direct `EntityManager` / managed collection 静态扫描后，该截面曾把事实修正为 owner 分类口径：`ToEntityArray` 运行命中 4 处，分别归 Debugger observation 3 处和 Cue managed boundary 1 处；`EntityManager.CreateEntityQuery` 当时只剩 `GasRuntimeDebugger.cs:888`。2026-06-08 Debugger singleton registered owner 切片已删除该 fallback，当前稳定口径为 `CreateEntityQuery` Runtime 可执行命中 0、`ToEntityArray` 运行命中 3 处。pending AttributeDelta owner-local apply 已进入 ASC chunk-local `IJobChunk`；`GASRuntimeFrameContext` current-frame lookup 与 `ActiveEffectStore` global index owner 已改为 registered/cache owner。AutoChess catalog 旧 `ToEntityArray` singleton 查找已过期，当前是缓存 catalog entity，缓存失效时 direct `CreateEntity`。长期事实已反哺 `00/架构重划分审查事实.md`、`00/Runtime主链事实.md`、`00/README.md`、`00/AutoChessDemo事实.md`、`00/Definition配置事实.md` 和相关 ISSUE/P0/P1 文件；目标态约束由 `01/16` API Health Owner Model、`01/07` Debugger evidence source 分层和 `01/90` 不变量 79/127 承载，后续 R3/R4/R7 需要把 singleton carrier、registered/cache owner integrity 和 observation-only query 成本纳入 owner-classified evidence 对账。

2026-06-07 T0 文档维护规则整体复核：全量读取根 README、`规范手册.md`、`00/01/02/04` README、`UnityDOTS官方文档参考/README.md`、`历史方案参考/README.md`、`迭代记录/README.md` 和 `00/_归档/README.md` 后，已把整体职责审查结论和本轮维护规则迭代计划写入 `02/T0-文档治理与目标态共识/文档维护规则迭代计划.md`；`迭代记录/README.md` 中旧 `93-T6-CHESS-AM` “当前推荐入口”口径已修正为历史追溯口径。当前规则维持：`01-目标态架构共识` 只消费抽象后的目标态设计 delta，当前实现事实和复审判断先进入 `00/02/04`。

2026-06-07 R6 AutoChess direct EM owner 分类复核：对照 `AutoChessGasCoreBridge.cs`、`AutoChessGasRuntimeHost.cs`、`AutoChessGasCatalogSession.cs`、`AutoChessGasObservationGateway.cs`、`AutoChessGasRuntimeTicker.cs`、`AutoChessGasBattleEntityLifecycle.cs`、`AutoChessGasCoreContracts.cs`、`AutoChessBattleReportBuilder.cs`、`AutoChessBattleResultBuilder.cs` 和 DOTS `20-GASRuntimeCore-API选型基线` 后，已把 direct `EntityManager` 使用面细分为 host bootstrap / catalog、driver lifecycle、unit lifecycle、unit destroy request、battle unit registry / internal `ASCHandle` command compatibility、structured unit snapshot、observation snapshot、runner sync / debugger timing、driver runtime store 内部 `_driverEntity` owner 和 report key coverage。长期事实已反哺 `00/AutoChessDemo事实.md` 与 `00/架构重划分审查事实.md`；R6 任务已反哺 `02/GAS架构重划分主线任务.md`。下一步执行入口是 raw ECS identity 退出、opaque handle / report key coverage 固化、ASC destroy request owner、catalog install / dispose owner、以及 headless / scene 同构 cost split。

2026-06-07 R6 AutoChess Adapter / Validation Evidence 复核：对照 `AutoChessBattleFlow.cs`、`AutoChessBattleReportBuilder.cs`、`AutoChessRuntimeRunner.cs`、`AutoChessGasCoreBridge.cs`、`AutoChessBattleContracts.cs` 和 DOTS 参考 `20-GASRuntimeCore-API选型基线` 后，确认 AutoChess flow / report 已成为业务验收链正向资产，但当前 timing contract 只覆盖 5 段 GAS physical group timing：`FramePrepare`、`CommandResolve`、`CoreSimulation`、`StructuralCommit`、`BoundaryProjection` 和 total tick；`CreateTimingSummary()` 输出 `ecsRuntimeTickOnly=true`，没有独立 runner / debugger / physics / render 字段。长期事实已反哺 `00/AutoChessDemo事实.md` 与 `00/架构重划分审查事实.md`，R6 任务已反哺 `02/GAS架构重划分主线任务.md`，下一步执行入口是 direct EM owner 分类、raw ASC identity 投影退出、headless / scene 同构 evidence，以及完整 core / boundary / debugger / runner / physics / render split 或 disabled reason。

2026-06-07 R5 SourceGenerator / CodeGen 事实复核：对照 `GasCodeGenPipeline.cs`、`GasGlueCodeGenPhases.cs`、`GasCodeGenValidationReport.md`、`RuntimeDefinitionGlue.gen.cs`、`DefinitionCatalog.gen.cs` 和 DOTS 参考 `20-GASRuntimeCore-API选型基线` / `14-DefinitionCodeGen目标链路Spec` / `15-SourceGenerator职责边界Spec` 后，已把旧结论“Core pipeline 仍包含 Demo phase”修正为当前已缓解事实：Core phase set 已拆离 `AutoChessDemoConfigPhase`，Demo 生成走 standalone output root。本轮又复核到 `Generated Runtime Boundary Gate` 当前为 `blocking-unclassified-migration-proof`，`GeneratedRuntimeUnclassifiedBoundaryHits=0`，但 boundary / lifecycle / structural / ownership / random lookup hits 仍以 classified `MigrationProofOnly` 存在。exact counts 维护在 `00/架构重划分审查事实.md` 和执行交还包，不在 `04` 长期重复。可领取任务已反哺 `02/GAS架构重划分主线任务.md` 与 R5 叶子任务；下一步执行入口是 classified `MigrationProofOnly` 退出、artifact 分类表、template vs `.gen.cs` 对照、catalog install/dispose owner 证明和 Core/Demo evidence 分 owner 消费。

2026-06-07 R4 StructuralCommit / Debugger 证据链复核：对照 `GASGroups.cs`、`GASRuntimeStructuralPlaybackGateContract.cs`、`GasRuntimeOfficialToolDiff.cs`、`GasRuntimeDebugger.cs`、`GASRuntimeDebuggerEvidenceGateContract.cs`、`AutoChessGasCoreBridge.cs`、`AutoChessRuntimeRunner.cs` 和 DOTS 参考 `结构变化-ECB` / `Diagnostics-Debugger` / `21-官方文档覆盖与流程闭环` 后，已把 `contractOnly` gate、`directEntityManagerAllowed=false` route、official diff capture state、Debugger structural counters、AutoChess bridge direct EM 分类、`CompleteAllTrackedJobs()` runner/debugger 成本归因和 AutoChess dataflow / sequence 当前字符串导出事实补入 `00/架构重划分审查事实.md`；`02/GAS架构重划分主线任务.md` 的 R4 交还门槛已收紧到必须说明 contract vs runtime vs official capture、required vs recorded 差异、disabled reason、core/boundary/debugger/runner/physics/render timing split、structured evidence、TopN 热点归因、derived diagram source 和 performance / diagnostic pass 分离。

2026-06-07 文档入口复核：`UnityDOTS官方文档参考/README.md` 的目录结构已改为 `主题/20-GASRuntimeCore-API选型基线.md` 与 `主题/21-官方文档覆盖与流程闭环.md` 承载唯一正文，`元信息` 下同名文件仅作兼容跳转；`04/迭代摘要.md` 的下一轮建议阅读已从旧 AM5 路径改为当前 `02/GAS架构重划分主线任务.md -> 00/架构重划分审查事实.md -> 01/16 Spec -> DOTS README` 链路；T0 计划已把旧 AM/T1 兼容入口纳入漂移分类规则。

2026-06-07 R2/R3 当前事实复核：对照 `RuntimeActiveEffect.gen.cs`、`GEEffectCommandSpecStream.cs`、`GEEffectCommandSpecStreamPhases.cs`、`GASRuntimeStreamOwnerContract.cs`、`GasGlueCodeGenPhases.cs` 和 DOTS 参考 `20-GASRuntimeCore-API选型基线` / `BUF-02` / `STORE-03` / `NAT-03` / `QRY-04` / `PRF-06` / `PRF-19` 后，已把 stream owner contract 的 `SingletonDynamicBuffer -> target carrier -> MigrationCarrier` 事实补入 `00/架构重划分审查事实.md`，并把 R2/R3 任务验收收紧到不能把 singleton serial `IJob` 或 contract 预声明误判为 scale-ready；后续执行必须改模板和 validation gate，不手改 `.gen.cs`。

2026-06-07 文档生成-消费-归档规则迭代：根 README、`规范手册.md`、T0 计划、T0 README、`02/README.md`、`GAS架构重划分主线任务.md`、`04/README.md`、`04/当前窗口.md`、`迭代记录/README.md` 已同步同一条链路。新材料必须先登记信号卡，再按单一 owner 消费；`01-目标态架构共识` 只保留目标态 Spec；`04` 可以暂存生成信号但不能当长期事实仓库；`最近验证摘要.md` 中旧路线命令只保留历史验证语境，不作为当前命令模板；归档内容重新消费前必须重新生成信号卡并重新判定第一 owner。

2026-06-07 文档链路去重：`UnityDOTS官方文档参考/README.md` 的 API selection checkpoint 和官方文档覆盖流程入口已改为 `主题/20-GASRuntimeCore-API选型基线.md`、`主题/21-官方文档覆盖与流程闭环.md`；旧元信息兼容跳转文件只保留跳转说明，不再维护第二份正文。`当前ECS实现GAS概念审查.md`、T1 Runtime README 和 AM2B Frame Backbone 入口已改为当前 `00/01/02/04` 路径，后续执行 Agent 不应再读取旧路线目录或 AM2B 旧目录 README。

## 当前反哺状态

1. `01/03E` 已拆为短索引和 4 个同 owner 目标态子页；拆分前全文进入 `01/_归档`。
2. `01/03B` 已拆为短索引和 3 个同 owner 目标态子页；拆分前全文进入 `01/_归档`。
3. `00/架构重划分审查事实.md` 已拆为短索引和 9 个同 owner 事实子页；拆分前全文进入 `00/_归档`，其中 `07` 承接端到端重划分事实，`08` 承接 TagRequirement query / Definition glue，`09` 承接文档矛盾与过时口径账本。
4. R5 generated registry fail-fast 当前事实已消费到 `00/02/04`；它只缓解缺失 type silent skip，不证明 SourceGenerator pure glue 完成。
5. Debugger registered owner 已收口到 registered/cache owner 事实；Runtime `CreateEntityQuery` 可执行命中为 0，Debugger / Cue `ToEntityArray` observation 成本仍需 R4/R8 证明。
6. Magnitude Source evidence 已进入 Runtime Debugger、AutoChess validation evidence、Unity batchmode x50 归档日志和静态防回流脚本；active effect slot pre-tick SourceAttribute snapshot gather 也已有 x50 Run3 验证。它们仍只证明当前切片链路和 safety 回归通过，不等于真实 magnitude source 非零业务覆盖、generated lifecycle owner 收权或 Profiler 性能闭环已完成。
7. `01-目标态架构共识` 只保留目标态 Owner Map、不变量、代码骨架判定准则和 `16-06` 端到端消息流 Spec；当前事实、行号、验证数字继续留在 `00/04`。
8. `02-主线任务树` 已把 `00/07` 当前端到端事实和 `01/16-06` 目标端到端代码骨架消费到 R0-R8 领取门与交还包；后续执行 Agent 不能只读 `16-01`，也不能只按局部任务证明完成。
9. TagRequirement query 事实已合并到 `00/08` 专题页，`01/15` / `01/90` 只保留目标态边界，R3/R5/R7 已补消费门；后续不得在 Runtime / CodeGen / AutoChess 事实页恢复第二份完整 TagRequirement 正文。
10. Goal 仍未完成：本轮已重跑 Core GasCodeGenCli / SourceGenerator，但仍缺 Unity Test Runner、AutoChess x100/x1000、Profiler / Journaling、Luban process gate、Unity CodeGen 和 R1-R8 退出门。
11. R1/R6 driver public raw handle 已收缩为 opaque `driverId/version`，有效 AutoChess x50 证据为 `00/_归档/2026-06-08-AutoChessBattleValidation-OpaqueDriverHandle-Run7.log`；R5 active effect lifecycle owner 已退出 SourceGenerator 输出、manifest、generated boundary report 和当前磁盘文件列表，SourceGenerator boundary gate 保持 `GeneratedRuntimeUnclassifiedBoundaryHits=0`。该轮仍不证明 driver StructuralCommit owner、generated lifecycle release-ready、Profiler enabled 或 x100/x1000 scale profile 完成。
12. 本轮 T0 规则已明确：下一次文档整理必须先判定采用拆分、合并、降权、归档、矛盾清理还是观察；如果只做拆分，也要说明为什么不合并；如果只做合并，也要说明为什么没有跨 owner 混写。
13. `01/13` 已拆为短索引和 3 个同 owner 目标态子页；拆分前全文进入 `01/_归档`。后续只观察 `03G` / `05` / `16` 是否恢复第二份物理布局、component capacity 或 phase mapping 正文。
14. `01/10/11/21` 已完成相似主题 owner 裁决：`10` 维护 runner / evidence / scale gate，`11` 维护 schema / generated artifacts，`21` 维护样例化消费；后续只观察是否恢复第二份 schema、runner evidence 或 sample 正文。
15. `01/03D`、`01/16-02`、`01/10B-04/05` 已完成通用规则 owner 合并裁决：`03D` 维护 Command / Target Resolve，`16-02` 维护 Boundary command envelope，`10B` 只保留业务案例投影；后续只观察是否恢复第二份通用规则正文。
16. `01/16-06` 与 `16-03..16-05` 已完成完整骨架 / 局部规则 owner map 裁决；`03B-03` 与 `08/14/15` 已完成 generated 配置链 owner 裁决。后续只观察是否恢复第二份局部规则、生成链流程、artifact gate 或 Runtime glue 调用代码。
17. `00/架构重划分审查事实/09-文档矛盾与过时口径事实.md` 已新增为事实侧账本；R0/R0-R8/T0 已接入消费门，`01/90` 第 177-181 条只补目标态文档纯度、相似主题合并、渐进式披露和生成-消费-归档闭环不变量。后续文档整理先对账 `00/09`，再决定拆分、合并、降权、归档、矛盾清理或观察。
18. 本轮 live codedb 截面旧记录：当时追平到 780 chunks / 1792 edges / 2007 indexed symbols / internal 551 / boundary 166 / incoming 138 / outgoing 28 / `GEEffectCommandSpecStream.cs` 967 行 / `ASCCommandBufferResolveSystem.cs` 952 行 / `GasRuntimeDebugger.cs` 4156 行 / `ActiveEffectLifecycleOwnerSystems.cs` 515 行 / `GasGlueCodeGenPhases.cs` 9449 行；后续曾校准过 `GasGlueCodeGenPhases.cs` 的 9467-line 口径，但当前事实以本文件顶部 live 校准为准。旧 773 / 774 / 775 / 777 / 778 / 779 chunks、1991 / 2002 / 2003 / 2017 / 2015 / 2016 / 2018 / 2020 / 2021 / 2022 / 2027 / 2029 indexed symbols、1787 / 1788 / 1789 / 1790 / 1791 / 1794 / 1796 / 1797 / 1799 / 1798 / 1800 / 1801 / 1786 / 1785 / 1784 / 1781 edges、internal 524 / 526 / 529 / 532 / 534 / 523 / 530 / 531、boundary 177 / 181 / 182 / 183 / 179 / 173 / 176 / 175 / 174 / 172 / 171 / 180、incoming 154 / 155 / 156 / 152 / 147 / 150 / 149 / 148 / 146 / 145、outgoing 26、1115 / 1241 / 1253 / 1279 / 1339 / 4146 / 1346 / 1358 / 1361 / 1364 / 924 / 434 / 9458 行与 `GasRuntimeDebugger` 4155 行口径只保留为防回流扫描项，不再作为活动事实卡。
19. 本轮文档治理规则补强已把观察 / 拆分 / 合并 / 降权 / 归档 / 矛盾清理写成整理动作状态机，并同步到 `规范手册.md`、R0 交还包、`00/09` 防回流账本、T0 第四十二轮信号卡和当前窗口。该项只约束文档维护链路，不是 Runtime Core 完成证明；本轮未运行 Unity、AutoChess、Profiler / Journaling、Luban、SourceGenerator、Unity CodeGen 或 build。

## 下一轮建议阅读

1. [当前窗口](当前窗口.md)
2. [最近验证摘要](最近验证摘要.md)
3. [00/架构重划分审查事实](../00-当前架构事实/架构重划分审查事实.md) 与 [事实子页](../00-当前架构事实/架构重划分审查事实/README.md)
4. [00/09 文档矛盾与过时口径事实](../00-当前架构事实/架构重划分审查事实/09-文档矛盾与过时口径事实.md)
5. [02/R0-R8 叶子任务索引](../02-主线任务树/R0-R8/README.md)
6. [01/16-01 目标分层官方依据与不变量](../01-目标态架构共识/16-纯ECS内核与边界重划分/16-01-目标分层官方依据与不变量Spec.md)
7. [01/16-06 端到端消息流代码骨架](../01-目标态架构共识/16-纯ECS内核与边界重划分/16-06-端到端消息流代码骨架Spec.md)
8. [01/16-02 Boundary Command](../01-目标态架构共识/16-纯ECS内核与边界重划分/16-02-BoundaryCommand与CoreCommandResolveSpec.md) / [01/03D Command Resolve 与 Target Resolve](../01-目标态架构共识/03-RuntimeCore管线/03D-CommandResolve与TargetResolveSpec.md)
9. [01/16-03 Fan-in / Debugger / SourceGenerator](../01-目标态架构共识/16-纯ECS内核与边界重划分/16-03-FanInDebuggerSourceGeneratorSpec.md) / [01/16-04 Shell Capability](../01-目标态架构共识/16-纯ECS内核与边界重划分/16-04-ShellCapabilityContractSpec.md) / [01/16-05 Snapshot / Identity / API Health](../01-目标态架构共识/16-纯ECS内核与边界重划分/16-05-SnapshotIdentityApiHealthSpec.md)
10. [01/03B 业务调用链与配置消费](../01-目标态架构共识/03-RuntimeCore管线/03B-业务调用链与配置消费Spec.md) / [03B-03 Generated Runtime Glue 消费接口](../01-目标态架构共识/03-RuntimeCore管线/03B-业务调用链与配置消费/03B-03-GeneratedRuntimeGlue消费接口Spec.md)
11. [01/08 Luban / SourceGenerator 配置生成链路](../01-目标态架构共识/08-Luban-SourceGenerator配置生成链路Spec.md) / [01/14 Definition CodeGen](../01-目标态架构共识/14-DefinitionCodeGen目标链路Spec.md) / [01/15 SourceGenerator 职责边界](../01-目标态架构共识/15-SourceGenerator职责边界Spec.md)
12. [01/03E Effect Fan-In / State / Attribute / Fact](../01-目标态架构共识/03-RuntimeCore管线/03E-EffectFanIn-State-Attribute-FactSpec.md)
13. [01/13 Entity / Component 物理布局](../01-目标态架构共识/13-EntityComponent物理布局Spec.md) 与 [13 子页索引](../01-目标态架构共识/13-EntityComponent物理布局/README.md)
14. [01/10B AutoChess 完整业务案例](../01-目标态架构共识/10B-AutoChess完整业务案例设计Spec.md) / [10B 子页索引](../01-目标态架构共识/10B-AutoChess完整业务案例/README.md)
15. [01/10 AutoChess 无头验收](../01-目标态架构共识/10-AutoChess无头验收Spec.md) / [01/11 AutoChess Luban 配置方案](../01-目标态架构共识/11-AutoChessDemo-Luban配置方案Spec.md) / [01/21 AutoChess 策划配置验收样例](../01-目标态架构共识/21-AutoChessDemo策划配置验收样例Spec.md)
16. [Unity DOTS API 选型基线](../../UnityDOTS官方文档参考/主题/20-GASRuntimeCore-API选型基线.md) / [流程闭环](../../UnityDOTS官方文档参考/主题/21-官方文档覆盖与流程闭环.md) / [规则编号索引](../../UnityDOTS官方文档参考/主题/90-规则编号索引.md)
