# 最近验证摘要

## 本轮静态验证（T1-RuntimeCore-AM3 RuntimeFrameContext fallback query 去临时化）

1. `rg -n "CreateEntityQuery\(ComponentType\.ReadOnly<GlobalTimer>" Assets/GAS/Runtime -g "*.cs"` 无命中，确认 current-frame fallback query 已删除。
2. `rg -n "RuntimeFrameContextCurrentFrameNoLongerOwnsHelperTempQueryRisk|RuntimeFrameContextDoesNotCreateFallbackQueryForIsolatedWorld|SUM-20260525-AM3-RFCF|fallback query 已删除|fallback query 已去除" Assets/GAS/Runtime Assets/_Test/GAS/Runtime "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"` 已确认 Runtime / tests / docs 新锚点可检索。
3. 旧测试名 / 旧验证结论锚点复扫后无有效命中，避免文档继续声明 current-frame fallback query 仍存在。
4. `rg -n "[ \t]+$" -- <本轮触达 code/docs>` 无命中。
5. `git diff --check -- <本轮触达 tracked code/docs>` 通过，仅有 LF -> CRLF warning；未跟踪测试文件由尾随空白检查覆盖。
6. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
7. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上，tests build 会连带构建 AutoChessDemo 相关 csproj。
8. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境阻塞解除后补跑。
9. API 选型：本轮采用 `GASManager.EntityGlobalTimer` known-owner main-thread read 作为迁移期 current-frame 来源，拒绝 `EntityManager.CreateEntityQuery` fallback / `ToEntityArray(Allocator.Temp)` / unknown world 自动猜测 frame；拒绝在本轮切 `NativeStream` / job 化 / 真实 frame owner，因为当前任务只删除 current-frame fallback query，不处理多 job fan-in 或真实 SystemGroup 搬迁。重新选型触发条件为 RuntimeCore frame owner 落地、profiler 指向 main-thread dependency wait，或需要多 World 并行验证。

## 本轮静态验证（T1-RuntimeCore-AM3 Command Writer Frame Query 收缩）

1. `rg -n "CommandWriterReusesResolvedFrameAndFlushesStreamCounters|BeginCommandWriter|writer\\.AppendCommand|writer\\.Flush|ResolveCurrentFrame|EnsureSingleton" Assets/GAS/Runtime/Effect/Component/Dynamic/CEffectCommandSpecStream.cs Assets/GAS/Runtime/System/Effect/GameplayEffectRequestWriter.cs Assets/_Test/GAS/Runtime/Effect/EffectCommandSpecStreamContractTests.cs` 已确认 Runtime / tests 锚点可检索。
2. `git diff --check -- Assets/GAS/Runtime/Effect/Component/Dynamic/CEffectCommandSpecStream.cs Assets/GAS/Runtime/System/Effect/GameplayEffectRequestWriter.cs Assets/_Test/GAS/Runtime/Effect/EffectCommandSpecStreamContractTests.cs` 通过，仅有 LF -> CRLF warning。
3. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
4. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
5. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
6. 官方 GAS 概念参考：已复核 Epic Gameplay Ability System 概览和 `tranek/GASDocumentation` 仓库入口，仅用于 Ability / GameplayEffect / Attribute / Cue 概念边界；Unity ECS 落地仍以本地 DOTS 参考为第一性依据。
7. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`；本轮不涉及 Physics / Entities Graphics，`ODF-15..18` not-related。
8. API 选型：继续使用 `CEffectCommandSpecStream` singleton DynamicBuffer 作为 AM3 proof-only 承载，但新增 `CommandWriter` 让批量 simple instant command 写入一次解析 stream entity / current frame / buffer 后复用；拒绝 `NativeStream` / `CASE-12` 是因为当前目标是收缩主线程 helper 内 per-command query，不处理多 job parallel fan-in / merge phase。

## 本轮静态验证（T1-RuntimeCore-AM5 Active Effect Store - Period Due / Overflow Simple Instant Derived Command）

1. `rg -n "PeriodSimpleInstantDerivedEffectWritesEffectCommandAndUpdatesStoreCursor|TryRefreshPeriodFrame|EEffectCommandSource\\.Overflow|TryAppendDerivedEffectCommand" Assets/GAS/Runtime Assets/_Test/GAS/Runtime "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"` 已确认 Runtime / tests / docs 锚点可检索。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
3. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
4. `rg -n "[ \t]+$" <本轮触达 code/docs>` 无命中。
5. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
7. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`；本轮不涉及 Physics / Entities Graphics，`ODF-15..18` not-related。
8. API 选型：继续使用 `CEffectCommandSpecStream` singleton DynamicBuffer 作为 AM5 period / overflow 派生命令 proof-only 承载；拒绝旧 request/runtime GE entity 作为 simple child GE 默认路径是为了避免 period tick 高频结构变化；拒绝 `NativeStream` / `CASE-12` 是因为当前 producer 仍在旧 lifecycle 主线程派生，未进入多 job parallel fan-in；拒绝 ECB `AppendToBuffer` 是因为本轮不做结构变化，也不需要 playback 可见性屏障。

## 上轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Multi-Target Simple Instant Command Fan-out）

1. `rg -n "AppendSimpleInstantCommands|CanAppendSimpleInstantCommand|RequestApplyEffectsWithMultipleSimpleInstantTargets|FallsBackWithoutPartialCommands" Assets/GAS/Runtime Assets/_Test/GAS/Runtime -g "*.cs"` 已确认 Runtime / tests 锚点可检索。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
3. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上，tests 工程会包含当前 AutoChessDemo 依赖但未出现编译错误。
4. `rg -n "[ \t]+$" <本轮触达 code/docs>` 无命中。
5. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
7. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`，`com.unity.physics` 1.4.6 at `Library/PackageCache/com.unity.physics@22d10f355559`，`com.unity.entities.graphics` 1.4.19 at `Library/PackageCache/com.unity.entities.graphics@1e91bb5cdef3`；本轮不涉及 Physics / Entities Graphics，`ODF-15..18` not-related。
8. API 选型：继续使用 `CEffectCommandSpecStream` singleton DynamicBuffer 作为 AM3 proof-only 多目标 fan-out 承载；拒绝 `NativeStream` / `CASE-12` 是因为当前 producer 仍是主线程 Timeline fan-out，非多 job 并行 fan-in；拒绝 ECB / `CASE-47` 是因为本轮不做结构变化，也不需要并行 `AppendToBuffer`。

## 上轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Damage Typed Fact Native Observation Consumer）

1. `rg -n "EGameplayFactDomain\\.Damage|AppendTypedDamageFact|CountPresentationDamageEvents|CountDebugReplayDamageEvents|SourceFactSequence|EGasStructuredLogModule\\.Damage" Assets/GAS/Runtime Assets/_Test/GAS/Runtime/Event/CommandEventBridgeContractTests.cs` 已确认 Runtime / tests 锚点可检索。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
3. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上，tests 工程会包含当前 AutoChessDemo 依赖但未出现编译错误。
4. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
5. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`，`com.unity.physics` 1.4.6 at `Library/PackageCache/com.unity.physics@22d10f355559`，`com.unity.entities.graphics` 1.4.19 at `Library/PackageCache/com.unity.entities.graphics@1e91bb5cdef3`；本轮不涉及 Physics / Entities Graphics，`ODF-15..18` not-related。

## 本轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Generic Typed Fact Native Observation Consumer）

1. `rg -n "ResolveGameplayEventCode|TypedGameplayFact|AppendTypedGameplayFact|TryCreateTypedPresentationEvent|TryCreateTypedReplayEvent" Assets/GAS/Runtime/System/Event Assets/_Test/GAS/Runtime/Event/CommandEventBridgeContractTests.cs` 已确认 Runtime / tests 锚点可检索。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
3. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上，tests 工程会包含当前 AutoChessDemo 依赖但未出现编译错误。
4. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
5. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`；本轮不涉及 Physics / Entities Graphics，`ODF-15..18` not-related。

## 本轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Cue Typed Fact Native Observation Consumer）

1. `rg -n "Cue Typed Fact Native Observation Consumer|TypedCueFact|CueProjectionSpecCursor|SourceFactSequence|ReasonCode|TryCreateTypedPresentationEvent|TryCreateTypedReplayEvent" Assets/GAS/Runtime Assets/_Test/GAS/Runtime "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"` 已确认 Runtime / tests / docs 锚点可检索。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
3. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上，tests 工程会包含当前 AutoChessDemo 依赖但未出现编译错误。
4. `rg -n "[ \t]+$"` 对本轮触达 code / docs 无命中。
5. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
7. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`；本轮不涉及 Physics / Entities Graphics，`ODF-15..18` not-related。

## 本轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Typed Fact Native Observation Consumer）

1. `rg -n "Typed Fact Native Observation Consumer|SourceFactSequence|ProcessedTypedFactCount|LastProjectedTypedFactSequence|TypedSimulationFactBuffer|attribute typed fact native Presentation / Replay consumer" Assets/GAS/Runtime Assets/_Test/GAS/Runtime "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"` 已确认 Runtime / tests / docs 锚点可检索。
2. `rg -n "[ \t]+$"` 对本轮触达 code / docs 无命中。
3. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
4. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突。
5. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上，tests 工程会包含当前 AutoChessDemo 依赖但未出现编译错误。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
7. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`。

## 本轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Cue-on-Apply Projection）

1. `rg -n "SInstantEffectCueRequestProjection|CueProjectionSpecCursor|CueRequestOnApplyCode|ProjectsCueOnApply|RequestApplyEffectsWithCueOnApplyWritesStreamAndProjectsCue" Assets/GAS/Runtime Assets/_Test/GAS/Runtime "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"` 已确认 Runtime / tests / docs 锚点可检索。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
3. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
4. `rg -n "[ \t]+$"` 对本轮触达 code / docs 无命中。
5. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。
7. 官方 DOTS 证据：`com.unity.entities` 1.4.6 at `Library/PackageCache/com.unity.entities@e90944159b94`，`com.unity.collections` 2.6.6 at `Library/PackageCache/com.unity.collections@9796e5ee0d9e`，`com.unity.burst` 1.8.29 at `Library/PackageCache/com.unity.burst@6bb9aca3ef38`，`com.unity.mathematics` 1.3.3 at `Library/PackageCache/com.unity.mathematics@19a9377c4ffa`。

## 本轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Typed Fact Attribute Observation Bridge）

1. `rg -n "STypedSimulationFactEventBridge|EventBridgeFactCursor|Typed Fact Attribute Observation Bridge|AttributeBaseValueChanged|attribute typed fact observation bridge" Assets/GAS/Runtime Assets/_Test/GAS/Runtime "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"` 已确认 Runtime / tests / docs 锚点可检索。
2. `rg -n "[ \t]+$"` 对本轮触达 code / docs 无命中。
3. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
4. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
5. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
6. 本轮尚未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Timeline ApplyEffects Single Target Producer）

1. `rg -n "TimelineApplyEffectsProducer|TimelineApplyEffectsAppliesSimpleInstantThroughEffectCommandStream|RequestApplyEffectsWithCatchSelfSimpleInstantWritesEffectCommandStream|RequestApplyEffectsWithCatchTargetSimpleInstantWritesEffectCommandStream|RequestApplyEffectsWithCueOnApplyWritesStreamAndProjectsCue|AppendSimpleInstantCommandOrCreateSingleTargetRequest" Assets/GAS/Runtime/Ability/TimelineAbility/TimelineApplyEffectsProducer.cs Assets/_Test/GAS/Runtime/Ability/TimelineApplyEffectsProducerTests.cs Assets/_Test/GAS/Runtime/Ability/AbilityTimelineActionSystemTests.cs` 已确认 Runtime / tests 锚点可检索。
2. `rg -n "Timeline ApplyEffects single-target|TimelineApplyEffects single-target|Timeline ApplyEffects|T1-RuntimeCore-AM3" <本轮触达 docs>` 已确认任务树、当前事实、目标态 Spec 和进度状态均有本轮回写锚点。
3. `rg -n "[ \t]+$"` 对本轮触达 code / docs 无命中。
4. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
5. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 顺序重跑通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
6. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 顺序重跑通过，0 error；warning 同上。
7. 首次并行构建 runtime / tests 时 runtime csproj 曾因两个构建进程同时写 `obj\Debug\com.exhard.exgas.runtime.dll` 出现 CS2012 文件锁；顺序重跑已通过，判定为并发构建输出锁，不是代码错误。
8. 本轮尚未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM3 Instant Spec Evaluation - Cost Self Producer）

1. `rg -n "RequestCostGameplayEffect|AppendSimpleInstantSelfCommandOrCreateRequest|RequestCostGameplayEffectWritesSimpleInstantSelfCommand|RequestCostGameplayEffectAppliesSimpleInstantThroughEffectCommandStream" Assets/GAS/Runtime Assets/_Test/GAS/Runtime -g "*.cs"` 已确认 Runtime / tests 锚点可检索。
2. `rg -n "[ \t]+$"` 对本轮触达 code / docs 无命中；全 `当前路线` 扫描仍可命中既有 UnityDOTS 官方参考文档空白，不属于本轮触达。
3. `git diff --check -- <本轮触达 code/docs>` 通过，仅有 LF -> CRLF warning。
4. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
5. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
6. 本轮尚未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM2B-F AM3 / AM5 Rebind 与交还验证）

1. `rg -n "FrameBackboneRebind|T1-RuntimeCore-AM3|T1-RuntimeCore-AM5|NoAutoChessBusinessChange" Assets/GAS/Runtime Assets/_Test/GAS/Runtime com.exhard.exgas.runtime.csproj com.exhard.exgas.runtime.tests.csproj -g "*.cs" -g "*.csproj"` 已确认 Runtime / tests / csproj 锚点可检索。
2. `rg -n "[ \t]+$"` 对本轮触达 code / meta / docs / csproj 无命中。
3. `git diff --check -- <本轮触达 tracked docs/csproj>` 通过，仅有 CRLF warning；新增 code / meta 通过尾随空白扫描覆盖。
4. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
5. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM2B-E Debugger Evidence Gate）

1. `rg -n "GASRuntimeDebuggerEvidenceGate|RuntimeCoreFrameBackbone|RecordCurrentRuntimeCoreFrameBackboneEvidence|runtimeCoreFrameBackbone" Assets/GAS/Runtime Assets/_Test/GAS/Runtime com.exhard.exgas.runtime.csproj -g "*.cs" -g "*.csproj"` 已确认 Runtime / tests / csproj 锚点可检索。
2. `rg -n "[ \t]+$"` 对本轮触达 code / meta / csproj 无命中。
3. `git diff --check -- <本轮触达 code/meta/csproj>` 通过，仅有 CRLF warning。
4. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
5. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM2B-D Structural Playback Gate）

1. `rg -n "GasStructuralPlayback|StructuralPlaybackGate|RecordRuntimeCoreStructuralPlaybackGate|ecbCommands|bulkQueries" Assets/GAS/Runtime Assets/_Test/GAS/Runtime com.exhard.exgas.runtime.csproj -g "*.cs" -g "*.csproj"` 已确认 Runtime / tests / csproj 锚点可检索。
2. `rg -n "[ \t]+$"` 对本轮触达 code / meta / csproj 无命中。
3. `git diff --check -- <本轮触达 code/meta/csproj>` 通过，仅有 CRLF warning。
4. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为既有 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
5. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM2B-C Stream Owner 与 Deterministic Merge）

1. `rg -n "StreamOwner|DeterministicMerge|ProofOnly|NativeStream|MergePolicy|GASRuntimeFrameStreamOwner" Assets/GAS/Runtime Assets/_Test/GAS/Runtime -g "*.cs"` 已确认 stream owner contract 和 Runtime tests 可检索。
2. `git diff --check -- Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStreamOwnerContract.cs Assets/_Test/GAS/Runtime/Event/RuntimeStreamOwnerContractTests.cs com.exhard.exgas.runtime.csproj com.exhard.exgas.runtime.tests.csproj` 通过，无输出。
3. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
4. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
5. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM2B-B Frame Arena 与 Query Budget）

1. `rg -n "QueryBudget|LookupUpdateBudget|AllocatorOwner|DependencyBudget|WorldUpdateAllocator|Rewindable|RuntimeCoreFrameBudget|GASRuntimeFrameBudget" Assets/GAS/Runtime Assets/_Test/GAS/Runtime -g "*.cs"` 已确认 frame budget contract、Debugger 预算字段和 Runtime tests 可检索。
2. `git diff --check -- Assets/GAS/Runtime/System/SystemGroup/GASRuntimeFrameBudgetContract.cs Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs Assets/_Test/GAS/Runtime/Event/RuntimeFrameBudgetContractTests.cs Assets/_Test/GAS/Runtime/Debugger/GasRuntimeDebuggerTests.cs com.exhard.exgas.runtime.csproj com.exhard.exgas.runtime.tests.csproj` 通过，仅 CRLF 提示。
3. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，0 error；warning 为 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
4. `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 通过，0 error；warning 同上。
5. 曾并行构建 runtime / tests 时遇到 `CS2012 obj\Debug\com.exhard.exgas.runtime.dll` 被另一构建进程占用；单独重跑 runtime build 后通过，判定为并行构建锁文件，不是代码错误。
6. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM2B-A Schedule / Phase Contract）

1. `rg -n "FramePrepare|CommandIngest|SpecEvaluation|StructuralPlayback|RuntimeCoreFrame" Assets/GAS/Runtime Assets/_Test/GAS/Runtime -g "*.cs"` 已确认 Runtime Core frame phase contract 和测试可检索。
2. `git diff --check -- Assets/GAS/Runtime/System/SystemGroup/GASSystemScheduleContract.cs Assets/_Test/GAS/Runtime/Event/SystemScheduleContractTests.cs` 通过，仅 CRLF 提示。
3. 全局 `git diff --check` 当前未通过，失败点来自工作区已有 `Assets/UniversalRenderPipelineGlobalSettings.asset` 和 `ProjectSettings/ProjectSettings.asset` trailing whitespace；这些文件不是本轮触达文件。
4. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 仍不直接访问 Runtime internal writer。
5. `rg -n "TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言。
6. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，10 warning / 0 error；warning 为 Unity / NetStandard 引用版本冲突、既有 `GameplayTag` hash warning 和 BurstCompile warning。
7. 本轮未跑 Unity Runtime tests；当前历史阻塞仍是 LicensingClient IPC return code 199，待环境可用后补跑。

## 本轮文档验证（Unity Physics / Entities Graphics 新包覆盖）

1. 已确认当前 Unity 版本为 `6000.3.14f1`，并确认 `Packages/manifest.json` / `packages-lock.json` 中 `com.unity.physics 1.4.6` 与 `com.unity.entities.graphics 1.4.19` 为直接依赖。
2. 已确认当前 PackageCache 实际目录为 `com.unity.physics@22d10f355559`、`com.unity.entities.graphics@1e91bb5cdef3`，并以本地 `Documentation~` 作为当前项目版本第一性证据。
3. 已对照 Unity 官方在线文档复核 Physics pipeline / singleton / query / simulation event，以及 Entities Graphics requirements / runtime entity creation / performance / material override 口径。
4. 已执行全文检索，确认 `Unity Physics`、`Entities Graphics`、`ODF-15`、`ODF-17`、`PHY-`、`GFX-`、`PhysicsWorldSingleton`、`SimulationSingleton`、`RenderMeshArray`、`MaterialMeshInfo`、`AM-1G` 已在目标态 Spec、任务树、治理规范和当前进度中贯通。
5. 本轮只修改文档，不运行 Unity 编译测试。

## 本轮信息验证（Unity DOTS 官方文档二次查缺补漏）

1. 已执行 `rg -n "Managed components|NativeContainer|EntityPrefabReference|RequestEntityPrefabLoaded|Burst Inspector|custom allocator|alias" Library\PackageCache\...`，确认本轮新增主题在本地官方文档中可追溯。
2. 已确认当前 PackageCache 实际目录为 `com.unity.entities@e90944159b94`、`com.unity.burst@6bb9aca3ef38`、`com.unity.collections@9796e5ee0d9e`、`com.unity.mathematics@19a9377c4ffa`。
3. 已执行文档层级审查，确认 `UnityDOTS官方文档参考/README.md -> 单主题文档 -> Debugger / Luban / AutoChess -> 02 / 03 / 04` 已形成 owner 路由。
4. 本轮只修改文档，不运行 Unity 编译测试。

## 本轮文档验证（Unity DOTS 官方文档参考体系主题化与流程闭环）

1. 已执行 `rg -n "官方文档参考体系|UnityDOTS官方文档参考|ODF-01|官方文档覆盖检查|AM-1E|AM1E" "方案讨论\针对2.0的ECS架构的迭代方案讨论\当前路线"`，确认主题入口、`ODF-*`、行动报告字段、任务树入口和当前窗口均已贯通。
2. 已执行 `git diff --check -- "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"`，无输出，未发现 diff 空白错误。
3. 已执行 `rg -n "[ \t]+$"` 检查本轮核心修改文档，无命中，未发现行尾空白。
4. 本轮只修改文档，不运行 Unity 编译测试。

## 本轮静态验证（T1-RuntimeCore-AM5 Active Effect Store Debugger 指标）

1. `git diff --check` 通过，仅有 CRLF 提示。
2. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 仍不直接访问 Runtime internal writer。
3. `rg -n "TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言。
4. `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 通过，结果为 4 个既有 warning、0 error；warning 来源为 Unity / NetStandard 引用版本冲突。
5. `GasRuntimeDebugger` 已能在 `runtimeCoreActiveEffectStore` 文本行输出 owner、slot、capacity、PendingApply / Active / Inhibited / PendingRemove、legacyBacked 和 externalizedOwners。
6. Unity Runtime tests 仍未进入编译 / 测试执行，`powershell -ExecutionPolicy Bypass -File 'Tools/Tests/Run-StackingRuntimeTests.ps1' -TestFilter GAS.Runtime.Tests` 在 LicensingClient IPC 阶段退出，返回码 199。

## 本轮文档验证（Unity DOTS 官方案例反推 Runtime Core）

1. `rg -n "UnityDOTS官方案例|CASE-01|CASE-12|AM1D|DocCodeSamples|PerformanceTests|官方案例对照" "方案讨论\针对2.0的ECS架构的迭代方案讨论\当前路线"` 已确认 `17`、`CASE-*`、`AM1D` 和任务入口贯通。
2. `rg -n "UnityDOTS官方文档参考/主题/12-官方案例模式|CASE-" "方案讨论\针对2.0的ECS架构的迭代方案讨论\当前路线\01-目标态架构共识"` 已确认目标态 Spec 索引、使用规范、API 选型、Runtime 管线、Debugger、Luban、AutoChess 已接入。
3. `rg -n "CASE-|官方案例对照|AM1D" "方案讨论\针对2.0的ECS架构的迭代方案讨论\当前路线\02-主线任务树"` 已确认 T1 / T2 / T4 / T6 任务树接入。
4. `git diff --check -- "方案讨论/针对2.0的ECS架构的迭代方案讨论/当前路线"` 无输出；注意 `方案讨论/` 当前被 git ignore，该命令只作为无报错检查，不代表 git 会展示忽略文档差异。
5. `rg -n "[ \t]+$"` 对本轮核心修改文档无命中，未发现行尾空白。

本轮只修改文档，不运行 Unity 编译测试。

## 本轮静态验证（T1-RuntimeCore-AM5 Active Effect Store 第一刀）

1. `git diff --check` 通过，仅有 CRLF 提示。
2. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 仍不直接访问 Runtime internal writer。
3. `rg -n "TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言。
4. 辅助 `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 在临时把新增 runtime 文件加入 Unity 生成 csproj 后通过，结果为 10 个既有 warning、0 error；临时 csproj 改动已还原，当前 csproj 无 diff。
5. Unity Runtime tests 仍未进入编译 / 测试执行，`powershell -ExecutionPolicy Bypass -File 'Tools/Tests/Run-StackingRuntimeTests.ps1' -TestFilter GAS.Runtime.Tests` 在 LicensingClient IPC 阶段退出，返回码 199。
6. 辅助 `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 未能作为有效测试编译证据：构建被当前暂停的 AutoChessDemo 迁移残留 partial / 访问边界错误挡住，不作为本轮 Runtime AM5 失败。

## 本轮静态验证（T1-RuntimeCore-AM3 Producer Migration）

1. `git diff --check` 通过，仅有 CRLF 提示。
2. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 仍不直接访问 Runtime internal writer。
3. `rg -n "TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言。
4. `SAbilityCommit` 的 activation self / target simple single-target producer 已从旧 legacy bypass request writer 切到 `AppendSimpleInstantCommandOrCreateSingleTargetRequest`；simple instant GE 在命中 AM3 条件时写入 `BEffectCommand` stream，不创建 request entity。
5. 该轮 `CanApplySimpleInstantSpec` 仍排除 Cue-on-Apply、Duration、Period、Stacking、Tag requirements、GrantedTags、GrantedAbilities 等复杂语义；其中 Cue-on-Apply fallback 已被后续 `SInstantEffectCueRequestProjection` 小闭环收敛。
6. 辅助 `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 在临时把新增 runtime 文件加入 Unity 生成 csproj 后通过，结果为 10 个既有 warning、0 error；临时 csproj 改动已还原，当前 csproj 无 diff。
7. 辅助 `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 未能作为有效测试编译证据：构建先被当前暂停的 AutoChessDemo 迁移残留 partial / 访问边界错误挡住，不作为本轮 Runtime AM3 producer migration 失败。
8. Unity Runtime tests 仍未进入编译 / 测试执行，Unity batchmode 在 LicensingClient IPC 阶段退出，返回码 199。

## 上轮静态验证（T1-RuntimeCore-AM3 Direct Proof）

1. `git diff --check` 通过，仅有 CRLF 提示。
2. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 仍不直接访问 Runtime internal writer。
3. `rg -n "TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言。
4. 辅助 `dotnet build com.exhard.exgas.runtime.csproj --no-restore` 在临时把本轮新增 runtime 文件加入 Unity 生成 csproj 后通过，结果为 10 个既有 warning、0 error；临时 csproj 改动已还原，当前 csproj 无 diff。
5. AM3 行为测试覆盖 direct simple instant command 的 constant modifier、SetByCaller magnitude、spec sequence 回写、attribute 更新、delta/fact 生成，以及不创建 request/runtime GE entity；Unity test runner 当前仍未进入编译，测试需待 LicensingClient 可用后补跑。

## 本轮静态验证（T1-RuntimeCore-AM2）

1. `git diff --check` 通过，仅有 CRLF 提示。
2. `rg -n "EffectCommandSpecStream|BEffectCommand|BInstantEffectSpec|BAttributeDelta|BTypedSimulationFact" Assets/GAS/Runtime Assets/_Test/GAS/Runtime -g "*.cs"` 命中 AM2 数据契约、pipeline contract、Debugger counters、query layout 和契约测试。
3. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 仍不直接访问 Runtime internal writer。
4. `rg -n "TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言。
5. 辅助 `dotnet build com.exhard.exgas.runtime.tests.csproj --no-restore` 在临时把本轮新增 runtime/test 文件加入生成 csproj 后，已越过 GAS Runtime 编译阶段；失败点位于当前暂停的 AutoChessDemo 迁移残留 partial / 访问边界，不作为本轮 Runtime AM2 代码失败。

## 本轮文档校准（Unity Entities）

1. 新增 `UnityDOTS官方文档参考/主题/01-Entities系统与World.md`。
2. 新增 `UnityDOTS官方文档参考/主题/90-规则编号索引.md`，将 Unity 官方 ECS 使用规则转译为 `SYS/JOB/QRY/SC/ECB/BUF/EN/FIL/DEF/NAT/DBG` 规则编号和 Runtime Core 业务编写检查表。
3. Runtime Core、EffectCommand、ActiveEffectStore、Observation、Debugger、Luban 相关 Spec 已接入 Unity Entities 1.4.6 的 SystemGroup、ISystem/job、ECB playback、DynamicBuffer、Enableable、Blob/Baker、Query filter、Profiler / Journaling 机制约束。
4. 新增 `00-当前架构事实/核心问题诊断/ISSUE-008-目标态Spec尚未充分UnityEntities机制化.md`。
5. 下一轮 Runtime Core 任务应优先领取 `T1-RuntimeCore-AM1 Unity Entities 机制校准`，并在行动报告中列出适用规则编号，完成后再继续 AM-2。

## 本轮静态验证（T4-Debugger-AM1）

1. `git diff --check` 通过，仅有 CRLF 提示。
2. `rg -n "new GasRuntimeDiagnosticSnapshot\(|new GasRuntimeCoreDiagnosticCounters\(" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 确认 snapshot / counters 构造点已更新。
3. `rg -n "RuntimeCoreCounters|CollectRuntimeCoreCounters|runtimeCoreCounters|presentationCursorLag|replayCursorLag" ...` 确认 Runtime Debugger、AutoChess summary 和 Spec / ISSUE 文档均已接入 AM-1 口径。
4. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 仍不直接访问 Runtime internal writer。
5. `rg -n "TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/AutoChessDemo Assets/_Test/GAS/Runtime -g "*.cs"` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言。

## 上轮静态验证（T1-RuntimeCore-AM0）

1. `rg -n "GameplayEffectRequestWriter\.ApplyFastOrCreateSingleTargetRequest|TryApplyFastInstantModifier|ApplyFastOrCreateSingleTargetRequest|FastInstant|FastDirect" Assets/GAS/Runtime Assets/_Test/GAS/Runtime Assets/AutoChessDemo` 仅命中 `RuntimeCoreFreezeSafetyGateTests` 中的禁止命名断言，Runtime / AutoChessDemo 代码不再暴露旧 `FastInstant` 命名入口。
2. `rg -n "GameplayEffectRequestWriter\." Assets/AutoChessDemo -g "*.cs"` 无结果，AutoChessDemo 不再直接访问 Runtime internal writer。
3. `rg -n "LegacyInstantEntityLifecycle|EffectCommandSpecStream|NoNewFeatureExpansion|GameplayEffectLegacyBridge|RuntimeCoreFreezeSafetyGateTests" Assets/GAS/Runtime Assets/_Test/GAS/Runtime Assets/AutoChessDemo` 命中 AM0 contract、legacy bridge、契约测试和现有 demo legacy bridge 调用。
4. `git diff --check` 通过，仅有 CRLF 提示。

## Unity 验证状态

本轮执行：

```text
powershell -ExecutionPolicy Bypass -File 'Tools/Tests/Run-StackingRuntimeTests.ps1' -TestFilter GAS.Runtime.Tests
```

结果：未进入编译 / 测试执行，Unity batchmode 在 LicensingClient IPC 阶段退出，返回码 199。

关键日志：

```text
[Licensing::Module] Trying to connect to existing licensing client channel...
[Licensing::IpcConnector] Connection to channel LicenseClient-Y refused
[Licensing::Module] Successfully launched the LicensingClient (PId: 16904)
[Licensing::Module] Timed-out after 60.01s, waiting for channel: "LicenseClient-Y"
IPC channel to LicensingClient doesn't exist; aborting
Application will terminate with return code 199
```

## 当前结论

1. `T1-RuntimeCore-AM5` 已完成 ActiveEffectStore 第一刀与 Debugger 指标小闭环：ASC 创建时具备 owner-local store 和 slot buffer，旧 duration runtime GE entity 的 Active / Inhibited / PendingRemove / Remove、duration refresh 和 stack count 已镜像到 owner slot；Debugger 已能输出 slot pressure、state distribution、legacy-backed 和 externalized owner 证据。这仍是 legacy entity-backed mirror，不代表完整 active effect entity lifecycle 已迁移完成。
2. `T1-RuntimeCore-AM3` 已完成 direct simple instant command 主链代码落点，并已让 ability activation simple single-target producer 接入 command stream；command 能生成 spec，constant / SetByCaller modifier 能写 attribute delta，并投影 typed fact；Unity 验证待补跑。
3. `T1-RuntimeCore-AM2` 已完成代码契约静态收口：EffectCommand / InstantEffectSpec / AttributeDelta / ActiveEffectMutation / TypedSimulationFact 已落成 ECS buffer contract，schedule / query layout / Debugger counters 已接入。
4. `T4-Debugger-AM1` 的代码 / 文档静态收口仍保持：RuntimeCoreDebugger 可采样 core counters、cursor lag、buffer peak，并接入 AutoChess validation summary；AM2/AM3 已把 command/spec/delta/fact stream 纳入权威计数来源。
5. `T1-RuntimeCore-AM0` 的代码 / 文档静态收口仍保持：旧 instant GE entity lifecycle 被标记为 legacy frozen / migration-only，旧 direct instant bypass 改为 `LegacyInstant...Bypass` 命名。
6. Unity 未进入编译，Runtime tests 待 LicensingClient 可用后补跑。
7. AutoChessDemo 的继续重构已暂停；当前只作为 Runtime 重构后的全链路验证与性能反馈工具；辅助 dotnet 编译暴露的 AutoChessDemo 残留问题不在本轮继续推进。

