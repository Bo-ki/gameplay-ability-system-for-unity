# 当前窗口

| 项 | 当前状态 |
|---|---|
| 当前阶段 | Runtime Core 架构重构优先。根看板已压缩至 16 条目，状态标签统一为 契约已确立/已完成。T0 主线核心目标达成进入收尾 |
| 当前主线 | AM-0~AM-2(Frame Backbone) 全部 契约已确立；AM3 推荐优先（5项完成边界见任务树看板）；AM5 进行中（owner-local store + period/overflow proof 已落地）。~175 DOTS 文档已读完，规则体系建立 |
| 当前推荐领取 | `T1-RuntimeCore-AM3`：优先闭合 AM3 完成边界条件 3（EventBus callsite 收尾）和条件 4（parallel fan-in contract test）。领取前读 `02-主线任务树/README.md` AM3 完成边界、`RuntimeCore重构.md` AM3 节、`GASRuntimeFrameBackboneRebindContract.cs`、`UnityDOTS官方文档参考/README.md` |
| 下一技术任务 | AM3 完成边界闭合 → AM5 granted cleanup / store-driven lifecycle → AutoChess 验证 |

## 注意事项

### 架构方向
1. 当前方向以 T1 GAS ECS Runtime 为主；AutoChess 后置为全链路验收工具，不在 Runtime 架构未稳定前继续重构。
2. Runtime Core 遵守 DOTS Backbone First：SystemGroup / Frame Arena / Query / Lookup / Allocator / Dependency / Structural Playback / Debugger evidence 完成前不扩张 AM3/AM5。
3. OOP 只位于 Application Shell / Runtime Boundary，不得成为 GAS Runtime Core 中间层。

### 执行规范
4. 领取任务前必须读父节点链（根看板→父→本节点）的完整上下文。
5. 行动报告规范见 `Agent行动报告规范.md`（Tier 1/2/3 分层）；纯文档治理(T0)和 Editor(T3)任务不填 Tier 2/3。
6. 交还回写收口为 2 处：任务树看板/支线文档 + 当前窗口。迭代摘要是独立窗口视图，不在交还规则中重复要求。
7. 迭代摘要超过 500 行必须触发滚动清理到 `迭代记录/迭代摘要归档-YYYYMMDD.md`。

### DOTS 合规
8. Runtime Core 任务必须先读 `UnityDOTS官方文档参考/README.md`，按任务读取主题文档，行动报告引用 `ODF-*` 规则。
9. 性能结论必须拆分 core/physics/render 成本，不得混入 `avgTickMs`。
10. Unity 官方文档证据使用真实 PackageCache hash 路径；禁止引用不存在的 `@version` 路径。
11. 涉及 Physics/Entities Graphics 时必须显式覆盖 `ODF-15..18`，不相关时写明原因。

### 环境与验证
12. Unity Test Runner 受 LicensingClient IPC 阻塞时，不得判为代码编译失败；继续依赖 `dotnet build` 辅助验证。
13. 不修改 `Library/PackageCache`。
14. 目标态 Spec 只写未来实现约束，不写完成清单。历史方案原文不得直接作为实现任务。

