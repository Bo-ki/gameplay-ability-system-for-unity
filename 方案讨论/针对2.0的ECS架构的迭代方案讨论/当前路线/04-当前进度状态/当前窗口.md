# 当前窗口

| 项 | 当前状态 |
|---|---|
| 当前阶段 | Runtime Core 架构重构优先；AutoChessDemo 已完成简单目录迁移和文件级拆解，后续只作为 Runtime 重构后的全链路验收与性能反馈工具 |
| 当前主线 | Runtime Core 架构重构已完成 AM2 数据契约、AM3 simple instant 局部 proof、AM5 owner-local store 第一刀，以及 `GAS ECS Runtime - Runtime Core Frame Backbone - Schedule / Phase Contract`（任务ID：`T1-RuntimeCore-AM2B-A`）contract-first 落点；这些结果仍缺少 query / lookup / allocator / dependency budget、stream owner、deterministic merge、真实 structural playback gate 和 Debugger evidence gate |
| 当前推荐领取 | 下一轮继续 GAS Runtime 重构任务线，不回到 AutoChess 业务拆分；优先领取 `GAS ECS Runtime - Runtime Core Frame Backbone - Frame Arena 与 Query Budget`（任务ID：`T1-RuntimeCore-AM2B-B`）。行动前先读 `02-主线任务树/T1-GAS_ECS_Runtime/RuntimeCoreFrameBackbone.md`、`00-当前架构事实/核心问题诊断/ISSUE-009-RuntimeCoreFrameBackbone缺失.md` 和 `UnityDOTS官方文档参考/README.md` 中 `02/10/20/21` 相关主题 |
| 下一技术任务 | 当前下一任务是 `GAS ECS Runtime - Runtime Core Frame Backbone - Frame Arena 与 Query Budget`（任务ID：`T1-RuntimeCore-AM2B-B`）；后续顺序是 Stream Owner 与 Deterministic Merge -> Structural Playback Gate -> Debugger Evidence Gate -> AM3 / AM5 Rebind 与交还验证 -> 回到 AutoChess 验证 |

## 注意事项

1. 不修改 `Library/PackageCache`。
2. 不把已删除旧方案恢复为当前输入。
3. 目标态 Spec 只写未来实现必须遵守的设计，不写完成清单。
4. 领取三级任务前必须先读对应一级主线 README 和二级支线文档。
5. Goal 模式循环推进以任务树节点为输入；历史方案原文不能直接作为实现任务。
6. AutoChess 新增业务不再写入 `Assets/GAS/Runtime/Demo/AutoChess`，当前根目录已迁移到 `Assets/AutoChessDemo`；Runtime Core 重构完成前不继续推进 AutoChess 拆分路线。
7. AutoChessDemo 默认业务链路要精而完整，未来十万 / 百万实体压力测试通过 ScaleProfile 设计进入。
8. 每轮交还必须更新 `04-当前进度状态/迭代摘要.md`，并把重要发现反哺到 `00/01/02/03` 对应 owner。
9. 迭代摘要中的 P0 / P1 发现必须有 Finding ID、owner 判定和反哺状态；无法当轮完成的事项必须标记 `Deferred` 并指向任务入口。
10. 如果业务迭代证明目标态 Spec 有错，先修订 `01-目标态架构共识/`，再检查 `02-主线任务树/` 是否需要重切。
11. AutoChessDemo 默认精品业务链路的性能优秀线已经写入 `01-目标态架构共识/10-AutoChess无头验收Spec.md`；不要再用 `1.x ms` 作为可接受目标。
12. Agent 每轮领取任务并完成上下文搜寻后、正式执行前，必须按 `03-文档治理规范/07-Agent行动报告规范.md` 输出行动报告；该报告不等待批准。
13. 近期 Unity 验证先遇到项目已被 Unity Editor 打开，随后 Editor lock 解除后仍因 LicensingClient IPC 超时退出；不要把该阻塞判为代码编译失败。
14. 当前方向以 `T1 GAS ECS Runtime` 为主；AutoChess 的职责是后置全链路测试和性能问题定位，不是 Runtime 架构未稳定前的继续重构对象。
15. Runtime Core 任务必须先引用 `UnityDOTS官方文档参考/README.md`，再同时引用 `UnityDOTS官方文档参考/主题/01-Entities系统与World.md`、`UnityDOTS官方文档参考/主题/90-规则编号索引.md` 和对应 GAS 语义 Spec；不要只凭历史方案或自定义 ECS 概念实现。
16. Runtime Core 任务行动报告必须列出适用规则编号，例如 `SYS-01`、`JOB-01`、`SC-01`、`ECB-02`、`BUF-04`、`DBG-02`。
17. Runtime Core 任务还必须通过 `UnityDOTS官方文档参考/README.md` 确认阅读顺序，并按任务读取单主题文档，说明是否影响 archetype / chunk、system / job、dependency、allocator、Burst、Mathematics、Debugger 证据。
18. Runtime Core 任务还必须阅读 `UnityDOTS官方文档参考/主题/20-GASRuntimeCore-API选型基线.md`，并在行动报告中输出 API 选型表，说明 `NativeStream`、Chunk Component、Cleanup Component、EntityQuery bulk、system-associated entity、WeakObjectReference / UnityObjectRef 等候选 API 的采用或拒绝理由。
19. API 选型表必须包含重新选型触发条件，例如 x50/x1000/x10w、buffer spill、random lookup count、chunk skip 失效、per-entity ECB、deterministic ordering 风险。
20. AutoChess 后续验收报告必须补 API 健康指标；没有 global buffer pressure、NativeStream merge、chunk skip、lookup pressure、structural query batch 和 deterministic output policy 时，不得宣布“DOTS API 选型已验证”。
21. 领取 AM3 后续任务时，行动报告必须区分 Boundary request、Core frame command、parallel fan-in stream、structural mutation request 四类 command，并说明当前任务采用哪一类承载。
22. 领取 AM5 后续任务时，行动报告必须区分 OwnerLocalStore、GlobalIndexedStore、LifecycleCleanupStore、ChunkSkipIndex 四类 ActiveEffectStore，并说明当前任务继续扩展哪一层和为何不是其他三类。
23. Runtime Core 性能讨论必须报告 filtered/unfiltered query count、enableable wait、DynamicBuffer externalized、allocator owner、job overhead、Burst warmup 和 Profiler / Journaling 对照，不能只报告 `avgTickMs`。
24. Runtime Core 任务还必须阅读 `UnityDOTS官方文档参考/主题/12-官方案例模式.md`，并在行动报告中新增“官方案例对照”小节，说明采用 / 拒绝哪些 `CASE-*` 官方示例模式。
25. Runtime Core / Debugger / Luban / Demo 任务还必须先阅读 `UnityDOTS官方文档参考/README.md`，再阅读 `UnityDOTS官方文档参考/主题/21-官方文档覆盖与流程闭环.md`，并在行动报告中新增“官方文档覆盖检查”小节，说明相关官方主题、PackageCache 证据、`ODF-*` 规则、采用 / 拒绝 / 暂不相关理由、反哺 owner 和验收指标。
26. 官方文档证据必须使用真实 PackageCache hash 路径和 actual package version；如果 manifest / packages-lock / PackageCache 有差异，行动报告和交还必须记录差异，不得引用不存在的 `@version` 本地路径。
27. 性能、Debugger、Generated、AutoChess 任务必须记录 Editor / Player、Burst AOT、OptimizeFor、Safety Checks、CPU architecture、warning policy 和 warmup 口径。
28. Luban / Demo / Presentation 任务必须记录 Baking world / phase、EntityPrefabReference / PrefabLoadResult / IncludePrefab、managed resource clone / dispose、allocator aliasing 或明确不相关理由。
29. 涉及目标获取、命中确认、范围、碰撞、触发器或 Physics profile 的任务必须记录 Unity Physics 口径：`PhysicsSystemGroup` / FixedStep、`PhysicsWorldSingleton` / `SimulationSingleton`、query broadphase、collision / trigger event 有效窗口、physics counters 或 disabled reason。
30. 涉及真实表现、渲染资源、rendered profile、UI / VFX / SFX 资源接入的任务必须记录 Entities Graphics 口径：URP/HDRP / SRP 前提、single render world、`RenderMeshArray` / `MaterialMeshInfo`、material override、draw command / BRG / Profiler evidence、render counters 或 disabled reason。
31. 后续性能结论不得把 Physics fixed-step、Presentation marker 或 Entities Graphics render cost 混入 `coreTickMs` 后归因 GAS Runtime Core；必须拆分 core / physics / render 三类成本。
32. AM3 / AM5 后续扩张前必须先完成 Runtime Core Frame Backbone；否则不得把局部 proof stream、owner-local mirror 或 legacy-backed slot 继续描述为目标态主线。
33. Runtime Core Frame Backbone 已拆为 `AM2B-A -> AM2B-F`，不得再把整个 AM2B 作为单轮粗任务领取；`GAS ECS Runtime - Runtime Core Frame Backbone - Schedule / Phase Contract`（任务ID：`T1-RuntimeCore-AM2B-A`）已完成 contract-first，当前只领取 `GAS ECS Runtime - Runtime Core Frame Backbone - Frame Arena 与 Query Budget`（任务ID：`T1-RuntimeCore-AM2B-B`）。
34. Runtime Core Frame Backbone 的行动报告必须报告领取任务、所属分支、目标态参考、官方文档主题、当前事实参考、API 选型表、执行计划和验收指标；该报告不等待批准，但必须让用户能判断 Agent 是否掌握了 DOTS 官方约束。
35. Runtime Core Frame Backbone 行动前必须读取 `00-当前架构事实/核心问题诊断/ISSUE-009-RuntimeCoreFrameBackbone缺失.md`；这是当前实现层 P0 诊断，不只是目标态 Spec 建议。

