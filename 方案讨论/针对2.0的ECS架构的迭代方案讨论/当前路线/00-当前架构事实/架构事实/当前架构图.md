# 当前架构图

## 当前实际链路

```mermaid
flowchart TD
    Facade["AbilitySystemFacade / Binding"] --> Request["Request Entity\nAbility / GE / ASC Commands"]
    Request --> Ability["Ability Systems\nTryActivate / Commit / Lifecycle"]
    Ability --> GEApply["GE Apply / Effect Runtime\nSApplyGameplayEffectRequest / SEffectApply"]
    GEApply --> Attribute["Attribute / Magnitude / Execution"]
    GEApply --> Tags["Tag / Ongoing Requirements"]
    Attribute --> EventBus["CGameplayEventBus\nBGameplayEvent / BAttributeChangeEvent / BDamageEvent"]
    Tags --> EventBus
    EventBus --> Presentation["Presentation Outbox"]
    EventBus --> Replay["Debug Replay / Structured Log"]
    EventBus --> Reactions["AutoChess Reaction Systems\n历史上部分链路读全局事件"]
    Reactions --> Request
```

## 当前问题边界

1. `Request -> GE Apply -> Runtime GE Entity -> EventBus` 对 simple instant effect 过重。
2. `EventBus -> Reactions` 在高频链路中容易形成 observation stream 反向参与 simulation 的事实依赖。
3. `Presentation / Replay / Debug` 与 simulation tick 的计时边界需要拆开。
4. 当前 AM2 / AM3 / AM5 局部链路已出现 `EffectCommandSpecStream` 和 `ActiveEffectStore`，AM2B-B 已补 `FrameBudget` contract，AM2B-C 已补 `StreamOwner / DeterministicMerge` contract，AM2B-D 已补 `StructuralPlaybackGate` contract，AM2B-E 已补 `DebuggerEvidenceGate` contract；但它们仍挂在旧 group / helper / singleton buffer 形态上，真实 `FramePrepareSystemGroup / StreamOwner / DeterministicMerge / StructuralPlaybackGate / RuntimeEvidenceSampling` 尚未成为完整当前实际链路。

## DOTS 官方参考重诊断后的缺口

```mermaid
flowchart TD
    CurrentGroups["当前 GASCommandGroup / GASEffectGroup / GASAttributeGroup"] --> LocalStream["局部 EffectCommandSpecStream"]
    LocalStream --> LocalProof["AM3 simple instant proof"]
    CurrentGroups --> StoreMirror["AM5 owner-local store mirror"]
    LocalProof --> DebugCounters["RuntimeCore counters"]
    StoreMirror --> DebugCounters

    BudgetContract["已有: Frame budget contract\nquery / lookup / allocator / dependency budget"] -.-> LocalStream
    StreamContract["已有: Stream owner contract\ndeterministic merge policy"] -.-> LocalProof
    PlaybackContract["已有: StructuralPlaybackGate contract\nunique gate / route policy"] -.-> StoreMirror
    MissingPrep["缺失: GasRuntimeFramePrepareSystemGroup\n真实 frame prepare owner"] -.-> BudgetContract
    MissingMerge["未闭合: NativeStream / owner-local 真实迁移\nmerge cost runtime evidence"] -.-> StreamContract
    MissingPlayback["未闭合: 真实 structural playback group 搬迁\n旧结构变化点迁移"] -.-> PlaybackContract
    EvidenceContract["已有: DebuggerEvidenceGate contract\nframe backbone counters / cost split"] -.-> DebugCounters
    MissingEvidence["未闭合: runtime evidence sampling\nProfiler / Journaling 真实采样"] -.-> EvidenceContract
```

当前事实判断：`EffectCommandSpecStream`、`ActiveEffectStore`、Debugger baseline、AM2B-B frame budget contract、AM2B-C stream owner contract、AM2B-D structural playback gate contract 和 AM2B-E Debugger evidence gate contract 只能证明局部迁移与 contract-first 口径已经开始，不能证明 Runtime Core 已具备 DOTS scale-ready 执行骨架。该缺口维护在 `核心问题诊断/ISSUE-009-RuntimeCoreFrameBackbone缺失.md`。

## 当前目标迁移方向

```mermaid
flowchart LR
    FramePrep["FramePrepare\nQuery / Lookup / Allocator / Dependency"]
    Command["EffectCommand"] --> Spec["InstantSpec / ActiveMutation"]
    Spec --> Delta["AttributeDelta / TagDelta"]
    Delta --> TypedFacts["Typed Simulation Facts"]
    TypedFacts --> CoreReactions["Simulation Reactions"]
    TypedFacts --> Observation["Observation Projection"]
    Observation --> Presentation["Presentation / Replay / Debugger"]
    FramePrep --> Command
    FramePrep --> Spec
    FramePrep --> Delta
    TypedFacts --> StructuralPlayback["StructuralPlayback Gate"]
```
