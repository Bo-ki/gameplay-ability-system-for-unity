# 当前未闭合点

1. Runtime 优先循环仍是当前优先入口：先推进 GAS Runtime 架构重构，再回到 AutoChess 做全链路验证和性能问题归因。
2. Runtime Core Debugger 已有 AM-1 baseline，AM5 又补了 ActiveEffectStore slot pressure / state distribution / legacy-backed / externalized owner baseline；但 ECB playback 仍是主要工具路径覆盖，后续若 profile 需要逐系统结构变化预算、cleanup / compact / chunk skip 证据，需要继续补专门 instrumentation；见 `核心问题诊断/ISSUE-003-RuntimeCoreDebugger证据不足.md`。
3. Effect Command / Spec Stream / Attribute Delta 的 AM2 数据契约已落地，AM3 已完成 direct simple instant evaluation 主链落点，并已迁入 ability activation simple single-target producer、ability cost self producer、Timeline ApplyEffects single-target simple instant producer 与 Timeline ApplyEffects multi-target simple instant command fan-out；AM3 command writer 已让多目标 simple instant fan-out 一次解析 stream entity / current frame / buffer 后批量 append，收缩了 `EffectCommandSpecStream.AppendCommand` 的 per-command query 放大；typed fact attribute observation bridge 已能把 `AttributeBaseValueChanged` fact 投影回旧 `BAttributeChangeEvent`，Presentation / Replay 已能直接消费 attribute typed fact 并通过 `SourceFactSequence` 跳过同源 legacy duplicate；Cue-on-Apply projection 已能从 simple instant spec 生成 `BTypedSimulationFact(CueRequested)`，Presentation / Replay 已能直接消费 cue typed fact，并对旧 `BCueRequest` / `BGameplayEvent(CueRequested)` 做同源去重；generic typed gameplay fact 也已能绕过旧 `BGameplayEvent` 直接投影 Presentation / Replay，并对同源 legacy gameplay event 去重；damage typed fact 已能绕过旧 `BDamageEvent` 直接投影 Presentation / Replay，并对同源 legacy damage event 去重；AM5 period / overflow simple instant child GE 已能写入 `BEffectCommand` 并复用 AM3 主链；但 cooldown、复杂 cost fallback、duration / stack / tag requirements / granted state ApplyEffects fallback、复杂 period / overflow child fallback、剩余 producer、业务 reaction typed fact consumer 和真实 parallel fan-in / deterministic merge 仍未闭合；见 `核心问题诊断/ISSUE-001-GE生命周期管线过重.md`。
4. Active Effect Store 已完成 owner-local slot 第一刀，并把旧 duration runtime GE entity 生命周期镜像到 ASC slot；Debugger slot pressure baseline 已补齐，period due simple instant 派生 EffectCommand 与 owner-local period cursor refresh 已完成 proof 小闭环，但 granted tag / ability cleanup、store-driven lifecycle、slot compact、chunk skip 和复杂 child GE 派生仍未闭合；见 `核心问题诊断/ISSUE-001-GE生命周期管线过重.md` 和 `核心问题诊断/ISSUE-004-结构变化边界脆弱.md`。
5. Runtime Core Frame Backbone 已完成 `GAS ECS Runtime - Runtime Core Frame Backbone - Schedule / Phase Contract`（任务ID：`T1-RuntimeCore-AM2B-A`）的 contract-first phase 顺序与结构变化权限表，完成 `GAS ECS Runtime - Runtime Core Frame Backbone - Frame Arena 与 Query Budget`（任务ID：`T1-RuntimeCore-AM2B-B`）的 query / lookup / allocator / dependency budget contract 与 Debugger 预算字段，完成 `GAS ECS Runtime - Runtime Core Frame Backbone - Stream Owner 与 Deterministic Merge`（任务ID：`T1-RuntimeCore-AM2B-C`）的 stream owner / deterministic merge contract，完成 `GAS ECS Runtime - Runtime Core Frame Backbone - Structural Playback Gate`（任务ID：`T1-RuntimeCore-AM2B-D`）的 unique gate contract / type anchor / route policy，完成 `GAS ECS Runtime - Runtime Core Frame Backbone - Debugger Evidence Gate`（任务ID：`T1-RuntimeCore-AM2B-E`）的 frame backbone counters、Profiler / Journaling 对照口径、cost split、overhead budget 和 disable policy，并完成 `GAS ECS Runtime - Runtime Core Frame Backbone - AM3 / AM5 Rebind 与交还验证`（任务ID：`T1-RuntimeCore-AM2B-F`）的 rebind handoff contract；真实 SystemGroup 搬迁、AM3 / AM5 功能迁移复用、真实 Profiler / Journaling 采样和 AutoChess profile 尚未闭合。当前下一步仍围绕 GAS Runtime 重构，在已迁入 activation / cost / Timeline ApplyEffects single-target 与 multi-target simple instant producer、attribute fact 观察桥、attribute / cue / generic gameplay / damage typed fact native Presentation / Replay consumer 后，继续收缩剩余 producer、业务 reaction typed fact consumer、parallel fan-in / deterministic merge 和 AM5 lifecycle 缺口；见 `核心问题诊断/ISSUE-009-RuntimeCoreFrameBackbone缺失.md`。
6. AutoChess x50 规模下性能曲线仍说明当前 pipeline 存在流程级问题；见 `核心问题诊断/ISSUE-001-GE生命周期管线过重.md` 与 `核心问题诊断/ISSUE-002-Observation与RuntimeCore热路径耦合.md`。
7. Observation / Presentation / Replay / Debugger 需要继续和 core simulation tick 分离；attribute / cue / generic gameplay / damage typed fact 已开始绕过旧 EventBus 直接投影，但业务 reaction、Replay sampling 和 projection tick 独立统计仍未闭合；见 `核心问题诊断/ISSUE-002-Observation与RuntimeCore热路径耦合.md`。
8. AutoChess Demo 已完成基础目录迁移到 `Assets/AutoChessDemo` 并暂停继续拆分；Runtime Core 重构形成可验收链路后，再回到 AutoChess 做验证和后续分层重构；见 `核心问题诊断/ISSUE-006-AutoChessDemo边界混入RuntimeCore.md`。
9. AutoChessDemo 需要单独的 Luban 配置方案；默认业务链路应精而完整，并预留 x10w / x100w ScaleProfile；见 `核心问题诊断/ISSUE-005-Generated链路未反哺RuntimeCore.md`。
10. 目标态 Spec 仍需按 Unity Entities 1.4.6、Unity Physics 1.4.6、Entities Graphics 1.4.19、Burst、Collections、Mathematics 官方机制、使用规则、API 选型、官方案例和官方文档参考体系继续校准；Runtime Core 任务必须明确 SystemGroup、ISystem/job、ECB playback、DynamicBuffer、Enableable、Blob/Baker、Query filter、Physics input boundary、Entities Graphics presentation boundary、Debugger 证据落点，以及适用的 `SYS/JOB/QRY/SC/ECB/BUF/EN/FIL/DEF/NAT/DBG/SEL/CASE/ODF/PHY/GFX` 规则编号；见 `核心问题诊断/ISSUE-008-目标态Spec尚未充分UnityEntities机制化.md`。
11. AM3 继续推进前，EffectCommand 承载仍需完成 API 选型复核：singleton DynamicBuffer、per-owner DynamicBuffer、`NativeStream`、request entity、ECB `AppendToBuffer`、EntityQuery bulk 的采用或拒绝理由必须写入行动报告。
12. AM5 后续小闭环仍需继续完成 API 选型复核：ASC slot buffer 第一刀已采用；stable active effect entity、enableable marker、enum state、Cleanup Component、Chunk Component、LinkedEntityGroup 的采用或拒绝理由仍必须写入行动报告。
13. AutoChess validation summary 仍需补齐 API 选型健康指标，包括 global buffer pressure、NativeStream merge、chunk skip、lookup pressure、structural query batch、deterministic ordering 和 proof-only API marker。
14. 行动报告规范已经要求 API 选型表，但后续代码任务还需要在实际交还中证明该表不是形式化填写，而是能驱动实现取舍和重新选型触发。
15. Runtime Core 任务已把 Query / Lookup / Allocator / Dependency 的预算口径写入 AM2B-B frame budget contract；后续还需继续证明这些预算在真实 SystemGroup / Profiler / Journaling 中成立，并补齐 filtered runtime evidence、enableable wait、DynamicBuffer externalized、job overhead 和 Burst warmup 证据。
16. AM3 后续实现前必须按 command 四分层重新定位当前任务：Boundary request、Core frame command、parallel fan-in stream、structural mutation request；不能继续把 singleton stream owner 当作所有 command 的目标态。
17. AM5 后续领取必须继续按 ActiveEffectStore 四分层定位：OwnerLocalStore 第一刀已采用，GlobalIndexedStore、LifecycleCleanupStore、ChunkSkipIndex 仍需按 scale gate 和访问模式逐项证明。
18. AM3 / AM5 / T4 / T2 后续行动报告还必须补“官方案例对照”：例如 hot path 遍历是否符合 `CASE-01/04/05`，结构变化是否符合 `CASE-08`，buffer 是否符合 `CASE-07`，bake / resource 是否符合 `CASE-10/11`。
19. DOTS 相关后续行动报告还必须补“官方文档覆盖检查”：先读取 `UnityDOTS官方文档参考/README.md`，再从 `UnityDOTS官方文档参考/主题/21-官方文档覆盖与流程闭环.md` 选择相关主题，列出 PackageCache 证据、`ODF-*` 规则、采用 / 拒绝 / 暂不相关理由、反哺 owner 和验收指标。
20. Unity Physics / Entities Graphics 已纳入官方参考体系，但代码侧尚未验证：Physics 只能作为 target / hit / query / event 输入边界，Entities Graphics 只能作为 Presentation / Boundary 渲染桥；后续任务必须输出 `ODF-15..18` 相关或不相关理由、disabled reason 和 core / physics / render 成本拆分。
21. **[P0] 全系统主线 foreach + EntityManager — `PRF-05` `PRF-02` `JOB-01`**：见 `核心问题诊断/ISSUE-010-代码执行范式未切换到DOTS.md`。当前所有热路径系统（`SAbilityCommit`、`STryActivateAbility`、`SEffectApply`、`SEffectTick`、`SApplyGameplayEffectRequest`、`SEffectCommandSpecStreamPhases`、`SPresentationOutboxProjection`、`SDebugReplayLogProjection`）全部使用 `_query.ToEntityArray(Allocator.Temp)` + `foreach` 主线程遍历，零 `IJobEntity`/`IJobChunk` 使用。x50 AutoChess `avgTickMs=13.77ms` 主要耗时来源。见 `代码DOTS合规审查报告.md` 缺陷 A。
22. **[P0] 热路径直接 EntityManager 结构变化 — `PRF-02` `SC-01` `ECB-01`**：`AbilityRuntimeActions.cs:77,107` 直接 `entityManager.AddComponentData`；`GameplayEffectEntityFactory.cs:79-92,121` 每个 GE 实例化 4 次结构变化；`EffectRuntimeUtility.cs:1549-1550` 在 foreach 内 `ecb.AddBuffer` + 立即 `PlaybackAndReset` 创建 sync point。见 `代码DOTS合规审查报告.md` 缺陷 B。
23. **[P0] 热路径 helper 中大量临时 EntityQuery — `PRF-09` `PRF-33`**：见 `核心问题诊断/ISSUE-011-临时EntityQuery泛滥与API承载选型错误.md`。历史上 5 个文件各自实现 `ResolveCurrentFrame` 并创建临时 `em.CreateEntityQuery`，现已收口为 `GASRuntimeFrameContext`，且 current-frame fallback query 已删除；但 frame owner 仍由 `GASManager.EntityGlobalTimer` static known owner 提供，真实 `SystemState` / frame owner 未闭合，`EffectCommandSpecStream.AppendCommand` 单条 API 仍会 `EnsureSingleton` / `TryGetSingleton`，多目标 simple instant command fan-out 只是通过 `CommandWriter` 收缩为一次解析后批量 append，`GasRuntimeDebugger.CountEntitiesWith<T>` 每帧仍创建 6+ 个临时 query。该问题仍未闭合，后续还需迁移到统一 frame owner、SystemState query 预创建、EventBus / Debugger / Presentation query 路径。见 `代码DOTS合规审查报告.md` 缺陷 C。
24. **[P0] ECB.Playback 散布在 helper 方法中 — `ECB-01` `PRF-09`**：`EffectRuntimeUtility.cs` 中存在 15+ 对 `(EntityManager, ...)` / `(EntityManager, ref EntityCommandBuffer, ...)` 重载，便捷重载每次创建临时 ECB → 立即 Playback → Dispose，调用者无法控制 ECB 合并。见 `代码DOTS合规审查报告.md` 缺陷 D。
25. **[P1] SystemGroup 层次未迁移到目标态 — `SYS-02`**：当前 6 个旧 SystemGroup（`GASCommandGroup`、`GASResetDirtyGroup`、`GASTagGroup`、`GASEffectGroup`、`GASAttributeGroup`、`GASAbilityGroup`、`GASCueGroup`）与目标态 8 个新 SystemGroup 完全不匹配。`GasStructuralPlaybackSystemGroup` 已声明但未作为唯一结构变化屏障。见 `代码DOTS合规审查报告.md` 缺陷 E。
26. **[P1] EventBus + Stream 双重事件路径 — `BUF-02` `SEL-02`**：见 `核心问题诊断/ISSUE-011-临时EntityQuery泛滥与API承载选型错误.md`。`EffectRuntimeUtility` 每个事件同时写入旧 EventBus 路径和新 stream 路径；`STypedSimulationFactEventBridge` 同时写 `BTypedSimulationFact` 和旧 `BGameplayEvent`；`GASManager.EntityEventBus` 是全局 singleton 承载 5 种 Buffer。见 `代码DOTS合规审查报告.md` 缺陷 F。
27. **[P1] Period/Overflow 创建 Request Entity 而非写入 Command Stream — `SEL-01`**：`EffectRuntimeUtility.CreateDerivedApplyRequest` 在大多数 period GE 有 SetByCaller 值时创建完整 request entity；`SAbilityCommit.ExecuteActivationEffects` 遍历 `BAbilityEffectOnActivate` 逐个创建 request entity。见 `代码DOTS合规审查报告.md` 缺陷 G。
28. **[P1] ThreadStatic 批量状态阻止 Burst Job 化 — `PRF-12` `BUR-01`**：见 `核心问题诊断/ISSUE-011-临时EntityQuery泛滥与API承载选型错误.md`。`EventBusHelper.cs` 使用 7 个 `[ThreadStatic]` 字段管理批量状态，`[ThreadStatic]` 在 Burst 编译的 job 中不工作。类文档声称"在 Burst 并行 Job 中使用 AsParallelWriter 安全入队"与 ThreadStatic 机制矛盾。见 `代码DOTS合规审查报告.md` 缺陷 H。
29. **[P1] EffectRuntimeUtility 承载过多职责 — `FSM-04`**：2126 行 static class 包含 GE 生命周期管理、Stack 合并、Granted tag/ability 管理、Modifier 管理、Cue 请求（6 种泛型重载）、事件入队（3 种事件类型）、Derived apply request 创建、Static definition blob 查询、ECB 管理等全部耦合。见 `代码DOTS合规审查报告.md` 缺陷 I。
30. **[已局部收口] ResolveCurrentFrame 重复实现 5 次 — `PRF-32`**：见 `核心问题诊断/ISSUE-011-临时EntityQuery泛滥与API承载选型错误.md`。`GasRuntimeDebugger.cs`、`EventBusHelper.cs`、`SPresentationOutboxProjection.cs`、`SDebugReplayLogProjection.cs`、`EffectCommandSpecStream.cs` 的重复实现已收口到 `GASRuntimeFrameContext`，且 `GASRuntimeFrameContext` fallback 临时 query 已删除；不再作为独立未闭合点继续追踪。剩余 frame owner static read、`EffectCommandSpecStream.TryGetSingleton` 与其它 helper query 缺口归并到第 23 点和 `代码DOTS合规审查报告.md` 缺陷 C。
31. **[P2] managed 数组分配在每帧调用路径 — `NAT-05`**：`GASRuntimeFrameBudgetPlanner.CreateCurrent()` 每次调用分配 managed 数组；`GasRuntimeDebugger.CreateSnapshot` 分配 managed 数组。见 `代码DOTS合规审查报告.md` 缺陷 K。
32. **[P2] IComponentData 上挂 NativeArray — `PRF-34`**：见 `核心问题诊断/ISSUE-011-临时EntityQuery泛滥与API承载选型错误.md`。`CCueOnApply.cues`、`CCueOnAdd.cues` 等 Cue 组件持有 `NativeArray<Entity>` 字段，阻止这些组件被 `IJobChunk`/`IJobEntity` 处理。见 `代码DOTS合规审查报告.md` 缺陷 L。补充审查确认 `CCueOnActivate`、`CCueOnDeactivate`、`CCueOnRemove`、`CCueOnTick` 同样违规（合共6个文件）。
33. **[P0] MCCue 为 managed class IComponentData — `SYS-01` `BUR-01`**：`MCCue.cs:8` 声明为 `class : IComponentData` 且字段 `GameplayCueBase cue` 是托管 abstract class 引用。这是全部 5 个 Cue System（`SCueDestroy`/`SCueEnd`/`SCueRequestBridge`/`SCueStart`/`SCueTick`）的 `OnUpdate` 上 `[BurstCompile]` 被注释关闭的根因，所有 Cue 逻辑锁死在主线程。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 M。
34. **[P0] SCueRequestBridge 零 ECB + 直接 EM 结构变化 — `ECB-01` `SC-01`**：全文无 ECB 使用。Line 107 `em.SetComponentEnabled<ECKillCue>`（直接结构变化）、Line 109 `em.DestroyEntity`（直接销毁）。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 N。
35. **[P0] EffectMagnitudeResolver.PlaybackAndReset — ECB 碎片化播放 — `ECB-03`**：Line 542-551 在 helper 方法内部 `ecb.Playback(em)` + `ecb.Dispose()` + 立即重建 ECB。被 `GetOrCreateResolvedModifiers`、`GetOrCreateAttributeCaptures`、`EnsureResolverBuffers` 三个路径调用，每次都是独立 sync point。同模式也出现在 `SExecutionCalculationOutputModifier.cs:198-217`。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 O。
36. **[P1] SAscCommandRequest / SAscDestroyFinalize / SAscDestroyRequest 三个 ASC 管理系统零 ECB — `SC-01` `ECB-01`**：三个文件合计 621 行、22+ 处直接 EM 操作、零 ECB 使用。`SAscDestroyRequest.DestroyOwnedAbilities` 在主线程嵌套调用 `AbilityRuntimeActions.RequestAbilityCancel()` 并在销毁 entity 前手动释放托管资源。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 P。
37. **[P1] TagHelper 四个静态托管 Dictionary 无法 Burst — `BUR-01`**：`TagHelper.cs:9-12` 使用 `Dictionary<int, GameplayTag>`、`Dictionary<int, string>` 等 4 个静态托管字典做 Tag 运行时查找。多处使用 `new List<int>()` 做临时收集。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 Q。
38. **[P1] GameplayCueUnit Hybrid ECS 遗留 class 包装 Entity — `SYS-01`**：`GameplayCueUnit.cs` 是托管 class 包装 Entity + 7 个托管字段（`Type`、`XParam`、6 个 `int[]`）+ 零 ECB。创建/销毁/启停全部直接 EntityManager。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 R。
39. **[P1] SAbilityTimelineAction 360 行零 ECB + 直接 Entity 创建 — `SC-01`**：Line 267 `em.CreateEntity()` 在 hot path 中直接创建 entity；Line 273-279 批量 `em.AddComponent`；全文零 ECB。String 比较做 dispatch（`clip.ActionType == "ApplyEffects"`）。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 S。
40. **[P1] CueHelper 运行时反射创建 Cue 实例 — `SYS-01`**：Line 32/52 使用 `Activator.CreateInstance` 运行时反射创建 `GameplayCueBase` 和 `XParam` 实例。三个静态 `Dictionary<string, Type>` 管理托管反射类型映射。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 T。
41. **[P1] Definition 层 6 个 DTO struct 全部含托管数组 — `BAKE-01`**：`GASDefinitionTable`、`GASGeneratedDefinitionBakingPlan`、`GASGeneratedDefinitionBakeContract`、`GASGeneratedDefinitionRuntimeIntegrationPlan`、`GameplayTagDefinitionSummary` 的值类型中含托管数组字段，无法作为 BlobAsset 内容或 ECS Component Data。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 U。
42. **[P1] BlobAsset 通过静态 Dictionary 而非 BlobAssetStore 管理生命周期 — `BLOB-02`**：`GameplayEffectConfigRegistry.StaticDefinitionBlobByCode` 静态 Dictionary 缓存 BlobAsset，绕过 Baker 的 `BlobAssetStore`。`DisposeCachedStaticDefinitionBlobs()` 手动释放。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 V。
43. **[P1] 代码库中零 Baker<T> 实现 — `CASE-39/40/41`**：全量搜索 `Assets/GAS/Runtime` 未发现任何 `Baker<T>` 类。自定义 BakeContract/BakePipeline/BakingPlan 是规划层，与 Unity Baker 的集成点缺失。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 W。
44. **[P2] 5 个 Cue System 的 OnUpdate [BurstCompile] 被注释关闭 — `BUR-01`**：`SCueDestroy`/`SCueEnd`/`SCueStart`/`SCueTick` 的 `OnUpdate` 上 `[BurstCompile]` 被 `//` 注释。根因是 `MCCue` managed class，修复后一并解决。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 X。
45. **[P2] 4 个空 struct Tag Component（PRF-03）**：`ECCuePlayable`/`ECCuePlaying`/`ECKillCue`/`CGASRunningTag` 为零字段 IComponentData enableable tag。功能合理但严格来说每个 tag type 使 archetype 排列数翻倍。见 `代码DOTS合规审查报告-补充篇.md` 缺陷 Y。
