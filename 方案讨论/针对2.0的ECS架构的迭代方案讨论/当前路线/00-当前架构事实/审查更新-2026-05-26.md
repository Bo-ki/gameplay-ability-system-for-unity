# 审查更新：2026-05-26

## 审查范围

重新审查 `Assets/GAS/Runtime/` 下 ~222 个 C# 文件，覆盖全部热路径系统、流处理、事件桥接、存储和调试器。对照 UnityDOTS 官方文档参考 P0/P1/P2 规则体系（SYS/PRF/JOB/QRY/SC/ECB）。

## 关键发现

### 已修复（合规）

| 原缺陷 | 修复内容 |
|--------|----------|
| **H: ThreadStatic** | `EventBusHelper.cs` 不再使用 `[ThreadStatic]`。`GameplayEventBusWriter` 改为显式 `struct` + `Create`/`Dispose` 生命周期。完全修复。 |
| **J: ResolveCurrentFrame 重复** | 5 处重复实现已收口到 `GASRuntimeFrameContext`，fallback 临时 query 已删除。已修复。 |

### 已改善（部分进展）

| 原缺陷 | 改善内容 |
|--------|----------|
| **A: foreach + ToEntityArray** | `SPresentationOutboxProjection`、`SDebugReplayLogProjection` 已改用索引 for 循环遍历 DynamicBuffer（非 ToEntityArray）。但 `SAbilityCommit`、`SEffectApply`、`SEffectTick`、`SApplyGameplayEffectRequest` 仍使用 `_query.ToEntityArray(Allocator.Temp)` + `foreach`。 |
| **D: ECB 碎片化** | `SAbilityCommit` 已改为单 ECB + 单次 Playback。但 `EffectRuntimeUtility` 仍有 8+ 处 `PlaybackAndReset`，`SEffectApply` 仍有中间回放。 |
| **E: SystemGroup 层次** | `GASSystemScheduleContract.cs`(447行) 已定义 8-phase 管线契约，`GASGroups.cs` 已定义 8 个 SystemGroup + `GasStructuralPlaybackSystemGroup`。但旧 SystemGroup 名称与目标态仍未完全匹配。 |

### 仍存在（未修复）

| 原缺陷 | 当前状态 |
|--------|----------|
| **A: foreach + ToEntityArray** | 4 个核心热路径系统仍全主线程 foreach，零 IJobChunk 使用 |
| **B: 直接 EM 结构变化** | `SApplyGameplayEffectRequest`、`AbilityRuntimeActions`、`EffectRuntimeUtility`、`GameplayEffectRequestWriter` 仍有 40+ 处直接 EM 操作 |
| **C: 临时 EntityQuery** | `GasRuntimeDebugger`(2389行) 每帧仍创建多个 query；`TryGetSingleton` 模式在 `EffectCommandSpecStream.AppendCommand` 中仍存在 |
| **D: ECB 碎片化** | `EffectRuntimeUtility.PlaybackAndReset` 在 8+ 处创建独立 sync point |
| **M: MCCue 托管类** | `MCCue`(class : IComponentData) 未修复，5 个 Cue System 的 `[BurstCompile]` 仍被注释 |
| **N: SCueRequestBridge 零 ECB** | 未修复，仍直接 EM 操作 |
| **O: EffectMagnitudeResolver ECB 碎片化** | `PlaybackAndReset` 模式仍存在 |

### 新增发现

| 发现 | 严重度 | 详情 |
|------|--------|------|
| **IJobEntity 使用极低** | P0 | 222 个文件中仅 SAbilityTick 和 SAttributeRecalculate 使用了 IJobEntity。热路径 Effect 系统零使用。 |
| **SEffectCommandSpecStreamPhases 设计良好** | 正面 | 619 行，6 个 partial ISystem，基于游标的 for 循环遍历 DynamicBuffer，无 ToEntityArray，无 ECB 碎片化。这是新代码的正面范例。 |
| **GASSystemScheduleContract(447行) 定义完整但未落地** | P1 | 8-phase 管线契约已完整定义（FramePrepare → CommandIngest → SpecEvaluation → ActiveEffectLifecycle → DeltaApply → TypedFactProjection → StructuralPlayback → ObservationProjection），但系统尚未按此契约搬迁。 |
| **GasRuntimeDebugger(2389行) 过于庞大** | P1 | 单体调试器，包含 query 创建、计数器收集、ECB 记录、诊断记录。建议按关注点拆分。 |
| **Typed Simulation Fact 桥接模式优秀** | 正面 | `BTypedSimulationFact` → `STypedSimulationFactProjection` → `STypedSimulationFactEventBridge` → 旧 EventBus / Presentation 的双出口设计，清晰分离了模拟事实与表现投影。 |

## 合规统计

| 严重度 | 活跃缺陷 | 明细 |
|--------|----------|------|
| P0 | 7 | A/D 已改善，B/C/M/N/O 仍存在 |
| P1 | 12 | E/F 已改善，G/I/P/Q/R/S/T/U/V/W 仍存在（H 已清除） |
| P2 | 5 | K/L/X/Y/Z 仍存在（J 已清除） |

**合计**: 24 活跃缺陷。本轮审查清除 2 个（H: ThreadStatic, J: ResolveCurrentFrame 重复），改善 4 个（A/D/E/F），无新增正式缺陷编号。

## 建议的后续行动

1. **P0 优先**：将 `SEffectCommandSpecStreamPhases` 的游标模式推广到 `SEffectApply`/`SEffectTick`/`SAbilityCommit`
2. **P0 优先**：在 `SEffectApply` 中实验 IJobEntity 替代 foreach——这是最小侵入的 job 化路径
3. **P1**: 将 `GASSystemScheduleContract` 中定义的 phase 名称落地到 SystemGroup 实际成员
4. **P1**: 拆分 `GasRuntimeDebugger` 为 query/counter/evidence 三个独立 debugger
5. **P2**: 修复 `MCCue` managed class → unmanaged struct，解除 5 个 Cue System 的 Burst 封锁
