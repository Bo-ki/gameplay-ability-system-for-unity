# 2026-06-08 ISSUE 驱动新一轮 GAS 架构瘦身计划

> Owner：`02-主线任务树` | 状态：新一轮可执行计划 | 输入事实：`../00-当前架构事实/架构重划分审查事实/11-ISSUE驱动Runtime架构复审事实.md` | 目标裁决：`../01-目标态架构共识/16-纯ECS内核与边界重划分/16-07-ISSUE驱动架构瘦身目标裁决Spec.md`

本计划只定义下一轮领取顺序、切片边界和验收链。实现事实写回 `00`，验证流水写回 `04`，目标态修正写回 `01`。

## 领取结论

下一轮不继续围绕旧 `QueryBuilder` / 旧 generated lifecycle 文件做小修；当前代码风险已经转为 capability、carrier、catalog lifetime 和 evidence owner 深度不足。

优先领取组合：

1. R1 + R6：Boundary Shell / AutoChess Adapter capability 收权。
2. R3 + R5：singleton carrier / fact sequence owner 与 SourceGenerator pure glue / catalog lifetime 对账。
3. R4：StructuralCommit、Debugger、Profiler / Journaling evidence 闭环。
4. R7：等 R3/R4 至少交还一轮 carrier / evidence 后再推进 ActiveEffectStore store-only lifecycle。
5. R8：等 R1-R7 至少闭合一轮后进入 scale gate。

## 本轮执行回写：GAS Concept Trace 与 Sequence Owner 收口

状态：代码已执行并完成本轮窄验证；进入后续 R1/R3/R4/R5/R6 领取前复核。

已完成切片：

1. G0/G1：新增 Runtime 机器可读 GAS 概念覆盖与 trace preview。落点为 `GASRuntimeConceptCoverageSnapshot`、`GASRuntimeTracePreview`、`GASRuntimeDefinitionResolver.CreateConceptCoverageSnapshot`、`TryBuildRuntimeTracePreview`。
2. G2：新增 `GASRuntimeSequenceOwnerContract` 与 `GASRuntimeSequenceAllocator`；Runtime / AutoChess 内部 GE stream context / command / spec / delta / fact sequence producer 不再直接分配 `Next*Sequence`。
3. G5：AutoChess validation evidence 增加 `gasConceptCoverageMask`、`gasConceptMissingMask`、`runtimeTraceStageMask`、`runtimeTraceMissingStageMask`、seed / modifier / fact / cue 计数；trace 样本使用真实业务 `AbilityPlayerExecute`。

本轮验证结果：

1. `dotnet build com.exhard.exgas.runtime.csproj --no-restore -m:1 -p:UseSharedCompilation=false --nologo --verbosity:minimal` 通过，0 error；仍有既有 `MSB3277` 引用版本 warning。
2. `dotnet build com.exhard.exgas.autochessdemo.csproj --no-restore -m:1 -p:UseSharedCompilation=false --nologo --verbosity:minimal` 通过，0 error；仍有既有 `MSB3277` 引用版本 warning。
3. Unity batchmode AutoChess x50 完成，日志 `Temp/AutoChessBattleValidation.ConceptTrace.log`，摘要 `TestResults/AutoChess/Headless/AutoChessHeadlessValidationSummary.txt`：`passed=True`、`completed=True`、`winner=Player`、`blockingDebugErrors=0`、`journalingCaptured=True`、`summaryHash=0x6CC8210F`、`factsHash=0x75720A4D`。
4. GAS concept trace 证据：`gasConceptCoverageMask=0x1FF`、`gasConceptMissingMask=0x1E00`、`runtimeTraceAbilityCode=9103`、`runtimeTraceStageMask=0x2CD`、`runtimeTraceMissingStageMask=0x430`、`runtimeTraceSeeds=1`、`runtimeTraceModifiers=0`、`runtimeTraceFacts=1`、`runtimeTraceCues=1`。
5. Sanity 扫描确认直接 `Allocate(ref stream.Next*)` 只保留在 `GASRuntimeSequenceAllocator` 内部；Runtime / AutoChess producer 通过 allocator 分配 sequence。

## 本轮执行回写：Debugger Data-Oriented Scorecard 第一刀

状态：已执行 Runtime-owned scorecard 与 Runtime Debugger export 落点，并完成 Runtime / AutoChessDemo 窄构建；进入下一刀 Debugger 单体拆分前复核。

已完成切片：

1. 新增 `GasRuntimeDataOrientedScorecard` 与 `GasRuntimeDataOrientedScorecardInput`，把 units、measured ticks、commands/facts per tick、us per unit / command / fact、query / lookup / random lookup / sync budget、observation pollution、official profiler state 收敛到 GAS Runtime Debugger 模块。
2. 新增 `GasRuntimeDiagnosticsPassMode`、`GasRuntimeDiagnosticsCostDomain`、`GasRuntimeDiagnosticsEvidenceTier`，先把 performance counter、diagnostic sample、official capture、derived export 的分类语言从 AutoChess validation 前移到 Runtime。
3. `AutoChessHeadlessLogicBudgetResult` 不再直接从散落字段拼 data-oriented budget；先创建 Runtime scorecard，再基于 scorecard 执行 strict pure logic budget 判定。
4. `AutoChessBattleValidationReport.CreateHeadlessLogicBudgetSummary` 输出 `scorecardSource=GasRuntimeDataOrientedScorecard`，后续 validation report 可以确认预算判断来源不是 Demo 自定义统计口径。
5. `GasRuntimeDebugger` 新增 `ExportDataOrientedScorecardToText(...)` 与 snapshot + scorecard overload，输出 `runtimeDataOrientedScorecard` 机器可读行。
6. `AutoChessRuntimeRunner` 的 runtime log 与 headless report 已调用 Runtime Debugger export 输出 scorecard，避免数据导向指标只停留在 AutoChess report 拼接层。
7. 第二刀已新增 `GasRuntimeDerivedExportSink`，把 scorecard 派生文本导出从 `GasRuntimeDebugger.cs` 单体拆出；`GasRuntimeDataOrientedScorecard` 增加 `MetricFamilyMask` 与 `DominantRisk`，AutoChess budget summary 与 Runtime export 均输出 `metricFamilyMask` / `dominantRisk`。

本轮验证结果：

1. `dotnet build com.exhard.exgas.runtime.csproj --no-restore -m:1 -p:UseSharedCompilation=false --nologo --verbosity:minimal` 通过，0 error；仍有既有 `MSB3277` 引用版本 warning。
2. `dotnet build com.exhard.exgas.autochessdemo.csproj --no-restore -m:1 -p:UseSharedCompilation=false --nologo --verbosity:minimal` 通过，0 error；仍有既有 `MSB3277` 引用版本 warning。
3. 本地未跟踪 Unity `.csproj` 为显式 `Compile Include` 列表，窄构建前已临时纳入 `Assets/GAS/Runtime/Debugger/GasRuntimeDataOrientedScorecard.cs` 与 `Assets/GAS/Runtime/Debugger/GasRuntimeDerivedExportSink.cs`；该调整不作为架构提交内容，Unity asmdef 仍是实际归属边界。

仍未完成切片：

1. `GasRuntimeDebugger.cs` 仍是单体 proof implementation，当前只物理拆出了 `DerivedExportSink` 起点；`DiagnosticsConfigOwner`、`RuntimeMetricSink`、`DiagnosticMaterializationPass`、`OfficialCorrelationPass` 仍未拆出。
2. `GASRuntimeDiagnosticEventBuffer` 仍承载稀疏大事件 row，metric family buffer / snapshot 尚未真正替换。
3. `GasRuntimeDataOrientedScorecard` 已有 Runtime export、metric family mask 和 dominant risk，但尚未进入独立 metric family snapshot / buffer，也没有替代 `GASRuntimeDiagnosticEventBuffer` 大行结构。
4. 本轮只做窄构建；尚未重跑 AutoChess headless x50，也不能宣称 strict budget、official evidence 或 x100 / x1000 scale gate 完成。

仍未完成切片：

1. R1/R6：`GASRuntimeShell` 与 `AutoChessGasRuntimeAccess` capability 物理拆分尚未完成。
2. R3：fact / delta / active mutation 的物理 carrier 仍未从 singleton stream 存储拆出，本轮只完成 sequence owner API 收口。
3. R5：`DefinitionCatalogLifetime`、`StaticLookups` dispose owner、manifest/report/file/schedule 对账仍未完成。
4. R4：Journaling 已能在本轮 x50 中捕获，但 Profiler 仍是 `profiler disabled; Entities profiler modules collect no data`；StructuralCommit official evidence matrix 尚未完整新增。
5. R8：本轮只跑 x50；未运行 x100 / x1000 scale profile，不能写成规模门完成。

## 切片 A：R1/R6 Capability Access 收权

目标：让业务层只看见 intent、opaque handle、snapshot 和 evidence；`World`、`EntityManager`、runtime singleton、raw `Entity`、live read model 和 job drain 只留在单 capability implementation 内。

执行范围：

1. `Assets/GAS/Runtime/General/GASRuntimeShell.cs`
2. `Assets/GAS/Runtime/AbilitySystem/ASCCommandGateway.cs`
3. `Assets/GAS/Runtime/AbilitySystem/ASCReadModel.cs`
4. `Assets/GAS/Runtime/AbilitySystem/AbilitySystemBinding.cs`
5. `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasRuntimeAccess.cs`
6. `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasRuntimeHost.cs`
7. `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasCatalogSession.cs`
8. `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasObservationGateway.cs`
9. `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasRuntimeTicker.cs`
10. `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasBattleEntityLifecycle.cs`

实施步骤：

1. 生成 `Capability Access Matrix` before 表：覆盖 `RuntimeSession`、`CommandPort`、`SnapshotReadModel`、`DiagnosticsSink`、`RunnerSync`、`DefinitionCatalogLifetime`、`PresentationBridge`。
2. 把 `AutoChessGasRuntimeAccess` 当前 15 个方法逐项拆分为 capability owner；聚合 wrapper 可以保留 composition，但每个 public / adapter-facing 方法必须只归一个 capability。
3. 先退出最污染性能结论的能力混用：`TryDrainRunnerJobs()` 只能归 `RunnerSync`；diagnostics reset / snapshot 只能归 `DiagnosticsSink`；catalog install / release 只能归 `DefinitionCatalogLifetime`。
4. `TryCaptureASCReadModel` 和 AutoChess business result 禁止 live read；若仍需临时保留，标 `proof-only compatibility` 并写下一步退出任务。
5. 更新 AutoChess validation evidence，让 report 输出 wrapper capability count、ECS handle proxy count、manual sync count、performance pass risk count、battle hash affecting count。

交还门：

| 项 | 要求 |
|---|---|
| public seam | Flow / Session / Report / Presentation 看不到 `World`、`EntityManager`、runtime singleton、raw `Entity`、live buffer |
| internal resolver | 每个 resolver 只服务一个 capability owner |
| timing | runner drain、diagnostics export、snapshot read、catalog lifetime 分开计时 |
| evidence | AutoChess report 输出 capability cost table 和 direct EM 分类 |
| 不能算完成 | 只把 Shell 调用集中到一个 wrapper、只新增注释、只输出 contract summary |

## 切片 B：R3 Fact Sequence / Carrier Owner 拆分

目标：把 command/spec/delta/fact/mutation 从 `GEEffectCommandStreamComponent` 的多语义 singleton carrier 中拆出，至少先拆 fact / delta sequence owner 和 execution output carrier。

执行范围：

1. `Assets/GAS/Runtime/Effect/Component/Dynamic/GEEffectCommandSpecStream.cs`
2. `Assets/GAS/Runtime/System/Effect/GEEffectCommandSpecStreamPhases.cs`
3. `Assets/GAS/Runtime/System/Effect/GEExecutionCalculationSystem.cs`
4. `Assets/GAS/Runtime/System/Effect/GEEffectInstantSystems.cs`
5. `Assets/AutoChessDemo/Battle/Ecs/AutoChessExecuteDamageCalculationSystem.cs`
6. `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStreamOwnerContract.cs`

实施步骤：

1. 建立 data-kind 表：command、set-by-caller、spec、delta、active mutation、gameplay fact、debug telemetry、presentation outbox。
2. 先把 `NextFactSequence` 从 stream proof owner 迁到 fact lane owner，或显式定义 `GameplayFactSequence` owner；所有 fact producer 统一通过该 owner 分配 sequence。当前回写：已通过 `GASRuntimeSequenceAllocator.AllocateFactSequence` 完成 API 收口，底层存储仍在 stream。
3. 对 `NextDeltaSequence` 做同样分类：Attribute delta 属于 target owner-local delta lane，不能长期由 stream component 统一分配。当前回写：已通过 `GASRuntimeSequenceAllocator.AllocateDeltaSequence` 完成 API 收口，底层存储仍在 stream。
4. AutoChess execution output 只能写 Runtime Core 认可的 delta / fact lane；不得创建 runtime GE entity 作为补丁路径。当前回写：AutoChess execution output 已使用 Runtime allocator，未创建第二套 runtime GE entity。
5. 每个保留在 stream 的 buffer 都写 proof-only mask、capacity、pressure、spill、reselect trigger 和退出任务。

交还门：

| 项 | 要求 |
|---|---|
| carrier matrix | current / target carrier、allocator owner、merge key、capacity / spill、proof-only 标记完整 |
| deterministic merge | 影响 battle hash 的 fan-in 有 target sort key、producer order 和 merge cost |
| sequence owner | fact / delta / command / spec sequence owner 不再混淆 |
| evidence | x50 或 diagnostic pass 输出 carrier pressure、owner group、spill / externalized ratio |
| 不能算完成 | `OwnerLocalGameplayFactBuffer` 已存在、stream counter 可读、NativeStream API 出现但无 merge policy |

## 切片 C：R5 DefinitionCatalogLifetime 与 Generated Gate

目标：把 SourceGenerator 收敛为 definition / Blob / lookup / pure glue / validation graph，并补 catalog install / release / dispose owner。

执行范围：

1. `Assets/GAS/Generated/CodeGen/Runtime/DefinitionCatalog.gen.cs`
2. `Assets/GAS/Generated/CodeGen/Runtime/StaticLookups.gen.cs`
3. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeDefinitionGlue.gen.cs`
4. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeAbilityActivation.gen.cs`
5. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeEffectInstant.gen.cs`
6. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeActiveEffect.gen.cs`
7. `Assets/GAS/Editor/CodeGen/Phases/GasGlueCodeGenPhases.cs`
8. `Tools/CodeGen`
9. `Tools/GasCodeGenCli`

实施步骤：

1. 输出 `Generation Artifact Responsibility Matrix`：manifest category、目标 artifact kind、consumer、first owner、allowed API、forbidden API、release-ready disposition。
2. 对账 `GasCodeGen.manifest.json` 与 `GasCodeGenValidationReport.md`；manifest 中 runtime-visible artifact 的 `ArtifactCategory` 不得缺失或与 report 不一致。
3. 为 `StaticLookups` 的 `NativeArray` / Blob dispose 补 `DefinitionCatalogLifetime` install / release 证据。
4. 增加 negative gate：generated `ISystem`、query、ECB、NativeContainer owner、random write lookup、managed config hot path、system registration helper 回流必须 fail-fast。
5. 保持 `RuntimeAbilityActivation.gen.cs`、`RuntimeEffectInstant.gen.cs`、`RuntimeActiveEffect.gen.cs` 为 marker / pure glue；任何 lifecycle 逻辑回流必须阻断。

交还门：

| 项 | 要求 |
|---|---|
| generated report | boundary / lifecycle / structural / ownership / random lookup / managed config hit 分项可读 |
| manifest 对账 | report 与 manifest 对同一 artifact 分类一致 |
| catalog lifetime | schema hash、catalog handle、install / release result、dispose owner 可验证 |
| negative gate | stale generated lifecycle 文件、缺失 type、type mismatch、assembly unavailable 有负例 |
| 不能算完成 | `GeneratedRuntimeBoundaryHits=0`、`.gen.cs` 是 marker、pure resolver 存在 |

## 切片 D：R4 StructuralCommit / Debugger Evidence 闭环

目标：让结构变化、Debugger、Profiler / Journaling、AutoChess evidence 能互相对账，避免字符串日志替代性能证据。

执行范围：

1. `Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs`
2. `Assets/GAS/Runtime/Event/GasStructuredLogExport.cs`
3. `Assets/GAS/Runtime/System/Event/ReplayLogSystem.cs`
4. `Assets/GAS/Runtime/System/Event/PresentationOutboxProjectionSystem.cs`
5. `Assets/GAS/Runtime/System/Cue/CueManagedLifecycleSystem.cs`
6. `Assets/AutoChessDemo/Battle/Validation/AutoChessBattleValidationReport.cs`
7. `Tools/Diagnostics/Analyze-AutoChessProfile.ps1`

实施步骤：

1. 建立 evidence matrix：Core、StructuralCommit、Boundary、Diagnostics、Runner、Presentation。
2. StructuralCommit 输出 recorded playback、required playback、missing evidence、Journaling captured / disabled reason、Profiler structural module state。
3. Debugger TopN 输出按 system、component、carrier、lookup、structural playback、observation materialization 归因。
4. Derived export 标明来源：structured snapshot、runtime diagnostics snapshot、replay sink、presentation outbox、official diff。
5. Cue managed boundary 的 `ToEntityArray`、`Time.time`、managed callback 独立计入 PresentationBoundary。

交还门：

| 项 | 要求 |
|---|---|
| official evidence | Profiler / Journaling state 或 disabled reason 明确 |
| pass split | performance / diagnostic / official diff pass 分离 |
| derived export | Mermaid、中文日志、assertion text 不作为机器依据源 |
| observation cost | `ToEntityArray` 和 string/file export 不混入 Core |
| 不能算完成 | Debugger 能输出文本、x50 功能跑通、Mermaid 图可读 |

## 切片 E：R7/R8 后置条件

R7 进入条件：

1. R3 至少交还 fact / delta / active mutation carrier owner 一轮。
2. R4 至少交还 StructuralCommit / Debugger evidence matrix 一轮。
3. ActiveEffectStore 的 slot capacity、source attribute snapshot lane、requirement query、cleanup / finalize owner 有当前事实卡。

R8 进入条件：

1. R1/R6 不再让 public业务层持有 ECS handle。
2. R3/R5/R4 已给出 carrier、generated gate、catalog lifetime、official evidence。
3. x50 / x100 / x1000 profile 能输出 completed、winner、summaryHash、factsHash、core / boundary / diagnostics / runner timing、official evidence state、disabled reason。
4. x10w / x100w synthetic profile 只能在同一 Runtime Core contracts 下执行，不得走第二套 simplified runner。

## 切片 F：数据导向性能瘦身

状态：新增优先切片；在继续扩大 scale gate 前先领取。

目标：把性能优化指标从“总 ms 结果”改为“DOTS 数据形态诊断”。x50 strict pure logic budget 不达标时，先优化 CoreSimulation / CoreRuntime 数据布局、owner-local range、lookup pressure 和 carrier pressure，再进入 x100 / x1000 扩展。

执行范围：

1. `Assets/GAS/Runtime/System/Effect/GASActiveEffectRuntime.cs`
2. `Assets/GAS/Runtime/System/Effect/GEActiveEffectLifecycleSystems.cs`
3. `Assets/GAS/Runtime/System/Effect/GEEffectInstantSystems.cs`
4. `Assets/GAS/Runtime/System/Effect/GEEffectCommandCatalogNormalizeSystem.cs`
5. `Assets/GAS/Runtime/System/Event/GameplayFactBoundaryProjectionSystem.cs`
6. `Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs`
7. `Assets/AutoChessDemo/Battle/Validation/AutoChessBattleValidationRun.cs`
8. `Assets/AutoChessDemo/Battle/Validation/AutoChessBattleValidationReport.cs`

领取前复核：

1. 读取 `AutoChessDemoHeadlessLogicBudget` 的 data-oriented scorecard：units、commands/facts per measured tick、us per unit / command / fact、ActiveMutation / PendingAttribute / OwnerLocalFact owner group、max range、random lookup、query / lookup / sync budget。
2. 对照 Journaling TopN：`GetBufferRW`、`GetComponentDataRW`、`GASActiveEffectPreTickSystem`、`OwnerLocalGameplayFactBuffer`、`GEEffectCommandStreamComponent`。
3. 判断每个热点属于数据布局问题、lookup/random access 问题、DynamicBuffer carrier 问题、structural playback 问题、sync/materialization 问题还是 Burst / managed boundary 问题。

执行步骤：

1. 为 ActiveEffect pre-tick / mutation / instant spec / fact projection 建立 Data-Oriented Hotspot Matrix：数据域、owner、buffer、访问模式、读写集合、chunk locality、lookup count、carrier、capacity、clear phase。
2. 优先处理 `GetBufferRW` 高频 owner：把跨 owner buffer lookup 改成 chunk-local buffer accessor、owner range 或 precomputed frame-local record。
3. 优先处理 `GetComponentDataRW` 高频 owner：减少可写 component random access，改为 owner chunk apply 或 frame-local marker request。
4. 对 `OwnerLocalGameplayFactBuffer` 与 `GEEffectCommandStreamComponent` 做 carrier pressure 审查：明确哪些仍是 migration input，哪些必须退出 singleton / proof carrier。
5. 对 Boundary fact export 做数据量归一化：fact per tick、flush count、owner group、max range、projection cost 必须能解释 BoundaryOwner avg。

交还门：

| 项 | 要求 |
|---|---|
| data-oriented matrix | 每个热点都有数据域、owner、访问模式和改造路径 |
| normalized metrics | 输出 us per unit / command / fact 的前后对比 |
| lookup pressure | TopN 中高频 `GetBufferRW` / `GetComponentDataRW` 有下降或明确 remaining owner |
| carrier pressure | singleton / owner-local / boundary carrier 的 peak、capacity、spill、migration input 解释清楚 |
| strict budget | x50 `headlessLogicBudgetPassed` 不通过时必须交还下一轮 owner；通过后再扩 x100/x1000 |
| 不能算完成 | 只调 ms 阈值、只减少日志、只优化字符串导出、只跑单次 x50 |

## 验证命令

优先顺序：

1. `.\Tools\Diagnostics\Verify-GAS-RuntimeCoreBoundary.ps1`
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore -m:1 -p:UseSharedCompilation=false --nologo --verbosity:minimal`
3. `dotnet build com.exhard.exgas.autochessdemo.csproj --no-restore -m:1 -p:UseSharedCompilation=false --nologo --verbosity:minimal`
4. `.\Tools\CodeGen\Generate-GAS-SourceGen.bat`
5. AutoChess batchmode：

```powershell
E:\Unity\UnityEditor\6000.3.14f1\Editor\Unity.exe -batchmode -quit -projectPath E:\Unity\UnityProjects\_Git\gameplay-ability-system-for-unity -executeMethod GAS.AutoChessDemo.Editor.AutoChessDemoBatchRunner.RunAutoChessBattleOnceAndExit -logFile E:\Unity\UnityProjects\_Git\gameplay-ability-system-for-unity\Temp\AutoChessBattleValidation.log
```

不能写成通过的情况：

1. Unity / AIBridge 返回 `statusConfirmed=false` 或超时。
2. 只跑 dotnet build，没有 AutoChess headless。
3. 只跑 x50，没有 x100 / x1000 或未写 scale gate blocked reason。
4. Profiler / Journaling disabled 但没有 disabled reason。
5. Validation report 为 0 hit，但没有负例和 manifest/report/file/schedule 对账。

## 本轮交还格式

每个切片完成后必须提交：

1. `00` 事实卡：代码证据、官方规则、不能推出的结论。
2. `02` 任务交还：执行范围、before / after matrix、验证命令、未跑项。
3. `04` 进度摘要：日志路径、测试状态、blocked reason。
4. git commit：只 stage 本切片相关代码和文档，不包含无关脏改。
