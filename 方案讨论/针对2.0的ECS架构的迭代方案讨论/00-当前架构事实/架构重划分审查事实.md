# 架构重划分审查事实

> 最近复核：2026-06-07 | Owner：`00-当前架构事实` | 范围：`Assets/GAS/Runtime`、`Assets/GAS/Generated/CodeGen/Runtime`、`Assets/AutoChessDemo`

本文件记录本轮“纯 ECS Core + OOP Shell / Thin Adapter + Debugger + Luban SourceGenerator”重划分审查后的当前事实。它只说明现实代码已经做到哪里、仍然暴露哪些架构问题，以及这些问题为什么必须继续拆分；目标态设计见 `../01-目标态架构共识/16-纯ECS内核与边界重划分Spec.md`，可执行任务见 `../02-主线任务树/GAS架构重划分主线任务.md`。

## 当前结论

当前 Runtime 已经不再是完整的旧 OOP pipeline，也不能继续用“全局 EventBus + request entity lifecycle + 大量主线程 query”这类旧口径描述。当前真实形态是：

```text
Layer 1 Application Shell / Demo
  -> Layer 2 Runtime Boundary command/read/diagnostics adapter
  -> Layer 3 GAS Runtime Core 5 段 FixedStep physical group
  -> Layer 4 generated definition catalog / runtime glue
```

这说明架构方向已经从旧式 OOP 中间层迁出，但还没有达到“纯血 ECS GamePlay Core”。当前最深的问题不是有没有 `ISystem`，而是 **Core 的数据接口仍不够深**：singleton stream、stream migration fallback、`GASRuntimeShell` assembly/internal capability 面、Boundary 侧 capture read model、SourceAttribute snapshot lane 缺口和 AutoChess bridge adapter 内部 direct ECS 读写仍把实现细节泄露到边界层和生成链。

## 整体架构审查结论（codedb 复核）

本轮使用 codedb 复核 `GASRuntimeShell`、5 段主链、generated runtime、ActiveEffectStore / SpecStream、Debugger / structured log、Definition catalog / SourceGenerator 和 AutoChess adapter 后，当前架构可以更准确地划分为“已有 DOTS backbone 的迁移期 ECS Runtime”，而不是“纯 ECS GAS 内核完成态”。

| 审查域 | 当前事实 | 架构判断 |
|---|---|---|
| Runtime physical backbone | 5 段 `FixedStepSimulationSystemGroup` 子 group 已存在，generated runtime 也由 `GASSystemScheduleContract` 注册进主链 | 物理执行域方向正确；但 generated type-name registry 仍会在 `Type.GetType(...) == null` 时静默跳过，调度 owner 还没有 fail-fast 证据 |
| OOP Shell / Runtime Boundary | `GASRuntimeShell` 同时解析 World / `EntityManager`、创建 command port、捕获 read model、解析 singleton、执行 job drain | 当前 Shell 是多能力 ECS 句柄 facade，不是目标态 OOP Shell；它把 command、snapshot、diagnostics、bootstrap、definition 和 dependency drain 混在同一 implementation 面 |
| Core data interface | command/spec/delta/fact/active mutation 仍大量依赖 `GEEffectCommandStreamComponent` singleton DynamicBuffer carrier；active mutation 和 pending AttributeDelta 已推进到 ASC chunk-local apply | 旧 random lookup 风险已明显收窄，但 scale-ready fan-in / store 仍未闭合；真正缺口是 owner-local range、NativeStream deterministic merge、capacity/spill/reselect evidence |
| Debugger / 日志 | `GasRuntimeDebugger`、structured log、OfficialToolDiff 和 AutoChess validation export 已存在，且 report projection 已有 Boundary report key | Debugger 方向应从“日志输出”升级为 evidence source；contract counter、runtime counter、official capture、validation evidence 和 derived export 必须分层消费 |
| Luban / SourceGenerator | `GASDefinitionCatalogBlob`、generated lookup、pure resolver 已进入 Runtime；generated lifecycle / query / ECB / lookup owner 仍以 classified `MigrationProofOnly` 存在 | SourceGenerator 的目标价值不是继续扩生成 runtime system，而是收权到 immutable catalog、static lookup、pure glue、validation graph；runtime lifecycle owner 必须回到手写 Core lane |
| AutoChessDemo | AutoChess 已成为业务验收 Shell，report projector 已消费 `SourceReportKey` / `TargetReportKey`；driver handle、registry/live read 和 observation gateway 仍触达 live ECS | Demo 分层比旧 bridge 健康，但 Thin Adapter 未完成；应继续拆成 runtime host、catalog session、entity lifecycle、snapshot / diagnostics gateway、fixed tick runner 的 owner evidence |

因此，当前最应优先治理的是 **接口深度和 owner locality**，而不是继续新增 OOP facade 或继续把迁移 proof 写成目标态。一个模块是否“深”应按以下事实检查：它是否隐藏了 query、allocator、dependency、capacity、merge、structural playback 和 diagnostics owner；如果调用方仍必须知道 `EntityManager`、singleton entity、raw `Entity`、buffer handle 或 generated lifecycle hit，它就还只是迁移期浅接口。

## 代码证据矩阵

| 事实 | 代码证据 | 判定 |
|---|---|---|
| 当前物理主链已是 5 段 FixedStep group | `Assets/GAS/Runtime/System/SystemGroup/GASGroups.cs:11`、`:22`、`:34`、`:58`、`:144` | 正向事实；这是 DOTS physical backbone，不是最终完成证明 |
| 结构变化 gate 已存在 | `GASGroups.cs:66-137` 定义 `BeginGASStructuralCommitECBSystem` / `EndGASStructuralCommitECBSystem` | 正向事实；仍需 Journaling / Profiler 证明来源和 phase |
| handwritten + generated runtime 统一注册 | `Assets/GAS/Runtime/System/SystemGroup/GASSystemScheduleContract.cs:313-321`、`:374-392` | generated runtime 是 Runtime Core 审查对象，不能当外部产物豁免；当前 generated type 解析失败会静默跳过，主链注册还缺 fail-fast 证据 |
| OOP Shell 已集中成 Boundary 写入口 | `Assets/GAS/Runtime/AbilitySystem/AbilitySystemBinding.cs:11-70`、`ASCCommandGateway.cs:6-169` | 正向事实；但 `EntityManager` 仍在 Boundary writer / binding implementation 内直接写 live ECS buffer |
| Boundary API 仍保留 raw ECS identity 内部解析面 | `AbilitySystemBinding.cs:59-62`、`ASCHandle.cs:17-25`、`ASCCommandGateway.cs:286`、`:331-392`、`ASCReadModel.cs:64-65` | 外部 target 已主要收缩为 `ASCHandle`，旧 runtime-adapter raw entity 方法已退场；但 Boundary / adapter assembly 内仍可通过 `TryResolveRuntimeEntityForBoundary`、`ASCHandle.RuntimeEntity` / `MatchesRuntimeEntity` 和 `ASCCommandPort.RuntimeEntity` 解析 raw `Entity` |
| ReadModel 已改为捕获式 snapshot | `Assets/GAS/Runtime/AbilitySystem/ASCReadModel.cs:16-159`、`AbilitySystemBinding.cs:49-55` | 本轮正向瘦身；getter 不再持有 live buffer，但 capture 仍由 Shell/Boundary 构造阶段直接读取 ECS，尚未形成统一 BoundaryProjection / ring buffer |
| generated active mutation 已进入 ASC chunk-local applicator | `Assets/GAS/Generated/CodeGen/Runtime/RuntimeActiveEffect.gen.cs:142-186`、`:362-700` | 当前最大 Core store 风险已从 active mutation random lookup 迁出；apply 已是 owner chunk 内直接访问 store/slot/mutation/tag/attribute/modifier/ability buffer，剩余风险是 singleton command carrier、gather serial command scan 和 SourceAttribute snapshot lane |
| pending AttributeDelta owner-local apply 已进入 ASC chunk-local applicator | `Assets/GAS/Runtime/System/Attribute/GASAttributeModifierDeltaApplySystem.cs:37-195` | owner-local pending delta 不再物化 owner entity list，`pendingAttributeEstimatedRandomLookups=0` / `pendingAttributeMigrationCarriers=0` 已由 AutoChess x50 复测证明；旧 stream migration fallback 已删除，剩余风险是 singleton stream fact carrier、generated instant delta record 和 fan-in 证明 |
| stream owner contract 已把当前 carrier 标为迁移承载 | `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStreamOwnerContract.cs:311-388`、`:418-440` | 当前 command/spec/delta/fact/mutation 都仍以 `SingletonDynamicBuffer` 为 current carrier，目标 carrier 才是 `PerThreadNativeStream` / `OwnerLocalDynamicBuffer`，且 scale status 是 `MigrationCarrier` |
| Debugger 已有 counter 和 official diff 工具 | `Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs:824-896`、`:1091-1207`、`:1947-2094`、`GasRuntimeOfficialToolDiff.cs` | 正向事实；需要把性能热点从字符串日志升级为 structured evidence model |
| AutoChess runtime ticker 手动推进 5 段 group | `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasRuntimeTicker.cs:9-37`、`:44-49`、`:53-113` | Demo runner 集中化是正向事实；它仍不是 Core timing 的纯证据 |
| AutoChess adapter 通过 `GASRuntimeShell` 仍直接读写 runtime singleton / entity | `AutoChessBattleRuntime.cs:24-66`、`AutoChessGasRuntimeHost.cs:7-60`、`AutoChessGasObservationGateway.cs:11-257`、`AutoChessGasBattleEntityLifecycle.cs:8-251`、`AutoChessGasCatalogSession.cs:6-16`、`AutoChessGasRuntimeTicker.cs:9-78`、`AutoChessGasBattleReportFactProjector.cs:7-77`、`AutoChessGasCoreContracts.cs:7-133`、`GasStructuredLogExport.cs:419-473` | 当前边界风险；adapter 已拆成 flow runtime / session facade / host / observation / lifecycle / catalog / ticker / report projector / contracts owner。report projector 已消费 Boundary report key，不再用 `Matches(Entity)`；剩余风险是这些 owner 仍通过 internal Shell capability 取得 World / `EntityManager` / singleton / job drain，driver handle 仍暴露 adapter raw entity，registry/live read 仍保存 `ASCHandle` |
| AutoChess 活动入口是 Flow runtime + Session facade + 多 owner | `AutoChessBattleRuntime.cs:24-66` 调用 host / observation / ticker；`AutoChessGasCoreBridge.cs:7-87` 组合 host / observation / lifecycle / report projection；`AutoChessGasRuntimeTicker.cs:12`、`:56` 调用 host，`:68` 调用 observation gateway；`AutoChessGasBattleReportFactProjector.cs:7-77` 消费 report projection | 当前活动事实；不得写成单一聚合口或不存在的万能访问器 |
| `GASRuntimeShell` 已变成多能力 ECS 句柄口 | `Assets/GAS/Runtime/General/GASRuntimeShell.cs:10-271`、`Assets/GAS/Editor/GameplayAbilitySystem/GASWatcher.cs:248-286`、`:396-436`、`:493-551`、`AutoChessGasRuntimeTicker.cs:54-58`、`:69-143` | 当前 Shell 的 public 面主要保留 command/read model，World / `EntityManager` / singleton / job drain 多数已 internal 化；但 assembly 内仍是同一个 facade 同时承载 bootstrap、command port、read model、diagnostics singleton 和 job drain，需要继续按 command / snapshot / diagnostics / bootstrap capability 拆分 |

## 整体项目审查：DOTS API 健康 owner 分类

2026-06-07 对 `Assets/GAS/Runtime`、`Assets/GAS/Generated/CodeGen/Runtime` 和 `Assets/AutoChessDemo` 做整体静态截面后，旧“所有 `ToEntityArray` 都是 hot path”以及旧“`GlobalTimer` fallback query 已删除”的口径都需要修正。当前更准确的事实是：同步等待已明显收窄，但 API 健康证据必须按 owner 分类，否则会把 Core、Boundary、Debugger 和 Demo adapter 成本混在一起。

| API / 模式 | 当前命中 | owner 分类 | 判定 |
|---|---|---|---|
| `state.Dependency.Complete()` / `.Run()` | `Assets/GAS/Runtime` 与 generated runtime 当前 0 命中 | Runtime Core | 正向事实；不能据此宣称 Core 已达 DOTS 优秀水平 |
| `CompleteAllTrackedJobs()` | `GASRuntimeShell.cs:141-147`，由 `AutoChessGasRuntimeHost.TryCompleteRuntimeJobs()` 和 `AutoChessGasRuntimeTicker.CompleteRuntimeJobsTimed()` 间接调用 | runner sync / debugger measurement | 只属于 AutoChess group timing wrapper 和 Shell internal drain capability，不是 CoreSimulation hot path 优化证明 |
| `GASRuntimeShell.TryResolveRuntimeEntityManager` | `GASRuntimeShell.cs:30-44`、`:66-115`、`:243-271` | Boundary facade / internal Shell capability risk | 外部 EM 面已收窄，但 assembly 内 adapter / diagnostics 仍可拿到 `EntityManager`；R1 需要继续收缩为 opaque handle + command/snapshot/diagnostics capability |
| `GASRuntimeShell` singleton / diagnostics capability | `GASRuntimeShell.cs:88-153`、`GASWatcher.cs:396-551`、`AutoChessGasObservationGateway.cs:11-257`、`AutoChessGasRuntimeTicker.cs:44-78` | Editor / Demo diagnostics / runner sync | 这些调用集中化是正向事实，但 capability 未分级；读 watcher、observation reset、debugger timing 和 job drain 必须从 Core/Battle 性能证据中拆出 |
| `EntityManager.CreateEntityQuery + CalculateEntityCount + GetSingletonEntity` | `GASGlobalTimerSystem.cs:125-129` | Runtime frame singleton cache-miss fallback | 已优先读取 registered owner，不再 materialize entity array；仍应迁到 stronger owner evidence 或标记低频 cache miss 并计数 |
| `EntityManager.CreateEntityQuery + CalculateEntityCount + GetSingletonEntity` | `ActiveEffectStore.cs:1374-1382` | ActiveEffectStore global index fallback | 可能是 singleton index owner cache miss 路径；需要 owner 注册、capacity / cache miss counter 和 hot path 触发证明 |
| `ToEntityArray` | `CueManagedLifecycleSystem.cs:39` | Boundary managed presentation | 承载 managed cue lifecycle，可接受但必须单独计入 Boundary / Presentation cost |
| `ToEntityArray` / `StringBuilder` | `GasRuntimeDebugger.cs:888-889`、`:2260`、`:2458` | Debugger / Observation | observation-only；必须与 CoreSimulation timing 拆开 |
| `MergeParallelCommandFanIn` managed `List` | `GEEffectCommandSpecStream.cs:627-662` | singleton fan-in proof carrier | deterministic ordering 是正向事实，但 managed list + `EntityManager` 写回不是 scale-ready `NativeStream` fan-in |
| handwritten ECB create / destroy | `ASCCommandBufferResolveSystem.cs:52-89`、`:538`、`:785`；`ASCDestroyFinalizeSystem.cs:62`、`:305` | StructuralCommit route | 方向正确；仍需 Journaling / Profiler / Debugger route-level required vs recorded 证据 |
| generated ECB create / destroy | `RuntimeActiveEffect.gen.cs:108-146`、`:1021-1043`、`:1138`、`:2268` | generated runtime lifecycle `MigrationProofOnly` | generated runtime 仍拥有 lifecycle / structural owner；目标态应迁回手写 Core owner 或被 validation gate 阻断 |
| Definition / authoring managed collection | `Assets/GAS/Runtime/Definition/*`、`GameplayEffectConfigRegistry.cs` | Definition / prototype / validation | 不按 Core hot path 缺陷处理；进入 Runtime Core hot path 才算违约 |
| AutoChess direct ECS access via RuntimeShell | `AutoChessGasRuntimeHost.cs:7-60`、`AutoChessGasObservationGateway.cs:11-257`、`AutoChessGasBattleEntityLifecycle.cs:8-249`、`AutoChessGasCatalogSession.cs:6-16`、`AutoChessGasRuntimeTicker.cs:9-78` | Demo adapter host / lifecycle / observation / runner sync | 已集中到 `Integration/GasCore` facade 之后的 owner 文件，但仍通过 internal Shell capability 取得 live ECS 句柄；继续按 R6 owner 表退出 raw ECS handle 和 direct EM capability |

当前 `ToEntityArray()` 运行调用点为 4 处：`GasRuntimeDebugger.cs:889/2260/2458`、`CueManagedLifecycleSystem.cs:39`。Debugger / Cue 仍按 Debugger 和 managed boundary 分类；pending AttributeDelta owner-local apply 与 GlobalTimer fallback 已不再归入 query materialization。`GASGlobalTimerSystem.cs:125-129` 和 `ActiveEffectStore.cs:1374-1382` 当前都是 `CreateEntityQuery + CalculateEntityCount + GetSingletonEntity` singleton fallback。AutoChess catalog builder 的旧 `ToEntityArray` 事实已过期，当前 `AutoChessBattleDefinitionCatalogBuilder.ResolveCatalogEntity()` 通过缓存 `_catalogEntity`，缓存失效时直接 `CreateEntity(ComponentType.ReadWrite<GASDefinitionCatalogComponent>())`。

### 拆分后静态复核补充（2026-06-07）

R0-R8 叶子任务拆分后，重新对 `Assets/GAS/Runtime`、`Assets/GAS/Generated/CodeGen/Runtime` 和 `Assets/AutoChessDemo` 执行 `rg` 静态复核。本截面形成时 `codedb_status` 曾不可用，报 `failed to load project: 系统找不到指定的文件。 (os error 2)`，因此该截面的 API 分类采用文本扫描和定点读取作为证据；本轮收口复测 `codedb_status` 已恢复 ready，427 files / scan ready，后续代码截面可优先恢复 codedb + `rg` 交叉复核。

1. `state.Dependency.Complete()`、`.Run()`、`SystemAPI.Query` 在 Runtime / generated runtime / AutoChess 扫描中仍无运行命中；这只是同步等待和主线程 query 语法的正向截面，不能证明 DOTS 优秀水平。
2. `GASRuntimeShell.` 命中必须区分可执行调用和验证报告字符串：`Assets/AutoChessDemo/Battle/Validation/AutoChessBattleValidationReport.cs:254` 只是 `snapshotOwner=GASRuntimeShell.TryCaptureASCReadModel` 文本证据字段，不是新的 Shell 调用点；真正的 Shell capability 调用仍集中在 `AbilitySystemBinding` 与 `AutoChessDemo/Integration/GasCore`。
3. `Dictionary<Entity` 文本命中当前来自 `Assets/GAS/Runtime/General/Helper/EntityHelper.cs:13` 的 `Dictionary<EntityBindingKey, GameObject>` presentation binding map。它是 Runtime Boundary / Presentation 绑定事实，不是 ActiveEffectStore、Runtime Core store 或 gameplay state 容器；后续 API 健康扫描不得把该命中直接写成 Core hot path `Dictionary<Entity,...>` 违规。
4. SourceGenerator gate 截面仍与当前生成报告一致：`GeneratedRuntimeBoundaryHits=135`、`GeneratedRuntimeLifecycleHits=21`、`GeneratedRuntimeStructuralChangeHits=8`、`GeneratedRuntimeOwnershipHits=2`、`GeneratedRuntimeRandomWriteLookupHits=104`、`GeneratedRuntimeBoundaryGateMode=blocking-unclassified-migration-proof`、`GeneratedRuntimeUnclassifiedBoundaryHits=0`。这说明未分类 generated runtime boundary hit 已被阻断，但已分类 `MigrationProofOnly` 仍必须走 R2/R3/R5 退出。

### codedb 恢复后的整体项目审查补充（2026-06-07）

本截面使用 `codedb_status`（427 files / scan ready）和 `rg` 交叉复核。它只记录当前代码事实；目标态约束写入 `../01-目标态架构共识/90-目标态不变量.md`。

1. Shell capability 风险仍集中在 `GASRuntimeShell`：`Assets/GAS/Runtime/General/GASRuntimeShell.cs:16-44` 解析 runtime `World` / `EntityManager`，`:49-115` 创建 command port，`:119-134` 捕获 read model，`:141-147` 执行 `CompleteAllTrackedJobs()`，`:168-234` 继续解析 runtime singleton，`:255-271` 解析 ASC runtime entity。public 面已经比旧版本窄，但 assembly 内仍是 bootstrap / command / snapshot / diagnostics / drain 共用 facade。
2. AutoChess adapter 当前直接消费 Shell internal capability：`Assets/AutoChessDemo/Integration/GasCore/AutoChessGasRuntimeHost.cs:13-17` 注册 runtime systems 并安装 catalog，`:25-30` 卸载 catalog / reset cache / shutdown，`:36-59` 读取 5 段 physical group，`:62-65` 通过 Shell drain runtime jobs。`AutoChessGasBattleEntityLifecycle.cs:51-55` 创建 battle driver，`:86-104` 读写 driver runtime store，`:106-115` 通过 command port 请求销毁 ASC，`:120-132` 捕获 attribute read model。
3. `ASCHandle` 已是外部 opaque handle 的正向过渡，但 raw identity 仍保留在 implementation 内：`Assets/GAS/Runtime/AbilitySystem/ASCHandle.cs:17-27` 暴露 internal `RuntimeEntity` / `TryResolveRuntimeEntity` / `MatchesRuntimeEntity`。当前 `codedb_callers` 显示 `MatchesRuntimeEntity` 调用方为 0；AutoChess report projection 已改为 `AutoChessRuntimeUnitResolver.ResolveUnitIndex(int reportKey)` / `AutoChessRuntimeUnitLink.Matches(int reportKey)`，消费 structured log 的 `SourceReportKey` / `TargetReportKey`。剩余 raw identity 风险在 `BattleUnitRegistry` 内部 `ASCHandle`、live read model、driver handle raw entity 和 Boundary export 解析 report key。
4. 同步等待当前运行命中仍只有 `GASRuntimeShell.cs:146` 的 `CompleteAllTrackedJobs()`；没有在 Runtime / generated runtime / AutoChess 运行代码中发现 `state.Dependency.Complete()`、`.Run()`、`SystemAPI.Query<...>` 或 `SystemAPI.QueryBuilder()`。这只能证明这些 API 没有当前运行命中，不能证明整体 DOTS 性能已达标。
5. `ToEntityArray()` 当前 4 个运行调用点：`GasRuntimeDebugger.cs:889/2260/2458` 属于 Debugger / Observation，`CueManagedLifecycleSystem.cs:39` 属于 managed presentation boundary。`EntityManager.CreateEntityQuery()` 当前 3 个运行调用点：`GasRuntimeDebugger.cs:888`、`GASGlobalTimerSystem.cs:125`、`ActiveEffectStore.cs:1374`。pending AttributeDelta stream migration fallback 已删除，不再作为当前承载风险；这些命中必须按 owner 分类，不能合并写成 Core hot path 或已清零。
6. generated runtime 有正向 job / Blob 进展：`RuntimeAbilityActivation.gen.cs:14-76` 使用 stored query 并调度 `AbilityCatalogCommitJob : IJobChunk`，`RuntimeActiveEffect.gen.cs:150-226` / `:229-300` 的 active effect pre-tick / remove path 也使用 `IJobChunk`，`DefinitionCatalog.gen.cs:21-77` 是 sorted code -> index lookup，`:79-84` 用 `BlobBuilder(Allocator.Temp)` 构建 runtime catalog blob。
7. generated runtime 仍不是 pure glue 完成态：`RuntimeEffectInstant.gen.cs:25-46` 的 `GEEffectSpecBuildSystem` 调度 serial `IJob`，从 singleton stream 读取 command/spec/set-by-caller buffer；`RuntimeActiveEffect.gen.cs:142-186` 当前先 gather active mutation command，再用 `GEActiveEffectMutationChunkApplyJob : IJobChunk` 按 ASC chunk apply。active mutation 和 pending AttributeDelta owner-local apply 的 lookup 面积已经大幅收窄，pending AttributeDelta stream migration fallback 已删除，但 command carrier、generated instant delta record / fan-in 证明和 SourceAttribute snapshot lane 仍未达到 pure glue / store-only 终局。
8. 生成报告当前仍显示 `RuntimeForbiddenDependencyHits=0`、`GeneratedRuntimeBoundaryHits=135`、`GeneratedRuntimeLifecycleHits=21`、`GeneratedRuntimeSystemRegistrationHits=0`、`GeneratedRuntimeStructuralChangeHits=8`、`GeneratedRuntimeOwnershipHits=2`、`GeneratedRuntimeRandomWriteLookupHits=104`、`GeneratedRuntimeManagedConfigHits=0`、`GeneratedRuntimeBoundaryGateMode=blocking-unclassified-migration-proof`、`GeneratedRuntimeUnclassifiedBoundaryHits=0`。因此当前状态是“未分类 generated boundary hit 被阻断，已分类 `MigrationProofOnly` 仍存在”，不是 SourceGenerator pure glue 完成证明。
9. `GeneratedRuntimeSystemRegistrationHits=0` 不能消费为 registry 完成证明：`GASSystemScheduleContract.AddSystemsByTypeName()` 当前使用 `Type.GetType(systemTypeNames[i])` 后在 `systemType == null` 时 `continue`，缺失 generated artifact、类型名漂移或 generated runtime assembly 不可用都会静默漏注册。
10. 当前正向 NativeStream 使用点集中在 `Assets/GAS/Runtime/System/ASCDestroyFinalizeSystem.cs:50-109`：三类 `NativeStream(Allocator.TempJob)` 随 scan/apply job handle 串联 dispose，并把 dispose handle 写回 `state.Dependency`。它是 R4/R3 可复用的正向模式，但仍需对照 `NAT-01` / `NAT-03` 补 owner、segment budget 和 deterministic merge evidence，不能自动泛化为所有 fan-in 已达标。

## R1/R2/R3/R4/R5/R6 当前代码证据截面

本截面只记录 2026-06-07 抽样复核到的当前实现事实，用于给 `02-主线任务树` 的交还门槛提供证据输入。

### R1 Boundary Shell 收权

1. `Assets/GAS/Runtime/General/GASRuntimeShell.cs:30-44` 的 `TryGetRuntimeEntityManager` / `TryResolveRuntimeEntityManager` 当前是 internal，而不是 public；`:66-115` 仍能把 `EntityManager` 作为 command-port 创建能力交给 assembly 内调用方。
2. `Assets/GAS/Runtime/AbilitySystem/AbilitySystemBinding.cs:11-70` 仍在 MonoBehaviour Shell implementation 内保存 `ASCCommandPort`，并通过 `GASRuntimeShell.TryCreateASCCommandPort(...)` 创建 runtime command port。
3. `AbilitySystemBinding.cs:47-62` 对外暴露 `Commands`、`ReadModel`、`CaptureReadModel()` 和 `ASCHandle`；旧 runtime-adapter raw entity 方法已退场，当前保留的是 internal `TryResolveRuntimeEntityForBoundary(out Entity)`，用于 Boundary/adapter 内部解析。
4. `Assets/GAS/Runtime/AbilitySystem/ASCCommandGateway.cs:262-290` 的 `ASCCommandPort` 仍保存 `EntityManager` 与 runtime ASC `Entity`，`Create()` 直接通过 `ASCEntityFactory.Create(entityManager)` 产生 ASC。
5. 正向事实：public target 参数已从旧 raw `Entity target` 多数收缩为 `ASCHandle`（如 `RequestGameplayEffectTo(..., ASCHandle)`、`RequestActivateAbility(..., ASCHandle)`）；但 `RequestRemoveGameplayEffect(Entity gameplayEffect)`、多处 `Request*` 返回 `Entity`、`RuntimeEntity` internal 面和 runtime-adapter raw entity 导出仍是迁移面。
6. `Assets/GAS/Runtime/AbilitySystem/ASCReadModel.cs:16` / `:64-65` 仍由 `EntityManager + Entity/ASCHandle` 构造。虽然当前 getter 已是构造期捕获结果，但 live ECS 捕获 owner 已下沉到 `GASRuntimeShell.TryCaptureASCReadModel()`，尚未变成 BoundaryProjection snapshot 产物。
7. `Assets/GAS/Runtime/General/GASRuntimeShell.cs:10-271` 当前 public 面主要保留 `TryCreateASCCommandPort`、`TryCaptureASCReadModel` / `CaptureASCReadModel`，World / `EntityManager` / runtime singleton / job drain 已转为 internal capability。正向事实是 public 面变窄；剩余风险是 bootstrap、write、read、diagnostics、runner sync 和 singleton 解析仍混在同一个 facade。
8. `Assets/GAS/Editor/GameplayAbilitySystem/GASWatcher.cs:248-286`、`:396-436`、`:493-551` 直接通过 `GASRuntimeShell` 读取 Entity 名称、GlobalTimer、Attribute / Tag / Ability / GameplayEffect buffer；这属于 Editor diagnostics consumer，不应进入 Runtime Core 性能结论，也不能作为目标态 Shell API 形态。
9. `Assets/GAS/Runtime/Effect/GameplayEffectConfigRegistry.cs:627-642`、`:731-757`、`:765-778` 仍持有 runtime `EntityManager` cache 用于 prototype 存活检查和清理；这是 Definition / prototype lifecycle 风险，不能和 Core hot path 或 App Shell command 面混写。
10. 剩余风险：Shell public contract 已有 `ASCHandle` 进展，且 raw ECS 句柄多已 internal 化；但 `GASRuntimeShell` 仍在 assembly 内集中解析 `EntityManager`、runtime singleton 和 job drain，ReadModel 仍由 Shell/Boundary 侧主动捕获 live ECS，而不是消费 `GASBoundaryProjectionSystemGroup` 统一产出的 snapshot / ring buffer；因此 R1 不能仅以“已改成 Request API / ASCHandle”交还，必须补 capability 分级和只读 diagnostics port。

### R6 AutoChess Adapter 与 Validation Evidence Chain

1. `Assets/AutoChessDemo/Integration/GasCore/AutoChessBattleRuntime.cs:24-66` 是 Flow 侧窄入口，负责打开 runtime、reset observation、tick、导出 snapshot / diagnostics 与 shutdown；`AutoChessGasCoreBridge.cs:5-45` 当前是 Session 侧 facade，只转发 unit / driver lifecycle、report projection 和 attribute snapshot 读取，bootstrap / teardown / observation / ticker 已由 `AutoChessBattleRuntime` 直接转给 `AutoChessGasRuntimeHost`、`AutoChessGasObservationGateway` 和 `AutoChessGasRuntimeTicker`。
2. `AutoChessGasRuntimeHost.cs:7-60` 仍通过 `GASRuntimeShell.TryResolveRuntimeWorld` 和 `TryResolveRuntimeEntityManager` 执行 runtime 初始化、system bootstrap、catalog session install/uninstall、tick group lookup 和 job drain。它是当前活动 host owner，不是 orphan。
3. `AutoChessGasObservationGateway.cs:11-257` 通过 `GASRuntimeShell` 读取 World、GlobalTimer、EventBus、EventLogSink、RuntimeDebugger，负责 official diff capture、observation reset、structured log / diagnostics snapshot 和 timing counter 写入。
4. `AutoChessGasBattleEntityLifecycle.cs:8-247` 负责 battle unit / driver lifecycle、battle unit key -> `ASCHandle` registry、runtime unit resolver 和 live attribute read；其中 `TryCreateBattleUnitCommandPort()` 仍通过 Shell command-port capability 创建 ASC，并直接给 ASC 写 `AutoChessBattleUnitComponent`。
5. `AutoChessGasBattleEntityLifecycle.cs:125-129` 当前 `DestroyBattleUnit()` 已通过 `ASCCommandPort.RequestDestroy()` 写销毁请求，不再在该文件中直接销毁 ASC / granted ability / active effect；这是正向事实。
6. `AutoChessGasRuntimeTicker.cs:53-57` 调用 `AutoChessGasRuntimeHost.TryCompleteRuntimeJobs()`，再进入 `GASRuntimeShell.TryDrainRuntimeJobs()` 的 `entityManager.CompleteAllTrackedJobs()`；它发生在 AutoChess runner timing 包裹内，只能作为 runner / debugger measurement sync evidence，不能证明 Core hot path 无同步成本。
7. `AutoChessGasCoreContracts.cs:7-58` 的 `AutoChessGasBattleUnitHandle` 当前只持有 battle unit key；旧 unit-handle raw-entity 导出口已无命中。raw ASC `Entity` 不再由 lifecycle 显式导出，当前 `AutoChessGasBattleEntityLifecycle.CreateRuntimeUnitResolver()`（`:67-80`）把 battle unit key 的 `ReportKey` 包装成 `AutoChessRuntimeUnitLink`（`:236-251`）；`AutoChessGasBattleReportFactProjector.cs:42-77` 只用 `entry.SourceReportKey` / `entry.TargetReportKey` 映射 battle unit index。`Assets/AutoChessDemo/Battle/Report/AutoChessBattleReportBuilder.cs` 只消费 `AutoChessBattleReportFact[]`。
8. `Assets/AutoChessDemo/Battle/AutoChessBattleContracts.cs:267-294` 的 `AutoChessBattleRuntimeTiming` 当前只承载 5 段 GAS physical group timing；`AutoChessRuntimeRunner.CreateTimingSummary()` 输出 `ecsRuntimeTickOnly=true` 和 group timing，没有独立 runner / debugger / physics / render 字段。
9. 正向事实：AutoChess 侧 GAS 访问已经集中到 `Integration/GasCore`，并拆出 `AutoChessBattleRuntime` Flow 入口、`AutoChessGasCoreBridge` Session facade、runtime host、catalog session、observation gateway、battle entity lifecycle、runtime ticker、report fact projector 和 contracts 文件。
10. 剩余风险：集中 adapter 还不是 Thin Adapter；flow runtime / host / observation / lifecycle / catalog / ticker / live read 仍共享 `GASRuntimeShell` internal ECS capability 或 `ASCHandle` compatibility handle。R6 交还必须给 direct EM owner 分类、report key coverage、headless / scene 同构 evidence、core / boundary / debugger / runner / physics / render timing split，而不是只证明 AutoChess x1/x50 跑通或只输出 5 段 group timing。

R6 当前 direct `EntityManager` owner 分类已进一步收窄：

| owner 分类 | 代码证据 | 当前判定 |
|---|---|---|
| runtime host / catalog bootstrap | `AutoChessGasRuntimeHost.cs:7-29`、`AutoChessGasCatalogSession.cs:6-16`、`AutoChessBattleDefinitionCatalogBuilder.cs:12-31` | 初始化路径可保留，但需要 install / dispose owner 和 fixed tick owner 证据 |
| unit / driver lifecycle adapter | `AutoChessGasBattleEntityLifecycle.cs:10-222` | 仍直接创建 driver entity、切换 driver destroy request、给 ASC 添加 demo component，不是 Thin Adapter 完成态 |
| battle unit registry / report key resolver | `AutoChessGasBattleEntityLifecycle.cs:11-12`、`:67-80`、`:140-166`、`:211-251`、`AutoChessGasCoreContracts.cs:7-58`、`GasStructuredLogExport.cs:419-473` | battle unit key 已替代对外 raw ASC handle；report projector 通过 report key 映射 battle unit index。Integration/GasCore 内部 registry 仍保存 `ASCHandle`，属于 command/read compatibility，不是稳定业务 key |
| live read model | `AutoChessGasBattleEntityLifecycle.cs:120-132`、`AutoChessBattleSession.cs:183-186` | 业务胜负和 unit result 仍经 adapter live-read ECS |
| observation access | `AutoChessGasObservationGateway.cs:11-257` | run boundary reset / replay-debugger snapshot 可作为 Boundary observation，但不得混入 CoreSimulation 性能证明 |
| runner sync / debugger timing | `AutoChessGasRuntimeTicker.cs:9-37`、`:44-113`、`AutoChessRuntimeRunner.cs:138-260` | `CompleteAllTrackedJobs()` 属于 runner timing sync 或 debugger measurement cost |
| report projection key | `AutoChessGasCoreContracts.cs:46-58`、`AutoChessGasBattleEntityLifecycle.cs:211-251`、`AutoChessGasBattleReportFactProjector.cs:7-77`、`GasStructuredLogExport.cs:419-473`、`AutoChessBattleReportBuilder.cs:8-91` | Integration/GasCore projector 已用 `SourceReportKey` / `TargetReportKey` 映射 battle unit index；Battle/Report builder 当前只消费 `AutoChessBattleReportFact[]`，后续要固化 boundary report key coverage 和 missing-key fail gate |

### R2 Generated Active Mutation

1. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeActiveEffect.gen.cs:142-186` 在 `GASActiveEffectMutationApplySystem` 中创建 frame-local `NativeList<GEEffectCommandBuffer>` 与 owner range map，先调度 `GEActiveEffectMutationGatherJob : IJob`，再调度 `GEActiveEffectMutationChunkApplyJob : IJobChunk`。
2. `RuntimeActiveEffect.gen.cs:362-495` 的 gather job 从 singleton stream 读取 command，筛选 `GEEffectCommandKind.ActiveMutation`，按 `TargetAsc -> Sequence -> ParentContextId` 排序，写入 `ActiveMutationCommandCount`、`ActiveMutationOwnerGroupCount`、`ActiveMutationMaxOwnerRange`、`ActiveMutationSortMoveCount`。
3. `RuntimeActiveEffect.gen.cs:496-700` 的 chunk apply job 在 ASC chunk 内读取当前 owner 的 active store、slot、set-by-caller snapshot、cleanup、mutation、tag、attribute、active modifier、tag source 和 ability slot buffer；它不再持有 `BufferLookup<AttributeValueBuffer>`，避免和 writable chunk attribute buffer alias。
4. 正向事实：active mutation 已有 owner 排序、owner range counter、ASC chunk-local applicator 和 x50 runner 证据；`activeMutationEstimatedRandomLookups=0`、`activeMutationOwnerResourceLookups=0`、`activeMutationMigrationCarriers=0`。
5. 剩余风险：当前仍从 singleton stream 收集命令，gather 阶段是 serial command scan；为避免 Unity safety alias，跨 owner `SourceAttribute` capture 已退出 active mutation apply，后续必须进入 magnitude snapshot / resolve lane。
6. `Assets/GAS/Editor/CodeGen/Phases/GasGlueCodeGenPhases.cs:3032-3410` 仍包含同类生成模板，因此修复入口是模板、manifest 和 validation gate，不是手工编辑 `.gen.cs`。

### R3 Singleton Carrier / Fan-In

1. `Assets/GAS/Runtime/Effect/Component/Dynamic/GEEffectCommandSpecStream.cs:38` 定义 `GEEffectCommandStreamComponent`；`:60`、`:93`、`:118`、`:142`、`:163` 分别定义 command、spec、attribute delta、active mutation、gameplay fact buffer。
2. `Assets/GAS/Runtime/System/Effect/GEEffectCommandSpecStreamPhases.cs:18-37`、`:92-121`、`:226-257` 的 phases 仍以 `GEEffectCommandStreamComponent` singleton entity 和 buffer lookup 为核心输入输出。
3. `GEEffectCommandSpecStream.cs:615-633` 存在 `MergeParallelCommandFanIn`，会对 `ParallelCommandFanInRecord` 做排序后写回 singleton command buffer。
4. `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStreamOwnerContract.cs:311-388` 已把 EffectCommand、EffectCommandSetByCaller、InstantEffectSpec、ActiveEffectMutation、AttributeDelta、TypedSimulationFact 全部登记为 current `SingletonDynamicBuffer`，target 分别是 `PerThreadNativeStream`、`CommandRangeAuxiliaryBuffer` 或 `OwnerLocalDynamicBuffer`。
5. `GASRuntimeStreamOwnerContract.cs:418-440` 为这些 stream 统一写入 `EGasRuntimeFrameStreamScaleStatus.MigrationCarrier`、`ProofOrMigrationMarker`、`X50BufferPressure`、`X1000ScaleGate`、`ParallelProducerIntroduced`、`MergeCostDominatesSpecEvaluation` 等 reselect trigger。
6. 正向事实：fan-in 已有 deterministic ordering 的显式代码路径，且 contract 已承认当前 stream 是迁移承载。
7. 剩余风险：command/spec/delta/fact/mutation 共用 singleton DynamicBuffer carrier，且 `MergeParallelCommandFanIn` 仍是 managed list + `EntityManager` 写回路径；当前更适合作为 proof/migration carrier 证据，而不是 scale-ready carrier 证明。

### R4 StructuralCommit 与 Official Evidence

1. `Assets/GAS/Runtime/System/SystemGroup/GASGroups.cs:62-105` 已定义 `GASStructuralCommitSystemGroup`、`BeginGASStructuralCommitECBSystem`、`EndGASStructuralCommitECBSystem`。
2. `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStructuralPlaybackGateContract.cs:293-309` 已把 required playback gate 绑定到 `GASStructuralCommitSystemGroup` / `EndGASStructuralCommitECBSystem`，但 `contractOnly: true`，所以它是当前约束和证据模型，不是 runtime 已闭环证明。
3. `GASRuntimeStructuralPlaybackGateContract.cs:326-336` 为 route 写入 `directEntityManagerAllowed: false`；`:339-347` 只把 create / destroy / add / remove / add buffer 判为需要 StructuralPlayback；`:434-463` 为 required route 附加 source phase、playback phase、ECB command、bulk query、cleanup 和 local migration evidence 标记。
4. `Assets/GAS/Runtime/Debugger/GasRuntimeOfficialToolDiff.cs:136-155` 会在 Editor / Development build 尝试开启 `EntitiesJournaling` 并读取 Profiler 状态；`:202-251` 统计 create、destroy、add、remove、enable、disable、set、get 等 record type；`:263-286` 输出 `journalingCaptured`、record count、Profiler enable/category 和 capture state。Profiler 不可用或未启用时只输出状态文本，不能当作 captured 证据。
5. `Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs:1091-1207` 记录 system timing 与 Runtime Core counters；`:1947-2094` 创建并导出 snapshot；`:2863-2875`、`:3114-3134` 会导出 `requiredStructuralPlaybacks`、`recordedStructuralPlaybacks`、`ecbCommands`、`bulkQueries`、`profilerMarkers`、`journalingMarkers`、core / physics / render / runner cost split。
6. `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeDebuggerEvidenceGateContract.cs:259-303` 从 frame phase、stream owner 和 structural plan 汇总 evidence gate；其中 `profilerMarkerCount: 4`、`journalingMarkerCount: 1`、core/physics/render/runner split 是 contract 计数，不等于本轮官方工具实际 capture。
7. `Assets/AutoChessDemo/AutoRunner/AutoChessRuntimeRunner.cs:63-82` 输出 Hotspot、Boundary owner、Boundary report key coverage、Runtime Debugger、Runtime Timing、OfficialToolDiff、DataFlow、Sequence 和 Diagnostics；`AutoChessBattleValidationReport.cs` 已把 official diff summary 输出为 `journalingAvailable`、`journalingCaptured`、`journalingStructural`、runtime / journaling create-destroy delta、Profiler state、TopN，并把 Boundary report key coverage 纳入 runtime chain；`AutoChessBattleValidationRun.cs:400-420` 在要求 official diff 时会把 `JournalingCaptured` 作为必要条件。
8. `Assets/AutoChessDemo/Integration/GasCore/AutoChessBattleRuntime.cs` 已成为 Flow 侧窄入口，`AutoChessGasCoreBridge.cs` 已成为 Session 侧 facade；direct ECS access 当前分布在 `AutoChessGasRuntimeHost.cs`、`AutoChessGasObservationGateway.cs`、`AutoChessGasBattleEntityLifecycle.cs`、`AutoChessGasCatalogSession.cs`、`AutoChessGasRuntimeTicker.cs` 和 `AutoChessGasCoreContracts.cs`。这说明业务入口更集中，且 report projection owner 已改为 report key；但 host / observation / lifecycle / catalog / ticker / live read 仍通过 internal Shell capability 或 `ASCHandle` 取得 live ECS 句柄，assembly 内 `EntityManager` capability、driver adapter raw entity 和 live read raw handle 仍未退出。
9. `GASRuntimeShell.cs:141-147` 的 `CompleteAllTrackedJobs()` 是当前唯一运行调用点；AutoChess 通过 `AutoChessGasRuntimeHost.TryCompleteRuntimeJobs()` 和 `AutoChessGasRuntimeTicker.CompleteRuntimeJobsTimed()` 间接调用。它发生在 AutoChess runner 对 physical group 的 timing 包裹后，应作为 runner/debugger measurement cost 或 explicit sync evidence 处理，不能被混入 CoreSimulation 热路径优化证明。
10. `AutoChessGasCoreContracts.cs:7-58` 的 `AutoChessGasBattleUnitHandle` 已改为保存 battle unit key，旧 unit-handle raw-entity 导出口当前 0 命中；R1/R6 的 report projection 风险已从 `Matches(Entity)` 收敛为 report key coverage。剩余 raw ECS identity 风险是 lifecycle registry 内部 `ASCHandle`、`ASCReadModel(em, ASCHandle)` live capture、`AutoChessGasBattleDriverHandle.TryGetEntityForAdapter(out Entity)` 和 Boundary export 解析 report key。
11. 正向事实：StructuralCommit group、Begin/End ECB systems、route contract、Journaling diff 工具、Debugger summary 字段和 AutoChess official diff summary 已存在。
12. `Assets/AutoChessDemo/Battle/Validation/AutoChessBattleValidationReport.cs:341-377` 已能输出 Mermaid dataflow / sequence diagram；当前实现从 `AutoChessBattleResult`、diagnostics 和 official diff 字段拼接字符串，作为导出格式是正向事实，但还不是独立机器验收源。
13. 剩余风险：group / contract / counter 字段存在不等于结构变化已集中；当前还缺 route-level required vs recorded 对账、每类 direct EM owner 分类、Profiler/Journaling captured / disabled / unsupported reason、AutoChess core / boundary / debugger / runner / physics / render timing split、TopN 与 Debugger counter 映射，以及由 structured evidence 派生日志 / 图表的一致证据。

### R5 SourceGenerator Boundary Gate

1. `Assets/GAS/Editor/CodeGen/GasCodeGenPipeline.cs:20-27` 当前把 Core phases 与 `AutoChessDemoConfigPhase` 拆成两组；`:41-47` 的 `TryRunAll()` 仍先跑 Core 再跑 AutoChessDemo standalone，因此 Core report 与 Demo scenario evidence 必须分 owner 消费。
2. `Assets/GAS/Editor/CodeGen/Phases/GasGlueCodeGenPhases.cs:6517-6547` 的 `ValidationReportPhase` 已统计 `GeneratedRuntimeBoundaryHits`、`GeneratedRuntimeLifecycleHits`、`GeneratedRuntimeSystemRegistrationHits`、`GeneratedRuntimeStructuralChangeHits`、`GeneratedRuntimeOwnershipHits`、`GeneratedRuntimeRandomWriteLookupHits`、`GeneratedRuntimeManagedConfigHits`、`GeneratedRuntimeBoundaryGateMode` 和 `GeneratedRuntimeUnclassifiedBoundaryHits`。
3. `GasGlueCodeGenPhases.cs:6612-6616` 把 Generated Runtime Boundary Gate 写成 `CurrentMode: blocking-unclassified-migration-proof`，并声明 `RuntimeAbilityActivation.gen.cs`、`RuntimeEffectInstant.gen.cs`、`RuntimeActiveEffect.gen.cs` 的 lifecycle / lookup / structural owner hit 必须保持显式分类并绑定 R2/R3/R5 退出工作；这已经不是旧 `report-only`，但仍不是 pure glue 完成态。
4. `Assets/GAS/Generated/CodeGen/GasCodeGenValidationReport.md:16-23` 当前快照为 `GeneratedRuntimeBoundaryHits=135`、`GeneratedRuntimeLifecycleHits=21`、`GeneratedRuntimeSystemRegistrationHits=0`、`GeneratedRuntimeStructuralChangeHits=8`、`GeneratedRuntimeOwnershipHits=2`、`GeneratedRuntimeRandomWriteLookupHits=104`、`GeneratedRuntimeManagedConfigHits=0`、`GeneratedRuntimeBoundaryGateMode=blocking-unclassified-migration-proof`、`GeneratedRuntimeUnclassifiedBoundaryHits=0`。
5. `Assets/GAS/Runtime/System/SystemGroup/GASSystemScheduleContract.cs:374-392` 当前由手写 registry 反射注册 generated systems；`Type.GetType(...)` 失败时直接 `continue`，所以 generated type-name、asmdef 或 assembly 缺失不会在注册阶段 fail-fast。
6. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeDefinitionGlue.gen.cs:13-108` 是正向 pure glue：静态 resolver 从 `GASDefinitionCatalogBlob` 构造 `AbilityActivationPlanRecord`、`GECommandSeedRecord`、`ResolvedModifierRecord`，不包含 `ISystem`、ECB 或 query owner。
7. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeAbilityActivation.gen.cs:14-76` 仍生成 `AbilityCatalogCommitSystem : ISystem`，并在 `OnUpdate` 中取得 `EntityManager`、`SystemAPI.GetComponentLookup`、`SystemAPI.GetBufferLookup` 后调度 job；这说明 generated runtime 仍拥有 gameplay lifecycle / lookup owner。
8. `Assets/GAS/Generated/CodeGen/Runtime/DefinitionCatalog.gen.cs:79-84` 暴露 `GASGeneratedDefinitionCatalogBuilder.BuildCatalog(Allocator.Persistent)` 并内部使用 `BlobBuilder(Allocator.Temp)`；这作为 bootstrap / baking 初始化可接受，但必须给 install / dispose owner 证据，不能被写成 hot path runtime lookup 完成证明。
9. 正向事实：generated runtime 已有 Blob catalog、sorted code lookup、pure resolver、Core/Demo phase 拆分和 boundary hit 分类报告。
10. 剩余风险：R5 不能以 `RuntimeForbiddenDependencyHits=0`、`GeneratedRuntimeUnclassifiedBoundaryHits=0`、`GeneratedRuntimeSystemRegistrationHits=0` 或 generated code 已存在交还；只要 135 个 boundary hit 仍以 `MigrationProofOnly` / bootstrap owner 分类存在，且 handwritten generated type-name registry 仍会静默跳过缺失 type，SourceGenerator 仍不是 pure glue 完成态，R5 还必须交还 artifact 分类、退出任务、阈值门、registry fail-fast 或失败条件。

## 当前重划分

### 1. OOP Shell 不是中间层

`AbilitySystemBinding`、`ASCCommandPort`、`ASCReadModel` 现在更像 Application Shell / Runtime Boundary 入口，而不是 Runtime Core 的中间层。这个方向正确，但当前接口仍有两类残留风险：

1. `ASCCommandPort` 通过 `ASCBoundaryCommandWriter` 直接拿 `DynamicBuffer` 并启用 pending marker。
2. `AbilitySystemBinding.Commands` 暴露 `ASCCommandPort`，`AbilitySystemBinding.TryResolveRuntimeEntityForBoundary(out Entity)` internal 面、`ASCCommandPort.RuntimeEntity` internal 面和多处 `Request*` 返回值仍允许 assembly 内 raw `Entity` 传播；`RequestGameplayEffectTo` / `RequestActivateAbility` 的 target 已主要收缩为 `ASCHandle`，但 boundary-adapter raw entity 解析仍是迁移面。
3. `ASCReadModel` 已在构造时捕获 level、tag mask、attribute、presentation event 快照，getter 不再直接读 `EntityManager` / `DynamicBuffer`；但 capture 动作仍由 Shell/Boundary 侧直接触达 ECS。
4. `GASRuntimeShell` 当前还是多能力句柄口：同一个 facade 服务 Runtime bootstrap、command port、read model、diagnostics singleton、job drain 和 Editor watcher。它把“业务 Shell 可见 interface”和“Boundary implementation 可用 ECS handle”混在一起，导致 owner 分类必须靠调用点人工判断。
5. 这类能力必须继续收缩到 Layer 2：Shell 只持有 opaque ASC handle / target ref；写入只生成 command record，读取只消费由 BoundaryProjection 统一产出的 snapshot / ring buffer；Editor watcher / Debugger 只能走只读 diagnostics evidence，不直接复用业务 Shell 写句柄。

### 2. Thin Adapter 必须继续变薄

AutoChess 的 `Integration/GasCore` 已把业务层对 GAS 的调用集中到一个 adapter facade，并拆出 runtime host、catalog session、observation gateway、lifecycle、ticker、contracts 等文件，这是正向事实。但 internal Shell capability 仍能把 `EntityManager` 交给 adapter implementation；host 直接负责 runtime world / tick group / job drain，observation gateway 直接重置和读取 Debugger / Replay singleton，lifecycle 直接写 demo component 与 driver store，接口仍不够深。目标态需要让这些 capability 继续收缩为：

1. `BattleRuntimeHost`：World / SystemGroup / fixed tick owner。
2. `BattleCatalogSession`：Definition catalog 安装和释放。
3. `BattleEntityLifecycle`：ASC / driver / unit structural request owner。
4. `BattleObservationGateway`：Replay / Debugger / Official diff snapshot。

这些子 owner 可以仍在同一个 adapter implementation 内，但对外接口不得暴露 `EntityManager` 或 GAS singleton。

### 3. Runtime Core 已 ECS 化，但还未 store-ready

当前 Core 已有 `ISystem`、stored query、ECB gate 和 generated catalog。真正不达标的是数据承载：

1. `GEEffectCommandStreamComponent` singleton owner 同时承载 command / set-by-caller / spec / mutation / fact。
2. `GASActiveEffectMutationApplySystem` 虽已 job 化、owner 排序并按 owner range 获取 store/slot/snapshot，但仍从 singleton buffer 收集命令，且 helper 内仍通过 `BufferLookup` / `ComponentLookup` random access 写属性、tag、ability 等派生状态。
3. 这类实现可以作为 `MigrationProofOnly`，但不能作为 DOTS 优秀水平。后续必须按 target / owner 分组，形成 owner-local range 或 chunk-local applicator。

### 4. Debugger 是 evidence owner，不是日志系统

当前 Debugger 已有 counter 和 official diff 工具，但目标应从“能输出日志”升级为“能定位 DOTS API 健康度和性能热点”。最低事实字段应覆盖：

1. physical group timing 和 kernel lane timing。
2. command/spec/delta/fact/cue/presentation 计数。
3. global buffer pressure、random lookup count、lookup update count。
4. structural playback 来源和 phase。
5. proof-only API、reselect trigger、official tool capture state。

没有这些字段时，AutoChess x50/x1000 跑通只能证明功能，没有证明 DOTS 性能优秀。

### 5. Luban + SourceGenerator 已进入 Runtime，但仍需收权

当前 generated catalog 已被 Runtime 和 AutoChess 消费，这是正向事实。后续重点不是“多生成代码”，而是让 SourceGenerator 只生成 Runtime Core 可消费的 immutable definition、static lookup、pure glue、validation graph，不生成或隐藏 runtime lifecycle owner。

## DOTS 规则对照

| 规则族 | 当前命中 | 审查结论 |
|---|---|---|
| `SYS-01` / `SYS-02` | 5 段 physical group 已存在 | 继续保持 Core 权威落在 ECS System / Job，Shell 不得成为 OOP scheduler |
| `QRY-01` / `PRF-05` | runtime hot query 已明显收窄 | 新任务不应恢复主线程 foreach；Debugger / Cue boundary 要分组计时 |
| `SC-01` / `ECB-03` / `PRF-04` | StructuralCommit group 已存在 | group 存在不等于合规，必须有 Journaling / Profiler / Debugger evidence |
| `BUF-02` / `BUF-04` / `PRF-10` | singleton stream 仍是 proof carrier，多个 buffer `InternalBufferCapacity(0)` | effect fan-in / active mutation 必须有 deterministic merge、容量预算和 externalized / spill 指标 |
| `STORE-03` / `NAT-03` / `CASE-12` | command/spec/delta/fact/mutation 数据性质不同但当前共用 singleton carrier | transient fan-in 应按数据性质选 `NativeStream` / owner-local range，并定义 deterministic merge 与 allocator owner |
| `QRY-04` / `PRF-06` / `PRF-19` | generated active mutation 与 pending AttributeDelta owner-local path 已收束到 ASC chunk-local applicator；stream migration fallback 与部分 pre-tick/source magnitude 路径仍有 lookup 风险 | 下一阶段主切口应转向 singleton stream carrier / fallback 拆分，并为 SourceAttribute snapshot lane 证明 lookup 目标集合无重叠竞态 |
| `DBG-01..05` | Debugger 已有雏形 | 需要形成机器可读 evidence model，而不是人工日志 |
| `BLOB-01` / `BLOB-02` / `BUR-01` | generated catalog 已进入 Runtime | SourceGenerator 只能生成 immutable/pure glue，不能拥有 gameplay lifecycle |

## 当前不是目标态的事实

1. `GASActiveEffectMutationApplySystem` 已是 gather + ASC chunk-local apply，但 command source 仍是 singleton stream carrier，SourceAttribute 跨 owner capture 还没有 magnitude snapshot / resolve lane，因此未形成完整 store-only 终局。
2. `ASCReadModel` 已是捕获式 snapshot，但仍由 Shell/Boundary 构造阶段直接读 `EntityManager`，尚未由 BoundaryProjection 统一产出 read snapshot / ring buffer。
3. `GASRuntimeShell` 仍把 World / `EntityManager` / runtime singleton / diagnostics / job drain 混成一个 public facade，外部消费者包含 runtime binding、AutoChess adapter 和 Editor watcher；这不是目标态 Shell capability model。
4. AutoChess adapter 已从单体 bridge 拆成 facade、runtime host、catalog session、observation gateway、ticker、lifecycle、contracts 等 owner 文件，但 direct `EntityManager` 操作仍在这些 owner 内部存在；这些 owner 是当前活动链路，不是 Thin Adapter 完成态。
5. StructuralCommit evidence 尚未证明所有 create / destroy / add / remove / enable / disable 的来源和 phase。
6. Debugger counters 尚未覆盖所有 API health、proof-only API、reselect trigger 和 official tool state。
7. SourceGenerator 已生成 runtime systems；这类 generated lifecycle 属于迁移事实，不应被目标态继续扩大。

## 下一步事实验证需求

后续执行 Agent 交还时，至少要给出：

1. 静态扫描：`GASManager.EntityManager`、`GASRuntimeShell.TryResolveRuntimeEntityManager`、`state.Dependency.Complete()`、`.Run()`、legacy EventBus writer、generated lifecycle owner。
2. 生成链：`Tools/CodeGen/Generate-GAS-SourceGen.bat` 或等价 CLI，且不手改 `.gen.cs`。
3. 编译域：runtime、generated runtime、AutoChessDemo asmdef 对应 csproj 或 Unity batchmode 编译。
4. 运行证据：AutoChess x50 / x100 / x1000 中至少一档，并输出 core / boundary / debugger / runner timing split。
5. 官方工具证据：`GasRuntimeOfficialToolDiff`、Entities Journaling 或 Profiler disabled reason；不能用内部日志替代官方工具状态。
