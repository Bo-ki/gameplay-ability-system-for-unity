# GAS 架构瘦身执行记录

> 归档日期：2026-06-07 | 来源计划：`00-当前架构事实/下一轮GAS架构瘦身计划.md` | 范围：`Assets/GAS/Runtime`、`Assets/GAS/Editor/CodeGen/Phases`、`Assets/GAS/Generated/CodeGen/Runtime`

## 归档结论

本轮破坏性瘦身计划已完成并归档。旧 gameplay EventBus 事实源、Damage/Attribute/Cue/Tag helper 兼容写入口、Presentation/Replay 双读、active mutation 无排序证据、Cue/EntityHelper/stream fallback 的全局 EntityManager 读取均已处理。

剩余的 owner-local store 终局、Layer 2 snapshot read model、结构变化 profiling 证据，不再作为本计划未完成项，后续应进入新的独立任务。

## 完成项

1. 删除 `DamageEventBuffer` / `EnqueueDamageEvent`，Damage 统一进入 `GameplayEventBuffer` typed fact。
2. 删除 `EventBusHelper.EnqueueAttributeChangeEvent`、`EnqueueCueRequest`、`EnqueueTagChangeEvent` 旧 gameplay observation enqueue API。
3. `PresentationOutboxProjectionSystem` 与 `ReplayLogSystem` 只读 `GameplayEventBuffer` typed fact，不再双读 Attribute/Cue/Tag/Damage 边界缓冲。
4. generated active mutation 只改 `GasGlueCodeGenPhases` 模板并重新生成 `.gen.cs`：active mutation command 进入 frame-local `NativeList`，按 `TargetAsc -> Sequence -> ParentContextId` 排序后应用。
5. `GEEffectCommandStreamComponent` 增加 active mutation 证据计数：`ActiveMutationCommandCount`、`ActiveMutationOwnerGroupCount`、`ActiveMutationMaxOwnerRange`、`ActiveMutationSortMoveCount`。
6. `EffectCommandSpecStream` 删除 `TryResolveGasManagerSingleton()` fallback；stream owner 只通过 `EnsureSingleton()` / `RegisterKnownSingleton()` 显式注册解析。
7. `EntityHelper` 改为显式 `EntityManager` 入口；`GameplayCueUnit` 构造签名破坏性要求 `EntityManager`；`GameplayCueBase` 通过 `SetRuntime(EntityManager, Entity)` 初始化 managed Cue runtime context。
8. `ASCCommandGateway` 写路径集中到 `ASCBoundaryCommandWriter`，`ASCReadModel` 显式接收 `EntityManager`。

## 验证

1. `Tools\CodeGen\Generate-GAS-SourceGen.bat`：通过。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore`：通过，仅既有 `MSB3277`。
3. `dotnet build com.exhard.exgas.generated.runtime.csproj --no-restore`：通过，仅既有 `MSB3277`。
4. `Assets/GAS/Generated/CodeGen/GasCodeGenValidationReport.md`：`RuntimeForbiddenDependencyHits=0`、`GeneratedHotPathRegressionHits=0`、`GeneratedDuplicateMethodHits=0`。
5. 静态门禁：Runtime/generated runtime 中 `state.Dependency.Complete()` / `.Run()` 清零；旧 Damage/EventBus gameplay enqueue 命中只剩 codegen validation gate 字符串；`TryResolveGasManagerSingleton`、`SetCueEntity`、`CopyCueComponent` 清零。

## 初始未纳入通过条件

初始归档时，`dotnet build com.exhard.exgas.autochessdemo.csproj --no-restore` 未纳入通过条件，原因是当时 AutoChess 工作流新增的 `Assets/AutoChessDemo/Battle/Report/AutoChessBattleReportBuilder.cs` 尚未被 Unity 生成的 csproj 纳入，导致 `AutoChessBattleSession.cs` 找不到 `AutoChessBattleRuntimeUnitIndex`。

该状态已被后续追加验收覆盖：`dotnet build com.exhard.exgas.autochessdemo.csproj --no-restore -m:1 -p:UseSharedCompilation=false` 已通过，仅剩既有 `MSB3277`。

## 复发入口

如果后续重新出现以下情况，应开新任务处理，不恢复本计划：

1. Runtime Core 或 generated runtime 重新直接写 Attribute/Cue/Tag/Damage 边界缓冲。
2. `.gen.cs` 被手工修改而不是通过模板和 sourcegen 生成。
3. `EffectCommandSpecStream` 恢复对 `GASManager.EntityEffectCommandSpecStream` 的隐式解析。
4. Cue / EntityHelper managed boundary 恢复内部 `GASManager.EntityManager` 读取。
5. active mutation 文档把当前 serial singleton carrier 写成 owner-local store 终局。

## 追加验收：ActiveMutation owner-local scratch

> 追加日期：2026-06-07 | 原始易失日志路径：`Temp/AutoChessBattleValidation-ActiveMutationOwnerLocal-Run2.log`、`Temp/AutoChessBattleValidation-PeriodGate.log` | 稳定证据：本节摘录

本次追加验收只确认一件事：active mutation 的旧 global migration carrier 已退出当前验收门。它不能证明 ActiveMutation 已经达到 scale-ready 终局，因为 `GASActiveEffectMutationApplySystem` 仍是 singleton stream serial `IJob`，owner resource 仍有 helper lookup random access。

### 追加改动事实

1. `ASCEntityFactory` 与 `GASRuntimeEntityArchetypes.ASC` 已把 `ActiveEffectMutationBuffer` 纳入 ASC 核心 archetype。
2. generated active mutation apply 不再把 mutation 结果写入 stream owner 的 `ActiveEffectMutationBuffer`，改为写当前 ASC owner-local `ActiveEffectMutationBuffer` scratch，并在 capture owner resources 时清空 scratch。
3. `GEEffectCommandStreamComponent.ActiveMutationMigrationCarrierCount` 不再因 active mutation 结果经 stream owner carrier 中转而增加。
4. AutoChess sourcegen 阈值收紧为 `MaxActiveMutationMigrationCarriers=0`，该值已进入 `AutoChessGeneratedScenarioProfile`。

### 追加验证命令

1. `cmd /c Tools\CodeGen\Generate-GAS-SourceGen.bat`：通过。
2. `dotnet build com.exhard.exgas.runtime.csproj --no-restore -m:1 -p:UseSharedCompilation=false`：通过，仅既有 `MSB3277`。
3. `dotnet build com.exhard.exgas.generated.runtime.csproj --no-restore -m:1 -p:UseSharedCompilation=false`：通过，仅既有 `MSB3277`。
4. `dotnet build com.exhard.exgas.autochessdemo.csproj --no-restore -m:1 -p:UseSharedCompilation=false`：通过，仅既有 `MSB3277`。
5. `powershell -ExecutionPolicy Bypass -File Tools\Diagnostics\Verify-GAS-RuntimeCoreBoundary.ps1`：通过，Runtime Core 边界无 AutoChess 引用，pending attribute delta contract 通过。
6. `rg -n "\.Run\(|state\.Dependency\.Complete\(|SystemAPI\.Query<|SystemAPI\.Query\(" Assets/GAS/Runtime Assets/GAS/Generated/CodeGen/Runtime Assets/AutoChessDemo -g "*.cs"`：无命中。

### AutoChess x50 batchmode 关键摘录

```text
AutoChessDemoValidationRunResult:
passed=True
thresholdsPassed=True
runtimeChainPassed=True
blockingDebugErrors=0

AutoChessDemoRuntimeRunner:
activeMutationCommands=200
activeMutationOwnerGroups=200
activeMutationMigrationCarriers=0
activeMutationEstimatedRandomLookups=3000
periodTickDamageFacts=150
periodTickDamageTotal=150
summaryHash=0xBD1684FE

AutoChessDemoRepeatRunEvidence:
passed=True
firstPeriodTickDamageFacts=150
secondPeriodTickDamageFacts=150

AutoChess timing:
GASTickTotal avgMs=1.282
GASCoreSimulationSystemGroup avgMs=0.439
```

### 结论

1. `activeMutationMigrationCarriers=0` 已成为机器验收字段，旧 global mutation carrier 不能再悄悄回流。
2. period child GE 仍在同一次业务链路中维持结构化验收证据：`periodTickDamageFacts=150`、`periodTickDamageTotal=150`。
3. 下一轮不能继续围绕“是否有 carrier”打补丁，而应拆 `GASActiveEffectMutationApplySystem` 的 singleton serial job / helper lookup random access，目标是 ASC chunk-local applicator 或 target-grouped owner-local apply，并降低 `activeMutationEstimatedRandomLookups`。
