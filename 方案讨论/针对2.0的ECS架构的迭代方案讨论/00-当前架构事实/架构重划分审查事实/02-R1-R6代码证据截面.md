# 02 R1-R6 代码证据截面

> Owner：00-当前架构事实/架构重划分审查事实 | 状态：当前事实子页 | 拆分来源：../架构重划分审查事实.md | 拆分时间：2026-06-08

承载 R1/R2/R3/R4/R5/R6 的当前代码证据截面，供任务树领取前复核。

本文件只记录当前代码事实、证据和 DOTS 判定；目标态设计正文回到 `../../01-目标态架构共识/`，任务拆分回到 `../../02-主线任务树/`。

## R1/R2/R3/R4/R5/R6 当前代码证据截面

本截面只记录 2026-06-07 抽样复核到的当前实现事实，用于给 `02-主线任务树` 的交还门槛提供证据输入。

### R1 Boundary Shell 收权

1. `Assets/GAS/Runtime/General/GASRuntimeShell.cs:30-44` 的 `TryGetRuntimeEntityManager` / `TryResolveRuntimeEntityManager` 当前是 internal，而不是 public；`:66-115` 仍能把 `EntityManager` 作为 command-port 创建能力交给 assembly 内调用方。
2. `Assets/GAS/Runtime/AbilitySystem/AbilitySystemBinding.cs:11-70` 仍在 MonoBehaviour Shell implementation 内保存 `ASCCommandPort`，并通过 `GASRuntimeShell.TryCreateASCCommandPort(...)` 创建 runtime command port。
3. `AbilitySystemBinding.cs:47-62` 对外暴露 `Commands`、`ReadModel`、`CaptureReadModel()` 和 `ASCHandle`；旧 runtime-adapter raw entity 方法已退场，当前保留的是 internal `TryResolveRuntimeEntityForBoundary(out Entity)`，用于 Boundary/adapter 内部解析。
4. `Assets/GAS/Runtime/AbilitySystem/ASCCommandGateway.cs:262-290` 的 `ASCCommandPort` 仍保存 `EntityManager` 与 runtime ASC `Entity`，`Create()` 直接通过 `ASCEntityFactory.Create(entityManager)` 产生 ASC。
5. 正向事实：public target 参数已从旧 raw `Entity target` 多数收缩为 `ASCHandle`（如 `RequestGameplayEffectTo(..., ASCHandle)`、`RequestActivateAbility(..., ASCHandle)`）；但 `RequestRemoveGameplayEffect(Entity gameplayEffect)`、多处 `Request*` 返回 `Entity`、`RuntimeEntity` internal 面和 runtime-adapter raw entity 导出仍是迁移面。
6. `Assets/GAS/Runtime/AbilitySystem/ASCReadModel.cs:16` / `:64-65` 仍由 `EntityManager + Entity/ASCHandle` 构造。虽然当前 getter 已是构造期捕获结果，但 live ECS 捕获 owner 已下沉到 `GASRuntimeShell.TryCaptureASCReadModel()`，尚未变成 BoundaryProjection snapshot 产物。
7. `Assets/GAS/Runtime/General/GASRuntimeShell.cs:10-271` 当前 public 面主要保留 `TryCreateASCCommandPort`、`TryCaptureASCReadModel` / `CaptureASCReadModel`，World / `EntityManager` / runtime singleton / job drain 已转为 internal capability。正向事实是 public 面变窄；剩余风险是 bootstrap、write、read、diagnostics、runner sync 和 singleton 解析仍混在同一个 facade。
8. `Assets/GAS/Editor/GameplayAbilitySystem/GASWatcher.cs:248-286`、`:396-436`、`:493-551` 直接通过 `GASRuntimeShell` 读取 Entity 名称、GlobalTimer、Attribute / Tag / Ability / GameplayEffect buffer；这属于 Editor diagnostics consumer，不应进入 Runtime Core 性能结论，也不能作为目标态 Shell API 形态。
9. `Assets/GAS/Runtime/Effect/GameplayEffectConfigRegistry.cs:627-642`、`:731-757`、`:765-778` 仍持有 runtime `EntityManager` cache 用于 prototype 存活检查和清理；这是 Definition / prototype lifecycle 风险，不能和 Core hot path 或 App Shell command 面混写。
10. 剩余风险：Shell public contract 已有 `ASCHandle` 进展，且 raw ECS 句柄多已 internal 化；但 `GASRuntimeShell` 仍在 assembly 内集中解析 `EntityManager`、runtime singleton 和 job drain，ReadModel 仍由 Shell/Boundary 侧主动捕获 live ECS，而不是消费 `GASBoundaryProjectionSystemGroup` 统一产出的 snapshot / ring buffer；因此 R1 不能仅以“已改成 Request API / ASCHandle”交还，必须补 capability 分级和只读 diagnostics port。

### R6 AutoChess Adapter 与 Validation Evidence Chain

1. `Assets/AutoChessDemo/Integration/GasCore/AutoChessBattleRuntime.cs:24-66` 是 Flow 侧窄入口，负责打开 runtime、reset observation、tick、导出 snapshot / diagnostics 与 shutdown；`AutoChessGasCoreBridge.cs:5-40` 当前是 Session 侧 facade，只转发 unit / driver lifecycle 和 report projection，unit result snapshot 已改由 `AutoChessGasBattleUnitSnapshotProjector` 消费 structured log，bootstrap / teardown / observation / ticker 已由 `AutoChessBattleRuntime` 直接转给 `AutoChessGasRuntimeHost`、`AutoChessGasObservationGateway` 和 `AutoChessGasRuntimeTicker`。
2. `AutoChessGasRuntimeHost.cs:7-60` 仍通过 `GASRuntimeShell.TryResolveRuntimeWorld` 执行 runtime 初始化、system bootstrap、tick group lookup，并通过 `GASRuntimeShell.TryDrainRuntimeJobs()` 承接 job drain；它不再为 catalog session install/uninstall 解析 `EntityManager`。catalog lifetime 的 `EntityManager` capability 已集中到 `AutoChessGasCatalogSession.TryInstall()` / `Uninstall()`，host 是当前活动 bootstrap owner，不是 orphan。
3. `AutoChessGasObservationGateway.cs:11-257` 通过 `GASRuntimeShell` 读取 World、GlobalTimer、EventBus、EventLogSink、RuntimeDebugger，负责 official diff capture、observation reset、structured log / diagnostics snapshot 和 timing counter 写入。
4. `AutoChessGasBattleEntityLifecycle.cs:8-205` 负责 battle unit / driver lifecycle、battle unit key -> `ASCHandle` registry 和 runtime unit resolver；其中 `TryCreateBattleUnitCommandPort()` 仍通过 Shell command-port capability 创建 ASC，并直接给 ASC 写 `AutoChessBattleUnitComponent`。live attribute read helper 已删除。
5. `AutoChessGasBattleEntityLifecycle.cs:105-113` 当前 `DestroyBattleUnit()` 已通过 `ASCCommandPort.RequestDestroy()` 写销毁请求，不再在该文件中直接销毁 ASC / granted ability / active effect；这是正向事实。
6. `AutoChessGasRuntimeTicker.cs:53-57` 调用 `AutoChessGasRuntimeHost.TryCompleteRuntimeJobs()`，再进入 `GASRuntimeShell.TryDrainRuntimeJobs()` 的 `entityManager.CompleteAllTrackedJobs()`；它发生在 AutoChess runner timing 包裹内，只能作为 runner / debugger measurement sync evidence，不能证明 Core hot path 无同步成本。
7. `AutoChessGasCoreContracts.cs:7-58` 的 `AutoChessGasBattleUnitHandle` 当前只持有 battle unit key；旧 unit-handle raw-entity 导出口已无命中。raw ASC `Entity` 不再由 lifecycle 显式导出，当前 `AutoChessGasBattleEntityLifecycle.CreateRuntimeUnitResolver()`（`:67-80`）把 battle unit key 的 `ReportKey` 包装成 `AutoChessRuntimeUnitLink`；`AutoChessGasBattleReportFactProjector.cs:42-77` 只用 `entry.SourceReportKey` / `entry.TargetReportKey` 映射 battle unit index，`AutoChessGasBattleUnitSnapshotProjector.cs:8-56` 只用 `entry.TargetReportKey` 派生 unit result Health / Energy。`Assets/AutoChessDemo/Battle/Report/AutoChessBattleReportBuilder.cs` 只消费 `AutoChessBattleReportFact[]`。
8. `Assets/AutoChessDemo/Battle/AutoChessBattleContracts.cs:267-294` 的 `AutoChessBattleRuntimeTiming` 当前只承载 5 段 GAS physical group timing；`AutoChessRuntimeRunner.CreateTimingSummary()` 输出 `ecsRuntimeTickOnly=true` 和 group timing，没有独立 runner / debugger / physics / render 字段。
9. 正向事实：AutoChess 侧 GAS 访问已经集中到 `Integration/GasCore`，并拆出 `AutoChessBattleRuntime` Flow 入口、`AutoChessGasCoreBridge` Session facade、runtime host、catalog session、observation gateway、battle entity lifecycle、runtime ticker、report fact projector 和 contracts 文件。
10. 剩余风险：集中 adapter 还不是 Thin Adapter；flow runtime / host / observation / lifecycle / catalog / ticker 仍共享 `GASRuntimeShell` internal ECS capability 或 `ASCHandle` compatibility handle。最新正向事实是 host/catalog Interface 已收窄，driver owner evidence 已可通过 raw-Entity-free snapshot 导出；但 `AutoChessGasCatalogSession`、`AutoChessGasBattleEntityLifecycle`、`AutoChessGasObservationGateway` 和 driver runtime store 的 Implementation 仍直接读写 ECS。R6 交还必须给 direct EM owner 分类、report key coverage、headless / scene 同构 evidence、core / boundary / debugger / runner / physics / render timing split，而不是只证明 AutoChess x1/x50 跑通或只输出 5 段 group timing。

R6 当前 direct `EntityManager` owner 分类已进一步收窄：

| owner 分类 | 代码证据 | 当前判定 |
|---|---|---|
| runtime host / catalog bootstrap | `AutoChessGasRuntimeHost.cs:7-29`、`AutoChessGasCatalogSession.cs:6-25`、`AutoChessBattleDefinitionCatalogBuilder.cs:12-31` | host 不再解析 catalog `EntityManager`；catalog lifetime capability 已集中到 catalog session。初始化路径可保留，但 runtime-created catalog entity / Blob dispose 仍需要最终 DefinitionCatalogLifetime owner 证据 |
| unit / driver lifecycle adapter | `AutoChessGasBattleEntityLifecycle.cs:14-205`、`AutoChessBattleDriverComponents.cs:24-250` | driver public raw `Entity` 已退出，driver owner snapshot 已导出；Implementation 仍直接创建 driver entity、切换 driver destroy request、给 ASC 添加 demo component，不是 Thin Adapter 完成态 |
| battle unit registry / report key resolver | `AutoChessGasBattleEntityLifecycle.cs:11-12`、`:67-80`、`:122-149`、`AutoChessGasCoreContracts.cs:7-58`、`AutoChessGasBattleUnitSnapshotProjector.cs:8-56`、`GasStructuredLogExport.cs:419-473` | battle unit key 已替代对外 raw ASC handle；report projector 和 unit result snapshot 通过 report key 映射 battle unit index。Integration/GasCore 内部 registry 仍保存 `ASCHandle`，属于 command compatibility，不是稳定业务 key |
| structured unit snapshot | `AutoChessBattleResultBuilder.cs:25-29`、`AutoChessBattleSession.cs:64-85`、`:110-124`、`AutoChessGasBattleUnitSnapshotProjector.cs:8-56` | 业务 unit result 不再经 adapter live-read ECS；Health / Energy 由 structured log AttributeChange 派生 |
| observation access | `AutoChessGasObservationGateway.cs:11-257` | run boundary reset / replay-debugger snapshot 可作为 Boundary observation，但不得混入 CoreSimulation 性能证明 |
| runner sync / debugger timing | `AutoChessGasRuntimeTicker.cs:9-37`、`:44-113`、`AutoChessRuntimeRunner.cs:138-260` | `CompleteAllTrackedJobs()` 属于 runner timing sync 或 debugger measurement cost |
| report projection key | `AutoChessGasCoreContracts.cs:46-58`、`AutoChessGasBattleEntityLifecycle.cs:193-234`、`AutoChessGasBattleReportFactProjector.cs:7-77`、`GasStructuredLogExport.cs:419-473`、`AutoChessBattleReportBuilder.cs:8-91` | Integration/GasCore projector 已用 `SourceReportKey` / `TargetReportKey` 映射 battle unit index；Battle/Report builder 当前只消费 `AutoChessBattleReportFact[]`，后续要固化 boundary report key coverage 和 missing-key fail gate |

### R2 Generated Active Mutation

1. `Assets/GAS/Runtime/System/Effect/GEActiveEffectLifecycleSystems.cs:48-154` 的 `GASActiveEffectMutationApplySystem` 当前创建 frame-local `NativeList<GEEffectCommandBuffer>`、set-by-caller list、owner range map 和 SourceAttribute snapshot map，并调度手写 `GASActiveEffectRuntime` 中的 collect / finalize / chunk apply jobs。
2. `Assets/GAS/Runtime/System/Effect/GASActiveEffectRuntime.cs:197-220` 起的 owner command collect job 从 owner-local `ActiveEffectMutationCommandBuffer` 收集 command；后续 finalize / apply job 继续按 `TargetAsc -> Sequence -> ParentContextId` 的 owner range 语义聚合和应用。
3. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeActiveEffect.gen.cs` 当前只有 12 行 `GASGeneratedActiveEffectRuntimeMarker.HandwrittenRuntimeOwner = true` marker；它不再承载 gather job、chunk apply job、pre-tick job 或 active-effect store helper。
4. 正向事实：active mutation 已转向 hand-written Runtime owner，且已有 owner 排序、owner range counter、ASC chunk-local applicator 和 x50 runner 证据；`activeMutationEstimatedRandomLookups=0`、`activeMutationOwnerResourceLookups=0`、`activeMutationMigrationCarriers=0`。
5. 剩余风险：当前仍存在 singleton stream / command carrier 迁移面，active mutation 和 active effect slot 的 SourceAttribute snapshot lane 还缺非零业务样本、capacity / spill 与 x100 / x1000 profile；R2 领取时应复核 hand-written owner、marker 和防回流 gate，而不是追旧 `.gen.cs` helper。
6. `Assets/GAS/Editor/CodeGen/Phases/GasGlueCodeGenPhases.cs` 仍是 sourcegen 模板和 validation gate 入口，因此修复必须落回模板、manifest 和 report gate；不能手工编辑 generated marker 当作架构修复。

### R3 Singleton Carrier / Fan-In

1. `Assets/GAS/Runtime/Effect/Component/Dynamic/GEEffectCommandSpecStream.cs:56` 定义 `GEEffectCommandStreamComponent`；`:107`、`:130`、`:139`、`:164`、`:188`、`:245`、`:273` 分别定义 command、set-by-caller、spec、attribute delta、active mutation、gameplay fact、owner-local fact buffer。
2. `Assets/GAS/Runtime/System/Effect/GEEffectCommandSpecStreamPhases.cs:18-37`、`:92-121`、`:226-257` 的 phases 仍以 `GEEffectCommandStreamComponent` singleton entity 和 buffer lookup 为核心输入输出。
3. `GEEffectCommandSpecStream.cs:607-673` 当前保留 `BeginCommandWriter`、`BeginOwnerLocalFactWriter` 和 `PrepareFrameLocalData`，围绕 `GEEffectCommandStreamComponent` singleton owner 清理 / 计数 / 写入；当前未再命中旧 `MergeParallelCommandFanIn` 方法名。
4. `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStreamOwnerContract.cs:311-418` 当前登记 8 个 stream id：EffectCommand / EffectCommandSetByCaller / TypedSimulationFact 的 current carrier 仍为 `SingletonDynamicBuffer`；InstantEffectSpec / ActiveEffectMutation / ActiveEffectNextFrameMutation / OwnerLocalInstantNextFrame / AttributeDelta 的 current / target carrier 为 `OwnerLocalDynamicBuffer`。
5. `GASRuntimeStreamOwnerContract.cs:454-476` 为这些 stream 写入 `EGasRuntimeFrameStreamScaleStatus.MigrationCarrier`、`ProofOrMigrationMarker`、`X50BufferPressure`、`X1000ScaleGate`、`ParallelProducerIntroduced`、`MergeCostDominatesSpecEvaluation` 等 reselect trigger。
6. 正向事实：carrier contract 已显式承认当前 stream 是迁移承载，且 owner-local instant / mutation / delta / fact 切片已经开始退出单一 singleton 写入口。
7. 剩余风险：command / set-by-caller 与 typed fact stream export 仍依赖 singleton DynamicBuffer owner，spec / delta / mutation / fact 也仍存在 owner-local 与 stream export 的混合链；当前更适合作为 proof/migration carrier 证据，而不是 scale-ready carrier 证明。
8. `DiagnosticsSnapshotSystem` 已把 `GasRuntimeDebugger.RecordEffectCommandSpecStreamPressure(...)` 接入 BoundaryProjection diagnostics pass；`GasRuntimeDebugger` 以 `EffectCommandSpecStream` buffer pressure 事件记录 `GEEffectCommandBuffer`、`GESetByCallerValueBuffer`、`GEEffectSpecBuffer`、`ActiveEffectMutationBuffer`、`AttributeModifierBuffer`、`GameplayEventBuffer` 的 count / capacity / budget / overBudget，AutoChess validation summary 输出 `streamCarrierPressureWarnings`、`streamCarrierPeak`、`streamCarrierCapacity`。这是 R3/R8 的 machine-readable carrier pressure baseline，不改变 current carrier 仍是 proof/migration 的事实。
9. 2026-06-07 新增 R3 迁移切片：`GEExecutionCalculationSystem` 不再从并行计算 job 通过 `EndGASStructuralCommitECBSystem.Singleton` / `EntityCommandBuffer.AppendToBuffer` 写 `ExecutionCalculationOutputUpdated` fact。当前路径是 `NativeStream` 收集、`GEExecutionCalculationFactMergeJob` 稳定排序、分配 `NextFactSequence` 后写回 typed fact buffer；`Verify-GAS-RuntimeCoreBoundary.ps1` 已把该约束加入防回流门。该切片证明 execution output fact 可退出 structural ECB fan-in，但不表示 `GameplayEventBuffer` 整体 carrier 已完成目标态。

### R4 StructuralCommit 与 Official Evidence

1. `Assets/GAS/Runtime/System/SystemGroup/GASGroups.cs:62-105` 已定义 `GASStructuralCommitSystemGroup`、`BeginGASStructuralCommitECBSystem`、`EndGASStructuralCommitECBSystem`。
2. `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStructuralPlaybackGateContract.cs:293-309` 已把 required playback gate 绑定到 `GASStructuralCommitSystemGroup` / `EndGASStructuralCommitECBSystem`，但 `contractOnly: true`，所以它是当前约束和证据模型，不是 runtime 已闭环证明。
3. `GASRuntimeStructuralPlaybackGateContract.cs:326-336` 为 route 写入 `directEntityManagerAllowed: false`；`:339-347` 只把 create / destroy / add / remove / add buffer 判为需要 StructuralPlayback；`:434-463` 为 required route 附加 source phase、playback phase、ECB command、bulk query、cleanup 和 local migration evidence 标记。
4. `Assets/GAS/Runtime/Debugger/GasRuntimeOfficialToolDiff.cs:136-155` 会在 Editor / Development build 尝试开启 `EntitiesJournaling` 并读取 Profiler 状态；`:202-251` 统计 create、destroy、add、remove、enable、disable、set、get 等 record type；`:263-286` 输出 `journalingCaptured`、record count、Profiler enable/category 和 capture state。Profiler 不可用或未启用时只输出状态文本，不能当作 captured 证据。
5. `Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs:1091-1207` 记录 system timing 与 Runtime Core counters；`:1947-2094` 创建并导出 snapshot；`:2863-2875`、`:3114-3134` 会导出 `requiredStructuralPlaybacks`、`recordedStructuralPlaybacks`、`ecbCommands`、`bulkQueries`、`profilerMarkers`、`journalingMarkers`、core / physics / render / runner cost split。
6. `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeDebuggerEvidenceGateContract.cs:259-303` 从 frame phase、stream owner 和 structural plan 汇总 evidence gate；其中 `profilerMarkerCount: 4`、`journalingMarkerCount: 1`、core/physics/render/runner split 是 contract 计数，不等于本轮官方工具实际 capture。
7. `Assets/AutoChessDemo/AutoRunner/AutoChessRuntimeRunner.cs:63-82` 输出 Hotspot、Boundary owner、Boundary report key coverage、Runtime Debugger、Runtime Timing、OfficialToolDiff、DataFlow、Sequence 和 Diagnostics；`AutoChessBattleValidationReport.cs` 已把 official diff summary 输出为 `journalingAvailable`、`journalingCaptured`、`journalingStructural`、runtime / journaling create-destroy delta、Profiler state、TopN，并把 Boundary report key coverage 纳入 runtime chain；`AutoChessBattleValidationRun.cs:400-420` 在要求 official diff 时会把 `JournalingCaptured` 作为必要条件。
8. `Assets/AutoChessDemo/Integration/GasCore/AutoChessBattleRuntime.cs` 已成为 Flow 侧窄入口，`AutoChessGasCoreBridge.cs` 已成为 Session 侧 facade；direct ECS access 当前分布在 `AutoChessGasObservationGateway.cs`、`AutoChessGasBattleEntityLifecycle.cs`、`AutoChessGasCatalogSession.cs`、`AutoChessGasRuntimeTicker.cs` 和 `AutoChessGasCoreContracts.cs`，`AutoChessGasRuntimeHost.cs` 保留 world bootstrap / tick group lookup / job drain capability。这说明业务入口更集中，且 report projection / unit result snapshot owner 已改为 report key structured evidence；catalog install/uninstall 已进一步收进 `AutoChessGasCatalogSession.TryInstall()/Uninstall()`，driver owner 证据已通过 raw-Entity-free `AutoChessBattleDriverOwnerSnapshot` 输出并由防回流脚本覆盖。剩余风险是 observation / lifecycle / catalog / ticker 仍通过 internal Shell capability 或 `ASCHandle` 取得 live ECS 句柄，assembly 内 `EntityManager` capability 和 driver runtime store 内部 `_driverEntity` structural owner 仍未完成。
9. `GASRuntimeShell.cs:141-147` 的 `CompleteAllTrackedJobs()` 是当前唯一运行调用点；AutoChess 通过 `AutoChessGasRuntimeHost.TryCompleteRuntimeJobs()` 和 `AutoChessGasRuntimeTicker.CompleteRuntimeJobsTimed()` 间接调用。它发生在 AutoChess runner 对 physical group 的 timing 包裹后，应作为 runner/debugger measurement cost 或 explicit sync evidence 处理，不能被混入 CoreSimulation 热路径优化证明。
10. `AutoChessGasCoreContracts.cs:7-58` 的 `AutoChessGasBattleUnitHandle` 已改为保存 battle unit key，旧 unit-handle raw-entity 导出口当前 0 命中；`AutoChessGasBattleDriverHandle` 当前也已改为 opaque driver id + version，旧 driver `Entity` resolver 已退场，validation summary 输出 `driverHandle=OpaqueDriverIdVersion` / `driverAdapterRawEntity=false`，并新增 `driverOwnerInstalled`、`driverOwnerEnabled`、`driverOwnerHandleMatched`、`driverStructuralCreates`、`driverEnableRequests`、`driverDisableRequests`、`driverUninstallRequests`。`Verify-GAS-RuntimeCoreBoundary.ps1` 已阻断旧 adapter entity resolver 和 public/internal static raw driver `Entity` 导出口回流。R1/R6 的 report projection、unit result snapshot 与 driver adapter identity 风险已从 `Matches(Entity)` / `ASCReadModel(em, ASCHandle)` / public driver `Entity` 收敛为 report key coverage 与 driver owner snapshot。剩余 raw ECS identity 风险是 lifecycle registry 内部 `ASCHandle` command compatibility、`AutoChessBattleDriverRuntimeStore` 内部 `_driverEntity` / direct `EntityManager` owner，以及 Boundary export 解析 report key。
11. 正向事实：StructuralCommit group、Begin/End ECB systems、route contract、Journaling diff 工具、Debugger summary 字段和 AutoChess official diff summary 已存在。
12. `Assets/AutoChessDemo/Battle/Validation/AutoChessBattleValidationReport.cs:341-377` 已能输出 Mermaid dataflow / sequence diagram；当前实现从 `AutoChessBattleResult`、diagnostics 和 official diff 字段拼接字符串，作为导出格式是正向事实，但还不是独立机器验收源。
13. 剩余风险：group / contract / counter 字段存在不等于结构变化已集中；当前还缺 route-level required vs recorded 对账、每类 direct EM owner 分类、Profiler/Journaling captured / disabled / unsupported reason、AutoChess core / boundary / debugger / runner / physics / render timing split、TopN 与 Debugger counter 映射，以及由 structured evidence 派生日志 / 图表的一致证据。
14. R4 已新增一条 R3 联动 evidence：Debugger 的 buffer pressure 事件不再只停留在 EventBus 口径，`EffectCommandSpecStream` carrier pressure 可作为 Hotspot / Reselect trigger 的结构化输入；但它仍属于 diagnostics owner，不能反向驱动 CoreSimulation，也不能替代 Profiler / Journaling official capture。

### R5 SourceGenerator Boundary Gate

1. `Assets/GAS/Editor/CodeGen/GasCodeGenPipeline.cs:20-27` 当前把 Core phases 与 `AutoChessDemoConfigPhase` 拆成两组；`:41-47` 的 `TryRunAll()` 仍先跑 Core 再跑 AutoChessDemo standalone，因此 Core report 与 Demo scenario evidence 必须分 owner 消费。
2. `Assets/GAS/Editor/CodeGen/Phases/GasGlueCodeGenPhases.cs:4159-5504` 的 `ValidationReportPhase` 已统计 `GeneratedRuntimeBoundaryHits`、`GeneratedRuntimeLifecycleHits`、`GeneratedRuntimeSystemRegistrationHits`、`GeneratedRuntimeStructuralChangeHits`、`GeneratedRuntimeOwnershipHits`、`GeneratedRuntimeRandomWriteLookupHits`、`GeneratedRuntimeManagedConfigHits`、`GeneratedRuntimeBoundaryGateMode` 和 `GeneratedRuntimeUnclassifiedBoundaryHits`。
3. `GasGlueCodeGenPhases.cs` 把 generated pure glue / lifecycle migration artifact 分类写入 validation report，并把 Generated Runtime Boundary Gate 写成 `CurrentMode: blocking-unclassified-lifecycle-migration`；`RuntimeAbilityActivation.gen.cs`、`RuntimeEffectInstant.gen.cs` 与 `RuntimeActiveEffect.gen.cs` 当前均已退为 `RuntimePureGlue` marker。真实 ability activation / instant GE / active-effect owner 分别转到手写 `AbilityCommitSystem` / `GASRuntimeDefinitionResolver`、`GEEffectInstantSystems.cs`、`GASActiveEffectRuntime.cs` / `GEActiveEffectLifecycleSystems.cs`。`rg --files` 当前未发现 `ActiveEffectLifecycleOwnerSystems.cs`，manifest / report / schedule 也不再登记该 companion；后续风险改为防回流扫描和四方对账。
4. `Assets/GAS/Generated/CodeGen/GasCodeGenValidationReport.md:16-26` 当前快照为 `GeneratedRuntimeBoundaryHits=0`、`GeneratedRuntimePureGlueArtifacts=4`、`GeneratedRuntimeLifecycleMigrationArtifacts=0`、`GeneratedRuntimeLifecycleHits=0`、`GeneratedRuntimeSystemRegistrationHits=0`、`GeneratedRuntimeStructuralChangeHits=0`、`GeneratedRuntimeOwnershipHits=0`、`GeneratedRuntimeRandomWriteLookupHits=0`、`GeneratedRuntimeManagedConfigHits=0`、`GeneratedRuntimeBoundaryGateMode=blocking-unclassified-lifecycle-migration`、`GeneratedRuntimeUnclassifiedBoundaryHits=0`。
5. `Assets/GAS/Runtime/System/SystemGroup/GASSystemScheduleContract.cs:374-392` 当前由手写 registry 反射注册 generated systems；`Type.GetType(...)` 失败时已直接抛 `InvalidOperationException`，所以 generated type-name、asmdef 或 assembly 缺失不再静默漏注册。
6. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeDefinitionGlue.gen.cs:13-108` 是正向 pure glue：静态 resolver 从 `GASDefinitionCatalogBlob` 构造 `AbilityActivationPlanRecord`、`GECommandSeedRecord`、`ResolvedModifierRecord`，不包含 `ISystem`、ECB 或 query owner。
7. `Assets/GAS/Generated/CodeGen/Runtime/RuntimeAbilityActivation.gen.cs` 当前是 12 行 `HandwrittenRuntimeOwner` / `RuntimePureGlue` marker，不再生成 `AbilityCatalogCommitSystem : ISystem`。这说明 ability activation generated lifecycle owner 已退出当前输出；R5 后续重点是 marker / pure glue / hand-written resolver 对账、缺失 artifact / type mismatch / assembly unavailable 负例、system 数量和 phase budget，而不是继续按旧 generated activation system 审查。
8. `Assets/GAS/Generated/CodeGen/Runtime/DefinitionCatalog.gen.cs:79-84` 暴露 `GASGeneratedDefinitionCatalogBuilder.BuildCatalog(Allocator.Persistent)` 并内部使用 `BlobBuilder(Allocator.Temp)`；这作为 bootstrap / baking 初始化可接受，但必须给 install / dispose owner 证据，不能被写成 hot path runtime lookup 完成证明。
9. 正向事实：generated runtime 已有 Blob catalog、sorted code lookup、pure resolver、Core/Demo phase 拆分和 boundary hit 分类报告。
10. 剩余风险：R5 不能以 `RuntimeForbiddenDependencyHits=0`、`GeneratedRuntimeBoundaryHits=0`、`GeneratedRuntimeUnclassifiedBoundaryHits=0`、`GeneratedRuntimeSystemRegistrationHits=0`、`RuntimePureGlueArtifacts=4` 或 `.gen.cs` 已退为 marker 交还；当前 generated runtime boundary / ownership / lifecycle / structural / random lookup / managed config / unclassified report hit 均为 0，`RuntimeLifecycleMigration` artifact 为 0，且 `ActiveEffectLifecycleOwnerSystems.cs` 当前磁盘缺失。release-ready 前仍必须证明 marker / hand-written owner 对账、缺失 artifact / type mismatch / assembly unavailable 负例、system 数量 / phase budget、generated lifecycle 回流阻断和 catalog lifetime / dispose owner。
