# 00 总览与代码证据事实

> Owner：00-当前架构事实/架构重划分审查事实 | 状态：当前事实子页 | 拆分来源：../架构重划分审查事实.md | 拆分时间：2026-06-08

承载架构重划分审查的当前结论、整体架构审查结论、模块职责事实和代码证据矩阵。

本文件只记录当前代码事实、证据和 DOTS 判定；目标态设计正文回到 ../../01-目标态架构共识/，任务拆分回到 ../../02-主线任务树/。

## 当前结论

当前 Runtime 已经不再是完整的旧 OOP pipeline，也不能继续用“全局 EventBus + request entity lifecycle + 大量主线程 query”这类旧口径描述。当前真实形态是：

```text
Layer 1 Application Shell / Demo
  -> Layer 2 Runtime Boundary command/read/diagnostics adapter
  -> Layer 3 GAS Runtime Core 5 段 FixedStep physical group
  -> Layer 4 generated definition catalog / runtime glue
```

这说明架构方向已经从旧式 OOP 中间层迁出，但还没有达到“纯血 ECS GamePlay Core”。当前最深的问题不是有没有 `ISystem`，而是 **Core 的数据接口仍不够深**：singleton stream、generated instant / execution delta record、`GASRuntimeShell` assembly/internal capability 面、Boundary structured projection 的证据面、pre-tick / execution SourceAttribute snapshot lane 缺口，以及 AutoChess adapter 对 RuntimeShell 能力面的依赖仍把实现细节泄露到边界层和生成链。active mutation 跨 owner SourceAttribute 已在 2026-06-07 进入 read-only snapshot lane；execution output typed fact 已开始从 structural ECB singleton append 迁到 `NativeStream` deterministic merge；后续只保留容量 / spill、merge cost 与真实配置语义证据。

## 整体架构审查结论（codedb 复核）

本轮使用 codedb 复核 `GASRuntimeShell`、5 段主链、generated runtime、ActiveEffectStore / SpecStream、Debugger / structured log、Definition catalog / SourceGenerator 和 AutoChess adapter 后，当前架构可以更准确地划分为“已有 DOTS backbone 的迁移期 ECS Runtime”，而不是“纯 ECS GAS 内核完成态”。

| 审查域 | 当前事实 | 架构判断 |
|---|---|---|
| Runtime physical backbone | 5 段 `FixedStepSimulationSystemGroup` 子 group 已存在，generated runtime 也由 `GASSystemScheduleContract` 注册进主链 | 物理执行域方向正确；generated type-name registry 对缺失 type 已 fail-fast，不再静默跳过；剩余是 system 数量、phase budget、负例验证和 generated lifecycle 退出证据 |
| OOP Shell / Runtime Boundary | `GASRuntimeShell` 同时解析 World / `EntityManager`、创建 command port、捕获 read model、解析 singleton、执行 job drain | 当前 Shell 是多能力 ECS 句柄 facade，不是目标态 OOP Shell；它把 command、snapshot、diagnostics、bootstrap、definition 和 dependency drain 混在同一 implementation 面 |
| Core data interface | command/spec/delta/fact/active mutation 仍大量依赖 `GEEffectCommandStreamComponent` singleton DynamicBuffer carrier；active mutation 和 pending AttributeDelta 已推进到 ASC chunk-local apply；execution output typed fact 已进入 `NativeStream` collect + deterministic merge | 旧 random lookup 和一部分 structural ECB fact fan-in 风险已明显收窄，但 scale-ready fan-in / store 仍未闭合；真正缺口是 owner-local range、更完整的 NativeStream carrier 替换、capacity/spill/reselect evidence |
| Debugger / 日志 | `GasRuntimeDebugger`、structured log、OfficialToolDiff 和 AutoChess validation export 已存在，且 report projection 已有 Boundary report key | Debugger 方向应从“日志输出”升级为 evidence source；contract counter、runtime counter、official capture、validation evidence 和 derived export 必须分层消费 |
| Luban / SourceGenerator | `GASDefinitionCatalogBlob`、generated lookup、pure resolver 已进入 Runtime；generated lifecycle / query / ECB / lookup owner 仍以 classified `MigrationProofOnly` 存在 | SourceGenerator 的目标价值不是继续扩生成 runtime system，而是收权到 immutable catalog、static lookup、pure glue、validation graph；runtime lifecycle owner 必须回到手写 Core lane |
| AutoChessDemo | AutoChess 已成为业务验收 Shell，report projector 与 unit result snapshot 已消费 `SourceReportKey` / `TargetReportKey`；driver handle、registry command compatibility 和 observation gateway 仍触达 live ECS | Demo 分层比旧 bridge 健康，但 Thin Adapter 未完成；应继续拆成 runtime host、catalog session、entity lifecycle、snapshot / diagnostics gateway、fixed tick runner 的 owner evidence |

因此，当前最应优先治理的是 **接口深度和 owner locality**，而不是继续新增 OOP facade 或继续把迁移 proof 写成目标态。一个模块是否“深”应按以下事实检查：它是否隐藏了 query、allocator、dependency、capacity、merge、structural playback 和 diagnostics owner；如果调用方仍必须知道 `EntityManager`、singleton entity、raw `Entity`、buffer handle 或 generated lifecycle hit，它就还只是迁移期浅接口。

## 模块深度与职责重划分事实

本轮 codedb `module_map` 显示 `Assets/GAS/Runtime` 的主要依赖社群仍是一个 141 files / 1947 indexed symbols 的大模块，中心文件包括 `GASRuntimeEntityArchetypes.cs`、`ActiveEffectStore.cs`、`GASDefinitionTable.cs`、`GASSystemScheduleContract.cs`、`GameplayEffectConfigRegistry.cs`、`GasRuntimeDebugger.cs`、`GASRuntimeQueryLayoutPlan.cs` 和 `ASCCommandBufferResolveSystem.cs`。这说明 Runtime 文件夹已经形成 DOTS backbone，但 Core、Definition、Debugger、Shell、generated runtime 仍在同一依赖社群中强耦合；目录分层不能代表职责完成。

按 module / interface / implementation 的深度审查，当前更合理的现实划分如下：

| 现实模块 | 当前 interface 形态 | implementation 暴露的问题 | 事实判定 |
|---|---|---|---|
| Runtime Backbone | 5 段 physical group + `GASSystemScheduleContract` | generated type-name registry、contract-only phase / lane 仍混在同一 schedule owner | backbone 已存在，缺失 type fail-fast 已落地；lane owner、phase budget 和 evidence owner 还需继续拆 |
| Boundary Shell | `GASRuntimeShell` + `AbilitySystemBinding` + `ASCCommandPort` / `ASCReadModel` | 同一 facade 同时服务 World / `EntityManager`、command port、snapshot capture、singleton resolver、job drain | interface 过浅；需要拆成 command / snapshot / diagnostics / bootstrap / definition capability |
| Active Effect Store | `ActiveEffectStore` + generated active runtime + `EffectRuntimeUtility` | store、global index、cleanup helper、generated lifecycle、debugger、factory 都直接依赖同一 store | store 正在变深，但 lifecycle owner、cleanup owner、capacity evidence 和 generated proof 仍未收口 |
| Effect Runtime Utility | static helper methods | `TryGetStaticDefinitionBlob`、`ApplyInstantEffect`、`ApplyInactiveDurationEffect`、`HasOngoingRequirements`、`TryMergeStackingApplication`、duration activate/deactivate/reactivate、`ShouldReject` 仍为空/固定返回；已实现部分集中在 cleanup / removal | 不是 Runtime lane owner，只能记录为迁移期 helper；目标态应由手写 system/job/store owner 接管 |
| Fan-in / Stream | `GEEffectCommandSpecStream` singleton buffers + execution output fact NativeStream merge | command/spec/delta/mutation/大部分 fact 仍共用 singleton carrier，`MergeParallelCommandFanIn` 仍是 managed list + `EntityManager` 写回 | proof carrier 仍未整体退出；execution output fact 是 R3 迁移切片，不是全 stream scale-ready |
| Debugger / Structured Log | `GasRuntimeDebugger`、structured log、OfficialToolDiff | evidence、derived export、runtime counter 和 official capture 仍需分层；`ToEntityArray` 命中集中在 Debugger observation，当前已输出 observation materialization counter | Debugger 是 Boundary evidence owner，不是 Core 计算层 |
| Cue / Presentation Boundary | Cue managed lifecycle + presentation outbox | Runtime/Cue 文件夹内存在 managed UnityEngine side effect，`ToEntityArray` 用于 managed boundary 批处理 | 可以保留为 Boundary cost，但不得进入 Core hot path 结论 |
| Definition / SourceGenerator | Blob catalog + generated lookup + generated runtime artifacts | pure glue 与 generated lifecycle 并存；validation gate 已分类但未把 lifecycle hit 归零 | Definition 方向正确，SourceGenerator 仍需从 runtime lifecycle owner 中退出 |

Deletion test 结论：`GASRuntimeShell`、`EffectRuntimeUtility`、singleton stream 和 generated lifecycle system 不能直接删除，因为调用复杂度会外溢；但它们当前 interface 没有隐藏足够多的实现复杂度，调用者或审查者仍要理解 ECS 句柄、global singleton、store cleanup、capacity、dependency 和 generated owner。因此这一轮“重新划分”的事实结论是：不要继续新增更大的 facade，而要把这些浅 interface 拆成更小、更深且可证据归因的 capability / lane / store owner。

## 2026-06-08 整体架构再审：接口深度与 DOTS 判定

本轮继续用 codedb 对 `GASRuntimeShell`、`GASSystemScheduleContract`、`GEEffectCommandSpecStream`、`GASAttributeModifierDeltaApplySystem`、`RuntimeActiveEffect.gen.cs`、`GEExecutionCalculationSystem` 和 AutoChess adapter 进行截面复核。结论是：当前代码已经具备可演进的 ECS backbone，但架构完成度必须按 **owner 是否足够深** 来判定，而不是按是否存在 `ISystem`、是否删除了 fallback query 或是否跑通 x50 demo 来判定。

### 最新静态截面

| 截面 | codedb 事实 | 判定 |
|---|---|---|
| Runtime 可执行 query / sync | `SystemAPI.Query=0`、`EntityManager.CreateEntityQuery=0`、`state.Dependency.Complete=0`、`.Run(`=0；`CompleteAllTrackedJobs=1`，位于 `GASRuntimeShell.TryDrainRuntimeJobs()` | Core API 健康有明显进展；manual drain 只应归 runner sync / debugger measurement，不能算 Core 性能证明 |
| Query materialization | `ToEntityArray` 运行调用点为 `CueManagedLifecycleSystem` 1 处、`GasRuntimeDebugger` observation 2 处；其余命中是 budget enum 或导出文本 | 当前 materialization owner 是 Boundary / Debugger，不是 CoreSimulation；R4/R8 仍需独立计时和污染隔离 |
| Generated runtime | `Assets/GAS/Generated/CodeGen/Runtime` 当前仍有 7 个 `: ISystem`、7 个 `OnUpdate(ref SystemState)`，并存在 `ComponentLookup` / `BufferLookup` / `EntityCommandBuffer` owner | `GeneratedDefinitionGlue` pure resolver 是正向面，但 generated lifecycle 仍是 `MigrationProofOnly`，不能写成 SourceGenerator 目标态 |
| Runtime module graph | `Assets/GAS/Runtime` 主社群为 141 files / 1947 symbols，Shell、Core、Definition、Debugger、generated runtime 语义邻接仍高度重叠 | 目录和 namespace 还没有形成足够深的 Module；后续验收必须看 dependency graph 与 owner map，而不只看文件夹 |
| Shell capability | `GASRuntimeShell.cs` 被 AutoChess lifecycle / observation / host、Editor watcher、`AbilitySystemBinding` 引用 | 当前 Shell 是多能力 implementation facade；目标态必须拆成 `RuntimeSession`、`CommandPort`、`SnapshotReadModel`、`DiagnosticsSink`、`RunnerSync`、`DefinitionCatalogLifetime` |
| Fan-in carrier | execution output typed fact 已有 `NativeStream` deterministic merge 切片；command/spec/delta/mutation/fact 的默认承载仍大量经过 stream singleton / proof carrier | 删除 fallback 或局部使用 `NativeStream` 不等于 scale-ready；还缺 allocator owner、segment / spill counter、merge budget 和 reselect trigger |
| Attribute / mutation apply | pending AttributeDelta 已是 ASC chunk-local `IJobChunk`；active mutation 使用 gather + owner range + SourceAttribute snapshot map | owner-local apply 是正向事实；gather serial scan、singleton command carrier、generated slot tick / execution magnitude snapshot 仍未闭合 |

### 官方规则对照

| 规则 | 本轮采用口径 |
|---|---|
| `SYS-01` | 权威计算必须落在 ECS System / Job 数据流；当前 `GASManager` / `GASRuntimeShell` 只能作为 bootstrap / Boundary / runner sync implementation 事实，不能作为 gameplay 中间层继续扩大 |
| `QRY-01` | Runtime Core 主线程 query 截面已归零是正向事实；但 Debugger / Cue materialization 必须按 owner 归因，不能混入 Core tick |
| `NAT-03` | `NativeStream` 只有在定义 merge 顺序、segment / memory budget、allocator owner 和 deterministic dispatch 后才算目标 carrier；当前 execution output fact 只是一个正向切片 |
| `DBG-01` | Debugger counters、Profiler / Journaling / official diff、validation evidence 不能互相替代；字符串导出和 Mermaid / 战报只能是 derived export |

### 重新划分后的当前事实结论

1. 当前最深的正向模块不是 Shell，而是 5 段 physical backbone、owner chunk applicator、registered/cache singleton owner、generated immutable catalog 和 structured evidence。
2. 当前最浅的 interface 是 `GASRuntimeShell`、`EffectRuntimeUtility`、singleton stream carrier 与 generated lifecycle system；这些模块的调用方仍需要理解 ECS 句柄、lookup、singleton、capacity、dependency 或 generated owner。
3. 后续架构审查应把 `RunnerSync` 与 `DiagnosticsSink` 从业务 Shell 中拆出；只要 `CompleteAllTrackedJobs`、observation materialization 或 derived export 与 command / snapshot public seam 共用同一 ECS handle，就不能判定 Shell 完成。
4. 后续 SourceGenerator 审查不能只看 `GeneratedRuntimeSystemRegistrationHits=0`；只要 runtime-visible generated artifact 仍拥有 `ISystem`、`OnUpdate`、lookup、ECB 或 NativeContainer owner，就必须保留 `MigrationProofOnly` 退出任务。
5. 后续 Fan-in 审查不能只看是否替换成 `NativeStream`；必须同时证明 sort key、merge order、segment count、capacity / spill、allocator / dispose、battle hash 和 Debugger evidence。

## 代码证据矩阵

| 事实 | 代码证据 | 判定 |
|---|---|---|
| 当前物理主链已是 5 段 FixedStep group | `Assets/GAS/Runtime/System/SystemGroup/GASGroups.cs:11`、`:22`、`:34`、`:58`、`:144` | 正向事实；这是 DOTS physical backbone，不是最终完成证明 |
| 结构变化 gate 已存在 | `GASGroups.cs:66-137` 定义 `BeginGASStructuralCommitECBSystem` / `EndGASStructuralCommitECBSystem` | 正向事实；仍需 Journaling / Profiler 证明来源和 phase |
| handwritten + generated runtime 统一注册 | `Assets/GAS/Runtime/System/SystemGroup/GASSystemScheduleContract.cs:313-321`、`:374-392` | generated runtime 是 Runtime Core 审查对象，不能当外部产物豁免；当前 generated type 解析失败会抛 `InvalidOperationException`，主链注册已具备缺失 type fail-fast 正向证据 |
| OOP Shell 已集中成 Boundary 写入口 | `Assets/GAS/Runtime/AbilitySystem/AbilitySystemBinding.cs:11-70`、`ASCCommandGateway.cs:6-169` | 正向事实；但 `EntityManager` 仍在 Boundary writer / binding implementation 内直接写 live ECS buffer |
| Boundary API 仍保留 raw ECS identity 内部解析面 | `AbilitySystemBinding.cs:59-62`、`ASCHandle.cs:17-25`、`ASCCommandGateway.cs:286`、`:331-392`、`ASCReadModel.cs:64-65` | 外部 target 已主要收缩为 `ASCHandle`，旧 runtime-adapter raw entity 方法已退场；但 Boundary / adapter assembly 内仍可通过 `TryResolveRuntimeEntityForBoundary`、`ASCHandle.RuntimeEntity` / `MatchesRuntimeEntity` 和 `ASCCommandPort.RuntimeEntity` 解析 raw `Entity` |
| ReadModel 已改为捕获式 snapshot | `Assets/GAS/Runtime/AbilitySystem/ASCReadModel.cs:16-159`、`AbilitySystemBinding.cs:49-55` | 本轮正向瘦身；getter 不再持有 live buffer，但 capture 仍由 Shell/Boundary 构造阶段直接读取 ECS，尚未形成统一 BoundaryProjection / ring buffer |
| generated active mutation 已进入 ASC chunk-local applicator | `Assets/GAS/Generated/CodeGen/Runtime/RuntimeActiveEffect.gen.cs:142-201`、`:377-1008` | 当前最大 Core store 风险已从 active mutation random lookup 迁出；跨 owner `SourceAttribute` 已由 gather 阶段只读 snapshot 承载，apply 只消费 `ActiveMutationSourceAttributeSnapshots`；剩余风险是 singleton command carrier、gather serial command scan、capacity / spill 证据和 pre-tick / execution magnitude snapshot lane |
| `EffectRuntimeUtility` 仍是迁移期 helper | `Assets/GAS/Runtime/System/Effect/EffectRuntimeUtility.cs:7-67`、`:106-155`、`:157-205`、`:333-419` | 多个 lifecycle / requirement / instant apply 方法仍为空或固定返回；已实现部分偏 cleanup / removed event / store 移除，不应被写成目标态 Effect lane |
| pending AttributeDelta owner-local apply 已进入 ASC chunk-local applicator | `Assets/GAS/Runtime/System/Attribute/GASAttributeModifierDeltaApplySystem.cs:37-195` | owner-local pending delta 不再物化 owner entity list，`pendingAttributeEstimatedRandomLookups=0` / `pendingAttributeMigrationCarriers=0` 已由 AutoChess x50 复测证明；旧 stream migration fallback 已删除，剩余风险是 singleton stream fact carrier、generated instant delta record 和 fan-in 证明 |
| stream owner contract 已把当前 carrier 标为迁移承载 | `Assets/GAS/Runtime/System/SystemGroup/GASRuntimeStreamOwnerContract.cs:311-388`、`:418-440` | 当前 command/spec/delta/fact/mutation 都仍以 `SingletonDynamicBuffer` 为 current carrier，目标 carrier 才是 `PerThreadNativeStream` / `OwnerLocalDynamicBuffer`，且 scale status 是 `MigrationCarrier` |
| execution output typed fact 已退出 structural ECB singleton append | `Assets/GAS/Runtime/System/Effect/GEExecutionCalculationSystem.cs`、`Tools/Diagnostics/Verify-GAS-RuntimeCoreBoundary.ps1` | `GEExecutionCalculationJob` 写 `NativeStream.Writer`，`GEExecutionCalculationFactMergeJob : IJob` 读取后按 Target ASC + record order 排序，写入 `GameplayEventBuffer` 并分配 `NextFactSequence`；static gate 阻断 `EndGASStructuralCommitECBSystem` / `EntityCommandBuffer.AppendToBuffer` 回流 |
| proof-only stream carrier pressure 已进入 Debugger evidence | `Assets/GAS/Runtime/System/Event/DiagnosticsSnapshotSystem.cs`、`Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs`、`Assets/AutoChessDemo/Battle/Validation/AutoChessBattleValidationReport.cs` | 正向事实；Debugger 能按 `EffectCommandSpecStream` 输出 command / set-by-caller / spec / mutation / delta / typed fact 的 pressure warning、peak 和 capacity，但这只是 R3 替换前的证据基线，不是 carrier 已 scale-ready |
| Debugger 已有 counter 和 official diff 工具 | `Assets/GAS/Runtime/Debugger/GasRuntimeDebugger.cs:824-896`、`:1091-1207`、`:1947-2094`、`GasRuntimeOfficialToolDiff.cs` | 正向事实；需要把性能热点从字符串日志升级为 structured evidence model |
| AutoChess runtime ticker 手动推进 5 段 group | `Assets/AutoChessDemo/Integration/GasCore/AutoChessGasRuntimeTicker.cs:9-37`、`:44-49`、`:53-113` | Demo runner 集中化是正向事实；它仍不是 Core timing 的纯证据 |
| AutoChess adapter 通过 `GASRuntimeShell` 仍直接读写 runtime singleton / entity | `AutoChessBattleRuntime.cs:24-66`、`AutoChessGasRuntimeHost.cs:7-60`、`AutoChessGasObservationGateway.cs:11-257`、`AutoChessGasBattleEntityLifecycle.cs:8-205`、`AutoChessGasCatalogSession.cs:6-16`、`AutoChessGasRuntimeTicker.cs:9-78`、`AutoChessGasBattleReportFactProjector.cs:7-77`、`AutoChessGasBattleUnitSnapshotProjector.cs:8-56`、`AutoChessGasCoreContracts.cs:7-133`、`GasStructuredLogExport.cs:419-473` | 当前边界风险；adapter 已拆成 flow runtime / session facade / host / observation / lifecycle / catalog / ticker / report projector / unit snapshot projector / contracts owner。report projector 与 unit result snapshot 已消费 Boundary report key，不再用 `Matches(Entity)` 或 live `ASCReadModel`；剩余风险是这些 owner 仍通过 internal Shell capability 取得 World / `EntityManager` / singleton / job drain，driver handle 仍暴露 adapter raw entity，registry 仍保存 `ASCHandle` 用于 command compatibility |
| AutoChess 活动入口是 Flow runtime + Session facade + 多 owner | `AutoChessBattleRuntime.cs:24-66` 调用 host / observation / ticker；`AutoChessGasCoreBridge.cs:7-87` 组合 host / observation / lifecycle / report projection；`AutoChessGasRuntimeTicker.cs:12`、`:56` 调用 host，`:68` 调用 observation gateway；`AutoChessGasBattleReportFactProjector.cs:7-77` 消费 report projection | 当前活动事实；不得写成单一聚合口或不存在的万能访问器 |
| `GASRuntimeShell` 已变成多能力 ECS 句柄口 | `Assets/GAS/Runtime/General/GASRuntimeShell.cs:10-271`、`Assets/GAS/Editor/GameplayAbilitySystem/GASWatcher.cs:248-286`、`:396-436`、`:493-551`、`AutoChessGasRuntimeTicker.cs:54-58`、`:69-143` | 当前 Shell 的 public 面主要保留 command/read model，World / `EntityManager` / singleton / job drain 多数已 internal 化；但 assembly 内仍是同一个 facade 同时承载 bootstrap、command port、read model、diagnostics singleton 和 job drain，需要继续按 command / snapshot / diagnostics / bootstrap capability 拆分 |
